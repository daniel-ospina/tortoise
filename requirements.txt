numpy>=1.24
scipy>=1.10
falkordb>=1.0
falkordblite>=0.10
pyyaml>=6.0
fastmcp==3.4.6
prometheus-client>=0.16

fastapi>=0.100.0
uvicorn>=0.23.0


# NOTE: sentence-transformers intentionally NOT in the deploy requirements.
# It downloads ~90MB from HuggingFace on first use, which hangs in the
# sandboxed Fly network and blocks create_point (see fix/embedding-hang).
# Embeddings are optional (Phase 1A) — enable via the pyproject extra only
# where the model is reachable (local dev). Vector search falls back to
# FTS+structural RRF without them.
