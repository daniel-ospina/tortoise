numpy>=1.24
scipy>=1.10
falkordb>=1.0
falkordblite>=0.10
pyyaml>=6.0
fastmcp==3.4.6
prometheus-client>=0.16

fastapi>=0.100.0
uvicorn>=0.23.0
posthog>=3.0


# NOTE: sentence-transformers intentionally NOT in the base deploy
# requirements — keeps the image slim. Dockerfile.hosted installs it via the
# [embeddings] pyproject extra at BUILD time and pre-downloads
# all-MiniLM-L6-v2 into /app/model (HF_HOME), so runtime load is pure local
# I/O with no HuggingFace network access (the runtime download merged in
# 69aefae hung in Fly's sandboxed network and was reverted in 0da50a0).
# Vector search without the model falls back to FTS+structural RRF.
