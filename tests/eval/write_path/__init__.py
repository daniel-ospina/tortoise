"""W2 write-path planted-gold corpus (epic #2080, issue #2097, W2-a).

The frozen planted-gold corpus + fixture/gold/baseline schemas + sealed gold
that the W2 benchmark runner (issue #2098, W2-b) grades against.  This
package is hermetic (pure validation + hashing — no DB, no network, no LLM).
"""
