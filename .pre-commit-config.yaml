# Pre-commit hooks (issue #176, Task 13)
# redis-guard blocks reintroduction of redislite process-leak patterns.
repos:
  - repo: local
    hooks:
      - id: redis-guard
        name: redis-guard (redislite leak patterns)
        entry: python tools/redis-guard.py
        language: system
        types: [python]
        pass_filenames: true
