---
title: "HANDOFF — 2026-08-12/13 session (epic #909, calibration loop + state-centric model)"
type: handoff
domain: operations
created: 2026-08-13
ownedBy: epistemic-team
status: ACTIVE — resume point
---

# HANDOFF — resume here (2026-08-13)

> Read this first in a new session. Everything below is verified on-disk.

## 1. What this session completed

**Epic #909 pipeline + implementation:**
- Pipeline: Plan ✅ (04-plan.md, 8 substeps, review gates) → Decompose ✅ (19 issues, MECE clean) → Verify ✅ (READY). Docs on main (PR #999 + follow-ups).
- Implementation: **9/19 issues merged** (#945 harness+kappa+min-signal, #947 quota P0, #948 ontology, #949 pack v3, #950 pack content, #951 domain_loader, #952 commit_schema, #953 commit endpoint, #960 metrics). All through commit-workflow gates with review records.
- Remaining: #946 (window-2 gate, owner), #954-#959, #961-#963 (mostly gated on #946).

**The calibration loop (the design work):**
- 12+ iterations on the extraction system; the single-pass probe plateaued → the **measured design**: staged pipeline + embedded-relations schema (extractor-spec-v1.md).
- Experiments ledger: `tools/experiments.jsonl` (every run logged; `--report` prints the table).
- 25 model-free tests (`tests/test_calibration_tools.py`) — the deterministic layer (S0 mask, guards, validate_stream, source events, ontology alignment).
- **The stall diagnosis**: the "stall" was unbounded streaming generation (no token cap by owner decision) + requests' per-read timeout never firing. Fixed with a 600s wall-clock deadline + per-chunk retry/skip + chunked extraction (6 EDUs).

**The state-centric model (owner design, filed across 32 issues):**
- The graph stores **STATE (objects + lifecycle + confidence) + POINTS (the logic, statement-only) + EVENTS (timeline incl. decision-as-event)** — never decision objects. "This state is based on these reasons."
- ONTOLOGY v3.8 (#1022): pointKind statement = the only extraction kind; observation removed, hypothesis folded into confidence; decision/vision/strategy/plan/goal/target/humanApproval/event = legacy.
- Issue #1013: remove pointKind:event → Event nodes (events[] in the payload). Issue #1026: pack extraction-behavior slots (packs drive the extractor).

## 2. Where the docs live

- ON main: ONTOLOGY v3.8, README (state-centric), plan/spec alignment (PR #1018), criteria v1 + design-space + hypothesis + calibration protocol + extractor-spec-v1 + window-2 artifacts (PR #1214 — verify).
- Tools on main: tools/{judge_harness,kappa,min_signal,calibration_harness,probe_extractor}.py, tools/experiments.jsonl, tests/test_calibration_tools.py (PR #1213).

## 3. Next steps (priority order)

1. **Owner: window-2 gate labels** (#946) — the rubric has run on 3 real windows (design discussion, #953 implementation, #977 workflow); the formal κ gate is the remaining owner step. Label sheet format: 2026-08-12-window2-owner-label-sheet.md.
2. **Extractor implementation** (slices 6a-6c) — the spec is measured (extractor-spec-v1.md): #954 value brief (with #1026 slots), #955 staged+embedded pipeline, #956 enforcer (validate_stream rules + guards are the E-ladder's deterministic core). Post-#946 (extractor work is gated).
3. **Ledger continuation** — more windows/rolls for medians; connection strategies (embedding/LLM/hybrid) for source events; the staged-embedded median needs ≥2 rolls (fix validated: 57 relations).
4. Close PRs #870 (product-success eval) decision.

## 4. Housekeeping

- Worktrees: tortoise-epic909-impl (feat/epic909-impl — has uncommitted session files), tortoise-epic909-docs, tortoise-epic909-ship, tortoise-epic909-* (per-issue), tortoise (stale main checkout — other sessions' files).
- The experiments ledger is live on main — new runs log automatically via the harness.
- GitHub rate limits were hit repeatedly; REST API via `gh auth token` + record-review.sh + curl merge is the working pattern.
- The pi guard blocks merge/rebase/force-push/checkout in worktrees — use REST ref moves / fresh worktrees / record-review + curl merge.
