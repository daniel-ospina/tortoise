---
title: "Gate record — Window #2 rubric validation run (epic #909 DE2E-1, issue #946)"
type: log
domain: capability
subjects:
  team: epistemic-team
doc_status: live
created: 2026-08-13
ownedBy: epistemic-team
governingAgreement: "#909, #753, #312"
verdict: REVISE
---

# Gate Record — Window #2 (w2-960) — **VERDICT: REVISE**

> **Honest verdict label:** frontier-consensus κ + owner adjudication — **NOT** the DE2E-1 owner-vs-judge statistic (the owner does not blind-label per owner decision; the κ is inter-judge). The gate result is a documented GO/REVISE decision, not a red CI.

## Verdict

| Assertion | Result | Threshold | Pass? |
|---|---|---|---|
| Cohen's κ (inter-judge) | **0.4997** | ≥ 0.60 GREEN | ❌ |
| κ band | **< 0.50 → REVISE** (rubric revision) | — | ❌ |
| Nothing-verdict agreement | 0.581 | 0.90 (pre-registered) | ❌ |
| Min-signal (operational ≥ 1 event) | 11 + 12 events | ≥ 1 | ✅ |
| Per-class agreement | event 0.78 · claim 0.69 · none 0.74 · decision 0.0 · process 0.0 | recorded (no v1 threshold) | — |
| Owner acceptance (disagreement set only — sample step skipped, see deviation note) | **81.8%** (18/22) | ≥ 85% (pre-registered, binding) | ❌ |

**DE2E-1 gate semantics:** κ < 0.50 → **REVISE** — the workflow stops and the rubric is amended. This is a documented decision, not progress. **Slices 954/955/956/957 and 961 remain gated.**

Two REVISE signals: (1) tool κ = 0.4997 below the revise line (primary, pre-registered); (2) owner disagreement-set acceptance 81.8% below the pre-registered 85% floor — **supporting signal**: the pre-registered statistic was the combined-denominator acceptance (disagreement set + ≥10% agreed-EDU sample), which was NOT evaluated (sample step skipped — see protocol-deviation note); the 81.8% is the disagreement-set-only subset, below floor but non-pre-registered.

## Window

- **window_id:** w2-960 · **window_type:** operational
- **Session:** #960 eval-metrics implementation session (`019ff63b-9eef-7f89-a572-308590e3b0e0`), 65 turns (1 user dispatch + 64 assistant narration), fully captured in `~/.tortoise/session-events/2026-08-12.jsonl`
- **Transcript:** `tests/eval/w2-960/transcript.txt` — 65 EDUs, 1-turn-1-EDU segmentation, sha256 `8960db77afb139f95c3e5f89b63599ebdfa32bdc6c789baef9f968e869e73742`
- **Truncation note:** user turn truncated at exactly 5000 chars (capture artifact, no truncation marker); included as EDU 0 per #953 precedent
- **Atomicity:** flags judged per rubric R2 under the 1-turn-1-EDU segmentation; multi-act turns carry the dominant act

## Judges (provenance)

| | Judge A | Judge B |
|---|---|---|
| Model | `anthropic/claude-opus-5` | `qwen/qwen3.8-max` |
| Registry key | `claude-opus-5` (added) | `qwen3.8-max` (added) |
| Temperature | 0.0 | 0.0 |
| max_tokens | 12000 | 8000 |
| Reasoning | OpenRouter default (5.6K reasoning tokens) | bounded `thinking_budget=2000` |
| Label JSON | `tests/eval/w2-960/judgeA.json` | `tests/eval/w2-960/judgeB.json` |
| Complete | 65/65, `incomplete: false` | 65/65, `incomplete: false` |

**Rubric:** harness `RUBRIC_SYSTEM` (tools/judge_harness.py, merged PR #975); class axis == criteria v1 §1 (decision/event/claim/process/none, R1∧R3 conjunction). The full criteria v1 document (entity axis, write mappings, research notes) was NOT part of the judge prompt — its §5 owner-review calls remain open.

**Model-selection note (judge-fidelity finding):** the originally planned DeepSeek judges were unusable on this task — deepseek-v4-pro collapsed to single-class (all-"none"), deepseek-v4-flash emitted duplicate EDUs (273 labels), deepseek-r1-0528 (old) misread process as decision. Root cause: OpenRouter reasoning models consume the shared `max_tokens` budget on internal reasoning (10K+ tokens observed), starving the label JSON. Fix landed in `tests/model_adapters.py` (reasoning bound/disable support). claude-opus-5 + qwen3.8-max were selected as the two independent frontier judges.

## Class distribution

| Class | Judge A (claude) | Judge B (qwen) | Agreed (both) |
|---|---|---|---|
| none | 25 | 43 | 25 |
| claim | 16 | 10 | 9 |
| event | 11 | 12 | 9 |
| process | 9 | 0 | 0 |
| decision | 4 | 0 | 0 |
| **total** | 65 | 65 | **43 agreed / 22 disagreed** |

## Per-class agreement (shared set, N=65)

| Class | Specific agreement |
|---|---|
| event | 0.78 |
| claim | 0.69 |
| none | 0.74 |
| decision | 0.0 (4 claude-only, 0 qwen — R1∧R3 discriminator untested by agreement) |
| process | 0.0 (qwen emits zero process — the process/none boundary is the key ambiguity) |

## Statistical caveats

- **N=65, single window** — criteria v1 §4: "κ on one window is statistically fragile; accumulate labels across ≥2–3 windows." κ CI (delta-method approx, SE ≈ √(po(1-po)/N(1-pe)²) ≈ 0.087 → ±0.17) — the REVISE call sits at the edge of the CI; a second window would tighten this.
- **Prevalence:** process-heavy narration (both judges skew none/process) depresses plain Cohen's κ vs PABAK; PABAK (po=0.66) ≈ 0.32 — same REVISE direction.
- **Decision axis UNEXERCISED:** 0 genuine decision-cued turns verified (scoping cue-scan); the R1∧R3 conjunction was not validated by this run. Decision-discrimination evidence deferred to DE2E-3 fixtures + #961 gold set.
- **S0/tool-dump class unexercised:** capture has no toolResult entries — the S0 filter for tool dumps was not tested.

## Threshold-authority conflict (recorded, resolved)

- **This run governed by:** issue #946 + plan DE2E-1 + tools/kappa.py — 0.60 GREEN / 0.50 REVISE / 0.50–0.60 middle band. `--nothing-threshold 0.90` pre-registered in the #946 scope comment BEFORE any judge run (not post-hoc).
- **Recorded for slice-8 (#960):** criteria v1 §4 RESEARCH proposes κ < 0.40 REVISE / 0.40–0.60 spot-check / ≥ 0.61 monitor, with pooling across ≥2–3 windows. `tests/extraction_eval/thresholds.yaml` (A21) has `judge_agreement_kappa {target: 0.60, rubric_gate: 0.50}` and no nothing-agreement/min-signal floor rows.

## Owner adjudication (daniel-ospina, 2026-08-13 — diagnostic, per owner decision)

> **Protocol deviation (disclosed):** the pre-registered protocol (plan Task 5) requires the disagreement set PLUS a ≥10% stratified sample of agreed EDUs, with acceptance over the combined denominator. The agreed-EDU sample step was **skipped** — acceptance was computed on the disagreement set only (22 EDUs). The combined-denominator statistic was not evaluated. Rationale: the tool verdict (κ = 0.4997 → REVISE) is primary and unaffected; the owner reviewed the full disagreement set in transcript-first order. The 81.8% figure is the disagreement-set acceptance, not the pre-registered combined statistic.

Owner reviewed all 22 disagreements (transcript-first order). Ruling: **all 22 point at content that should be DISCARDED** — "overly granular AI chatter... not clear points, objects, nor events that inform the worldview of the company/product." Matches qwen on 18/22 → **acceptance 81.8%** (< 85% pre-registered floor).

Owner class-level rulings (feed the rubric revision):
- **claim → downgrade:** the agreed claims ("Key finding: A22 is missing") are mostly low-value procedural state-talk; closer to event; low value at pack abstraction level
- **event → ingestion:** "Committed/Pushed/PR created" are events but **better derived deterministically from GitHub event ingestion**, not LLM extraction
- **decision → VALUABLE:** the only class worth keeping; keep R1∧R3 strict
- **none/process boundary:** work-narration should be none (discard), not process-with-log

## Amendment directions (for the rubric-revision workstream this REVISE spawns)

1. **S0/value gate tightening:** operational-session narration ("branch behind origin/main...", "rebase to fresh main", "dispatching the VGATE gate") → `none`, not claim/process/decision. The owner wants near-empty extraction from such windows.
2. **claim class narrowing:** stative state-talk about repo/tooling mechanics is low value — raise the claim bar toward product/company-worldview content (pack abstraction level).
3. **process class:** owner's ruling suggests process-with-log may be unnecessary for routine work narration — either merge into none or require it to be decision-adjacent.
4. **event dedup with ingestion:** extracted events that GitHub already records (commit/push/PR) are redundant — prefer deterministic ingestion; extraction events should be the residue.
5. **decision (R1∧R3):** confirmed the valuable class; discriminator stays strict.

## Artifacts

- `tests/eval/w2-960/transcript.txt` (65 EDUs)
- `tests/eval/w2-960/judgeA.json` (claude-opus-5)
- `tests/eval/w2-960/judgeB.json` (qwen3.8-max)
- `tests/eval/w2-960/report.json` (kappa/min-signal report)
- `tests/eval/w2-960/adjudication.md` (owner adjudication log — this record)
- `tools/build_window_transcript.py` (transcript builder)
- `tests/model_adapters.py` (registry: qwen3.8-max, claude-opus-5, deepseek-v4-pro-noreason + reasoning-control support)
- `docs/plans/2026-08-13-946-w2-gate-run.md` (plan)

## Next steps

- **REVISE is terminal for this gate run** — no GREEN, no extractor unblock.
- Spawn rubric-revision issue (epic #909) with the amendment directions above.
- Window #2 stays a candidate for re-run AFTER the rubric amendment + a second window (pooling per criteria v1 §4).
- Post verdict to epic #909.
