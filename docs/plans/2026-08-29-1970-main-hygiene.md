---
title: "Issue #1970 — Main Hygiene: Repair Push-CI Breakage (lint + onboarding registry lane + hosted-e2e)"
type: engineering
domain: platform
doc_status: live
subjects.team: epistemic-team
created: 2026-08-29
---

<!-- research-path: (none — main-hygiene, no epic research brief) -->

# #1970 Main Hygiene — Repair Push-CI Breakage

> **For Pi:** Use `executing-plans` to implement this plan task-by-task.

**Goal:** Make main's push CI green again (lint + onboarding registry-lane tests + hosted-e2e) so every PR merge unblocks.

**Team:** epistemic-team
**Role:** (omitted)

**Architecture:** Four independent mechanical/root-cause fixes: (1) 18 ruff violations across 3 files (all `--fix`-able or trivial renames); (2) a one-line dict line-merge bug in `DEFAULT_ONBOARDING_STATE` that silently dropped the bare `session_capture_receipt` key from the provisioning default; (3) a test-fixture isolation bug (shadowed closure `db_path`) that makes URI-lane SDKs bind to the shared docker `registry_control_plane` graph — fixed by mirroring `test_hosted_api.py`'s proven `_patch_tortoise_sdk_init` pattern; (4) a deterministic Playwright `json=` kwarg TypeError in the hosted-e2e suite. Plus a coverage-preservation seed and a falsification test pinning the #1877 one-shot guard semantics.

### Pattern Research
> **Gate skipped:** plan touches zero third-party dependencies and zero novel patterns — all fixes are mechanical (ruff rules, one dict entry, a fixture-patch closure, one kwarg rename) with an in-repo precedent for the fixture fix (`tests/test_hosted_api.py::_patch_tortoise_sdk_init`, used by the whole mirror suite).

### Integration Surface Map
- **Lint gate (CI, ci.yml:481 `ruff check .` full-repo):** verified failing on exactly 18 violations in 3 files (hosted_api 7, supabase_control 2, test_free_team_entitlement 9). Test layer: `ruff check .` → 0. Bug flags: RUF059×5 are NOT autofixable (unsafe-fix bucket) — manual rename with per-site review; a single `--fix` pass can introduce NEW violations (UP017 → F401 unused import → I001 re-sort) — loop to convergence.
- **Onboarding state keys (API + registry graph):** `DEFAULT_ONBOARDING_STATE` (provisioning default, written at team creation) vs `_ONBOARDING_DEFAULT_STATE` (read-time merge) vs `_ALLOWED_STATE_KEYS` (derived) vs PATCH model — the parametrized registration test enforces the invariant across all four. Test layer: `test_state_keys_registered_parametrized` + `test_capture_surface_keys_shared_across_defaults`.
- **Team provisioning registry (DB):** fixture isolation in URI mode (shared `registry_control_plane`). Test layer: full-file `test_onboarding_endpoints.py` on a FRESH docker graph.
- **Session capture e2e:** Playwright `APIRequestContext` (no `json=` kwarg). Test layer: hosted-e2e job (CI) — local verification via `grep` for `json=` on api calls.

### Journey Test Map
n/a — no user-facing journey changes (main-hygiene).

### Failure Modes
- Fixture fix implemented as "unshadow + forward caller db_path" instead of "force fixture path, drop kwargs" → carve-out lane leaks via `_make_sdk`'s shared-path fallback (the exact "Team already exists" the fixture comment warns about). **Expected:** force `orig_init(self, fixture_db_path, namespace=namespace)` with NO kwargs forwarding; keep `_FALLBACK_KEEPALIVE.clear()`; keep per-test `tmp_path` (the #1647 redirect derives per-path server graphs — a shared temp dir would derive ONE graph and the 409s persist).
- Mint-seed shape drift (wrong status/role) → the >1-membership disambiguation branch silently reverts to single-team. **Expected:** precondition assert `len(active user-1 memberships) == 2` immediately before the mint.
- `--unsafe-fixes` on the 5 RUF059 → wrong rename breaks the entitlement tests (now in selection). **Expected:** manual rename to `_fake`/`_tc`, run the file before commit.

**Tech Stack:** Python 3.12, ruff 0.16.4 (CI-pinned), pytest, FastAPI TestClient, Playwright (hosted-e2e), FalkorDB (docker lane).

### Task 0: Merge origin/main + register the #1946 drift file

**Intent:** main has moved past the plan's base (38fe001a → 20c4bee6). `ad23cd71` (fix(eval) #1946) added `tests/test_eval_extraction_health.py` WITHOUT a `config/ci-surfaces.yml` registration → `tools/ci_selection.py --integrity` exits 1 on current main → the `changes` job (aggregated by the REQUIRED `python-ci-gate`) reds EVERY PR — the exact drift-gate loop this issue exists to end (the same class as the issue's informational note re: test_free_team_entitlement.py). In-scope as main-hygiene.
**Acceptance:** `git merge-base --is-ancestor HEAD origin/main` true; `python3 tools/ci_selection.py --integrity` exits 0 on the merged tree.
**Files:**
- Modify: `config/ci-surfaces.yml` (register `test_eval_extraction_health.py` under the eval surface)

**Step 1:** `git -c commit.gpgsign=false merge origin/main` (resolve conflicts if any — the plan doc is untracked, no overlap expected).

**Step 2:** `python3 tools/ci_selection.py --integrity` → expect exit 1 with `❌ manifest drift: ['test_eval_extraction_health.py']`.

**Step 3:** Add `- test_eval_extraction_health.py` to `config/ci-surfaces.yml` under the appropriate surface (eval; follow the file's existing structure). No `tests/test_markers.py` ROUTED_NAMESPACES entry needed (0 namespace literals in the file).

**Step 4:** `python3 tools/ci_selection.py --integrity` → exit 0 (residual #1266 coverage-hole warning is pre-existing/out of scope).

---

### Task 1: Lint — 12 ruff violations in 2 files

**Intent:** Indicator 1 of #1970 — `ruff check .` green on main (the repo-convention gate; full-repo command per ci.yml:481). MERGED-TREE baseline (20c4bee6, post Task 0): **12 errors** — `tortoise/supabase_control.py` 2 (RUF100 @2226 unused `# noqa: I001`, UP017 @2362) + `tests/test_free_team_entitlement.py` 10 (I001×3 @13/33/194, RUF100 @24 unused `# noqa: F401`, RUF059×5 @150/209/229/287/293 — `fake`/`tc` unpacked-never-used, UP017 @~186). `tortoise/hosted_api.py`: **0** — main's own 615-line rewrite between the plan's base (38fe001a) and 20c4bee6 already fixed its 7; no longer in scope. The issue's "9" list was stale on both counts.
**Acceptance:** `.venv/bin/ruff check .` exits 0 (0 errors); `tests/test_free_team_entitlement.py` still passes (24 tests) after the RUF059 renames.
**Files:**
- Modify: `tortoise/supabase_control.py` (2)
- Modify: `tests/test_free_team_entitlement.py` (10)

**Step 1:** `.venv/bin/ruff check .` → expect 12 errors (baseline recorded above).

**Step 2:** `.venv/bin/ruff check --fix tortoise/supabase_control.py tests/test_free_team_entitlement.py` → clears 7 autofixable (I001×3 + RUF100×2 + UP017×2).

**Step 3:** Inspect the 5 remaining RUF059 sites in `tests/test_free_team_entitlement.py` (unused unpacked `fake`@150/209/229, `tc`@287/293); rename each to `_fake`/`_tc` manually. Verify each rename is a genuinely unused unpack (the name is not referenced later in the function body).

**Step 4:** Loop `.venv/bin/ruff check .` until 0 errors — twice. Watch for UP017→F401→I001 churn (an import left unused after `timezone.utc`→`UTC` conversion must be dropped).

**Step 5:** `TORTOISE_DB_URI='docker://:falkordb@localhost:6379/tortoise_test_hygiene_ent_<suffix>' .venv/bin/python -m pytest tests/test_free_team_entitlement.py -q` → expect all 24 pass (FRESH graph — the file seeds fixed team ids + counts active memberships, so the shared matrix graph's leftovers could flake count asserts; plan-review P3).

### Task 2: Restore `session_capture_receipt` in `DEFAULT_ONBOARDING_STATE`

**Intent:** Fix the deterministic state-key test failures — the provisioning default must carry the same capture-surface key set as the read-time default (the #1727 registration contract; a raw-state writer can otherwise silently drop the bare receipt key).
**Acceptance:** `test_state_keys_registered_parametrized` + `test_capture_surface_keys_shared_across_defaults` pass.
**Files:**
- Modify: `tortoise/hosted_api.py:1942`

**Step 1:** Split the merged line `"capture_ask_shown": False,  # backward-compat write (#1927 re-ask machinery removed)    "session_capture_receipt": None,` into two lines so `"session_capture_receipt": None,` is a real dict entry.

**Step 2:** `python3 -c` AST/literal check: `session_capture_receipt in DEFAULT_ONBOARDING_STATE` → True.

**Step 3:** Run the 2 state-key tests → pass.

### Task 3: Fix fixture isolation (client + unauth_client)

**Intent:** Root-cause fix for the order-dependent 409 class — the shadowed `db_path` closure parameter lets URI-mode SDK constructions bind to the shared docker graph, leaking `team_created`/`session_recording` state across tests. Mirror the proven `test_hosted_api.py` pattern.
**Acceptance:** Full-file `tests/test_onboarding_endpoints.py` passes on a FRESH docker graph; carve-out lane does not regress below its pre-fix baseline.
**Files:**
- Modify: `tests/test_onboarding_endpoints.py` (client fixture @23-52, unauth_client fixture @54-75)

**Step 1:** Rename the outer closure var (`fixture_db_path`), DROP the now-unused `# noqa: F841` (the rename makes the variable used → the stale noqa would be a RUF100 in Task 7's gate), and rewrite `_patched` in BOTH fixtures to force the fixture path and drop caller kwargs:

```python
def _patched(self, db_path_arg=None, *, namespace=None, **kw):
    # Isolate EVERY SDK construction (registry included) to the fixture's
    # temp DB. URI-mode _make_sdk constructs TortoiseSDK(namespace=...) with
    # db_path=None — forwarding that (the old shadowed-db_path code did)
    # binds to the SHARED TORTOISE_DB_URI graph, leaking state across tests
    # ("Sub-team already created" 409s). Force the fixture path like
    # test_hosted_api.py::_patch_tortoise_sdk_init: under the URI lane the
    # explicit path redirects to a unique per-test server graph; embedded
    # lane stays per-test.
    orig_init(self, fixture_db_path, namespace=namespace)
```

**Step 2:** Keep `_FALLBACK_KEEPALIVE.clear()` in `client`; ADD it to `unauth_client` (mirror parity — harmless, closes a latent anchor leak). ADD the mirror's TEARDOWN parity too: after `TortoiseSDK.__init__ = orig_init`, clear `_FALLBACK_KEEPALIVE` in BOTH fixtures (test_hosted_api.py's `_restore_tortoise_sdk_init` does this — otherwise the last test's anchored SDK, bound to a deleted tmpdir, persists into the next test file in the same process). Keep per-test `tmp_path` (load-bearing).

**Step 3:** Full-file docker-lane run on a FRESH graph → all pass (measured baseline on a fresh URI graph: 4 failed / 26 passed — the issue's 4 named tests; carve-out baseline: 6 failed / 24 passed). NOTE: acceptance is PROVISIONAL — Tasks 4/5 add the 31st test; re-verified at Task 7 Step 3.

**Step 4:** Carve-out sanity: `TORTOISE_TEST_CARVE_OUT=1` run of the file → no regression below the 24-passing baseline; document the q3 exemption as satisfied (CI runs this file DOCKER-only — it is NOT in the 17-stem carve-out set — so the q3 carve-out nondeterminism cannot flip CI; plan-review P2).

> **q3-exemption note (plan-review P2):** the two q3 tests' own `_patched` blocks (`_invoke_session_recording_tool` + wizard block) are intentionally NOT fixed. Their team ids (`team-1728-q3`, `team-1728-re`) are FIXED constants, not per-run unique — their isolation comes from distinct-per-test ids + the session-end journal sweep of `registry_control_plane`. A consistently-forced per-test temp path WOULD preserve their read-after-restore interleaving, so the exemption is a scope/minimal-diff decision, not a technical necessity. Known exemption debt (pre-existing, out of scope): in URI-less mode their `_patched` forwards the shared tempfallback path, and repeated CREATEs of the same Team id produce duplicate nodes (no uniqueness constraint) with `_get_onboarding_state` reading `rows[0]` from an unordered MATCH → order-dependent across runs (passed twice in verification, nondeterministic-by-construction).

### Task 4: Preserve mint-test multi-membership coverage

**Intent:** The fixture fix isolates each test, which would silently delete the mint test's multi-team disambiguation coverage (its stated "production multi-team shape"). Seed a deterministic second membership instead.
**Acceptance:** `test_create_team_then_session_key_mint_registry` passes AND its `>1 memberships` disambiguation branch is exercised (precondition assert proves the seed).
**Files:**
- Modify: `tests/test_onboarding_endpoints.py` (test @396-450)

**Step 1:** After the `rows == [["user-1","owner","active"]]` assertion (before the mint), seed a second active owner membership for user-1 on a DIFFERENT team (uuid-suffixed `seed_team_id` — assert `seed_team_id != sub_team_id` — real Team node WITHOUT `suspended_at` + Membership node with `team_id: seed_team_id` EXPLICIT (the precondition query filters `team_id <> ''` — an implicit-relationship membership would count 0), `status:'active'`, `role:'owner'`). The mint handler filters `user_id + status:'active' + team_id <> ''` and requires `team_id` when `len(memberships) > 1` — the seed makes the disambiguation branch provably live.

**Step 2:** Add the self-verifying precondition before the mint (mirror the mint's own filter — `team_id <> ''`, matching the handler's query):
```python
n_memberships = reg.query(
    "MATCH (m:Membership {user_id:'user-1', status:'active'}) "
    "WHERE m.team_id <> '' RETURN count(m)"
).result_set[0][0]
assert n_memberships == 2, "multi-team disambiguation seed must be active"
```

**Step 3:** Rewrite the now-false shared-state comment (~417-423) to describe the deterministic seed.

**Step 4:** Run the test → pass (mint resolves sub_team_id despite 2 memberships).

### Task 5: 409 falsification test (pin #1877 one-shot guard)

**Intent:** Falsify the issue's "real provisioning regression" hypothesis with a negative test: a second sub-team create on the SAME fresh team must 409 — the #1877 one-shot guard is production-intended, and the fixture fix must not mask a guard regression.
**Acceptance:** New test passes; the 409 class is pinned as test-state collision, not production regression.
**Files:**
- Modify: `tests/test_onboarding_endpoints.py` (new test in `TestOnboardingTeam`)

**Step 1:** Add `test_create_team_reentry_409(client)`: FIRST seed the parent Team node (the #1877 guard reads the MAIN team's persisted state; `_write_onboarding_state` is a `MATCH...SET` that silently no-ops without the node — `team_create` provisions the SUB-team node, never the parent):

```python
_make_sdk(namespace="registry")._get_registry().query(
    "CREATE (t:Team {id:'test-team-1', onboarding_state:'{}'})",
)
```

Then POST `/v1/onboarding/team` twice with the same client, with VALID distinct names (name validation runs BEFORE the guard — an invalid/empty name 400s instead of reaching the 409): first POST `{"name": "reentry-1"}` → 200; second POST `{"name": "reentry-2"}` → 409 with `detail == "Sub-team already created"`. Document the MATCH...SET no-op hazard in the test comment. (Plan-review P1: without the parent seed, both POSTs 200 — the write no-ops and the guard never fires; verified empirically.)

**Step 2:** Run it → pass (with the seed: 200 then 409).

### Task 6: hosted-e2e `json=` → `data=`

**Intent:** Fix the deterministic Playwright TypeError (`APIRequestContext.patch() got an unexpected keyword argument 'json'`) — the #1885-class failure still latent in test_10 (introduced by #1892's lint-fix commit `24126090`; #1939 only fixed test_dashboard_gate.py).
**Acceptance:** No `json=` on any `api.` call in `tests/e2e/hosted/`; the file matches the suite-wide `data=` convention.
**Files:**
- Modify: `tests/e2e/hosted/test_10_session_capture.py:33`

**Step 1:** `json={"session_recording": True}` → `data={"session_recording": True}`.

**Step 2:** `grep -rn "json=" tests/e2e/hosted/` → 0 matches (the only `json=` in the dir is the one being fixed; a line-based `api.patch(...)` grep is a FALSE PASS here because the kwarg sits on the next line).

### Task 7: Full verification

**Intent:** Prove the issue's Verification Checklist end-to-end AND the actual merge gate. (Merge mechanics, plan-review cycles 3-4: branch protection on main requires exactly 6 contexts — `pricing-artifact, docs, test-isolation, license-surface, legal-e2e, python-ci-gate` (verified via `gh api .../branches/main/protection`). `agent-infra-ci`/lint and `hosted-e2e` are NOT required contexts, but the repo convention (PR #1953 admin path) treats red lint as a blocker. The MECHANICAL merge gate is `python-ci-gate` — the docker matrix containing the onboarding surface + the now-in-selection entitlement file + the `changes` job's unconditional `--integrity` (drift closed by Task 0).)
**Acceptance:** `python-ci-gate` green (onboarding surface + entitlement file green on `tortoise_test_matrix`; `--integrity` drift closed by Task 0); `ruff check .` 0 errors (repo convention); hosted-e2e: test_10 `json=` fixed + test_08 402 documented pre-existing + follow-up filed. NOT "all checks green" — hosted-e2e's pre-existing test_08/GRAPH.COPY classes stay non-green by design, handled via follow-up + PR-body documentation (Step 7).
**Files:** none.

**Step 1:** `python3 tools/ci_selection.py --integrity` → exit 0 (pre-existing #1266 coverage-hole warning is out of scope, documented).

**Step 2:** Markers: `TORTOISE_DB_URI='docker://:falkordb@localhost:6379/tortoise_test_matrix' .venv/bin/python -m pytest tests/test_markers.py::test_namespace_literals_guard_passing_or_routed -q` → pass.

**Step 3:** Fresh graph full file: `TORTOISE_DB_URI='docker://:falkordb@localhost:6379/tortoise_test_hygiene_<suffix>' .venv/bin/python -m pytest tests/test_onboarding_endpoints.py -q` → all pass.

**Step 4:** `.venv/bin/ruff check .` → 0 errors.

**Step 5:** Report hosted-e2e status: fix is deterministic and grep-verified locally; CI job is the gate.

**Step 6:** Mirror-suite spot check (Tasks 1-2 modify `hosted_api.py`): `TORTOISE_DB_URI='docker://:falkordb@localhost:6379/tortoise_test_matrix' .venv/bin/python -m pytest tests/test_hosted_api.py -k "team or register or provision or session or key" -q` → pass (NOTE: `-k onboarding` collects ZERO tests in test_hosted_api.py — no test name contains "onboarding"; plan-review P2).

**Step 7:** hosted-e2e full-suite classification (NOT a merge gate — hosted-e2e is not a required context; the issue's Indicator 3 is the #1885-class flake only): `RUN_HOSTED_E2E=1 TORTOISE_TEST_CARVE_OUT=1 .venv/bin/python -m pytest tests/e2e/hosted/ -q` and classify EVERY failure:
- `test_10_session_capture*` `json=` TypeErrors → FIXED by Task 6 (verify 0).
- `test_08_multi_team::test_multi_team_ownership_and_listing` 402 "Create another team requires a paid plan" → PRE-EXISTING on main (verified; written for pre-#1877 multi-free-team semantics; the #1877 `_count_active_free_memberships` gate 402s the second free-team create; the other 3 test_08 tests pass). OUT OF SCOPE per the issue's fix list + dispatcher constraint (no other test files) → file a follow-up issue carrying the concrete fix proposal (`bump_team_tier(api, t1, "team")` before the second create — the helper is proven in the same file) and document in the PR body.
- `test_05_backup_restore`/`test_12_selfhost_migration` GRAPH.COPY 503 fork errors → PASS LOCALLY (verified cycle 3); the "suspected local macOS fork limits" theory has no current support. Pre-file the follow-up issue to cover BOTH this class and test_08 (asymmetric handling would leave a second untracked class); after push, inspect the CI hosted-e2e log (the job logs to /tmp/hosted-e2e.log and its parse is warn-only) for GRAPH.COPY before merge.
- RULE for any other failure: PR-INTRODUCED failure (not reproducible at base commit) → BLOCK + fix; PRE-EXISTING failure (reproducible on origin/main) → follow-up issue + PR-body documentation, never silent.

**Step 8:** CI-lane checks (the REAL lane — `python-ci-gate` runs these on the SHARED `tortoise_test_matrix`; a fresh-graph pass does not catch matrix-leftover flakes): `TORTOISE_DB_URI='docker://:falkordb@localhost:6379/tortoise_test_matrix' .venv/bin/python -m pytest tests/test_free_team_entitlement.py tests/test_onboarding_endpoints.py -q` → all pass (plan-review P2: a full-file onboarding run on the matrix graph proves the fixture fix survives DIRTY leftovers, not just a fresh graph — a construction escaping the patch would fail here).

**Step 9:** Confirm the merge gate: after push, verify the 6 required contexts (`pricing-artifact, docs, test-isolation, license-surface, legal-e2e, python-ci-gate`) report green/skipped on the PR; lint + hosted-e2e are convention gates — document their state in the PR body. **PR-body documentation checklist (plan-review P3 — the code-review gate will flag these):** the 12-vs-9 lint delta (issue listed 9 across 2 files; merged main shows 12 across 2 files — `hosted_api.py`'s 7 already fixed by main's own rewrite between 38fe001a→20c4bee6, `test_free_team_entitlement.py`'s 10 are the new delta; Indicator 1 = full-repo `ruff check .`); the q3 fixture exemption (scope/minimal-diff); the Task 0 drift registration; test_08/GRAPH.COPY pre-existing classes + follow-up issue links. **Task 4 is MANDATORY despite being CI-invisible on skip** (its coverage loss only shows when the mint disambiguation branch dies silently).

> **Registry-graph drift note (plan-review P3):** pre-fix, the shared `registry_control_plane` graph is URI-independent and only swept at a CLEAN session end — a killed/timeout/concurrent CI run leaves `team_created`/teams behind, flipping the next run's failures between 409 ("Sub-team already created") and 400 ("Team 'X' already exists"). The Task 3 fixture fix makes this file immune (per-test graphs); no further change required.

> **Cross-task dependency (plan-review P3):** Task 5's 200-then-409 determinism is 100% downstream of Task 3's fixture patch — the seed, the handler reads/writes, and `sdk.team_create`'s internal constructions must all bind to the SAME per-test graph. If Task 3 regresses, Task 5 silently tests the shared graph (both POSTs can 200). Re-run the full file after Task 3's acceptance before trusting Task 5.

<!-- plan-review: status=clean (5 cycles: scope-verify 2 cycles, plan-verify 4 cycles; all P0/P1 fixed; controller-incorporated P2/P3s documented inline; final round NO P0/P1) -->
