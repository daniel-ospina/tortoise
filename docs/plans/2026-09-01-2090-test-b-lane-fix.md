<!-- research-path: docs/scoping/2026-09-01-2090-test-b-lane-scoping.md -->

# #2090 — test (b) lane intermittent 403s: embedded-lane data-loss fix

> **For Pi:** Use `executing-plans` to implement this plan task-by-task.

**Goal:** Eliminate the intermittent 403/410-class `test_export_delete` CI failures by killing the embedded-lane keepalive-anchor churn (b1 `TORTOISE_DB_PATH` pin) and the dropped-seed GC-NOSAVE trigger (seed-hold), with a deterministic zero-eviction regression guard.

**Team:** unknown (swarm SOR unreachable — see issue)
**Role:** —

**Architecture:** Test-file-local, precedented fix (Approach A, scoping-verified): mirror the in-repo #1950 fixture pattern (pin `TORTOISE_DB_PATH` to the fixture temp DB so `_anchor_usable` stops evicting the #1607 keepalive anchor every call) + the f69c775d/`_SEED_SDKS` seed-hold pattern (never drop the seed SDK) + a committed eviction-counter 0-guard with a `_db_path` discriminator. Files: `tests/test_export_delete.py` + `tests/test_free_team_entitlement.py` (cross-file churn representative for the second symptom class). No production code, no CI config changes.

### Pattern Research

> **Findings date:** 2026-09-01

> **Gate skipped:** plan touches zero third-party deps (falkordblite 0.10.0 vendored; `redislite/client.py:183-200` CLIENT LIST semantics read in-repo). All patterns are in-repo precedent with 2+ examples: #1950 `TORTOISE_DB_PATH` pin (test_hosted_api.py:174-202, test_capture_session.py:1759-1764, test_pack_state.py:751/:802), `_SEED_SDKS` hold (test_suspension_parity.py:156-163, test_dr_endpoints.py:123-130), `_close_seed_sdks` (test_dr_endpoints.py:34-39), `_close_keepalive_anchors` (test_hosted_api.py:144, test_capture_session.py:1724), f69c775d seed-return. External research not demonstrated per activation rule — scoping Phase 1.5 justified-skip recorded.

### Integration Surface Map

| Surface | Test Layer | Expected Verification | Notes |
|---|---|---|---|
| `ha_mod._FALLBACK_KEEPALIVE` anchor lifecycle (evict/close/pop) | regression guard | eviction-counter 0-guard == 0 post-fix (RED `>=1` pre-fix) | dict-subclass pop override + `_db_path` discriminator (survives close) |
| env `TORTOISE_DB_PATH` pin vs `_make_sdk`/`_registry_anchor` computed path | integration | anchor REUSED (no per-call eviction); #1950 fixture pattern | pin in both fixtures; pop at restore |
| dropped-seed GC-NOSAVE (seed SDK lifetime) | regression | seed data survives across `_registry_anchor()` calls; masked-pin assert == 1 | `_SEED_SDKS` hold + function-scoped close |
| second symptom class (dead-socket ConnectionError/500) | cross-file | test_free_team_entitlement green both lanes | representative migration |
| #1502 stale-rebind guard | unit | `test_make_sdk_reuses_healthy_anchor` + `test_make_sdk_rebinds_stale_anchor` green (appended to gate) | zero production change keeps them green |
| docker lane immunity | integration | both files green under `TORTOISE_DB_URI` (data-plane via projection redirect; keepalive branch never engages) | post-merge full-matrix = URI evidence (Step 6-ii) |
| CI tier-2 embedded lane (the failing lane) | e2e | 10 consecutive green tier-2 runs on the PR | acceptance (i) |

### Task Overview

| Task | Deliverable | Gate |
|---|---|---|
| 0 | Trigger-discriminator spike (embedded carve-out; deleted before merge) | G0: trigger sub-question recorded |
| 1 | RED: eviction counter + warn probe-failure counter | G1: counter `>=1` deterministic on embedded |
| 2 | GREEN: b1 pin + `_close_keepalive_anchors` restore | G2: fresh-context verifier |
| 3 | seed-hold + function-scoped close + masked-pin assert | G3: gate green ×10 embedded |
| 4 | cross-file representative migration | G4: verifier + both lanes green |
| 5 | full regression gate both lanes + #1502 pair | G5: all green, ×10, both lanes |
| 6 | docs row + follow-up issues + streak protocol | G6: commit-workflow + streak evidence |

---

### Task 0: Trigger-Discriminator Spike (experiment — not shipped)

**Intent:** Resolve the open trigger sub-question (refcount-at-scope-exit vs cycle-pinned cyclic-GC; enter-pin `_get_proj()`-failure variant) so the scoping doc's trigger-timing record is evidence, not assertion (O/I/T indicator 1).

**Acceptance:** `spike/2090_trigger_discriminator.py` runs the deterministic construction on the EMBEDDED carve-out shape and **records which timing variant fires, OR narrows the sub-question with evidence** (the construction is informative either way — no-variant-firing is a legitimate narrowing); results written to the scoping doc's Fix-phase experiment section; spike file deleted (or untracked) before merge.

**Files:**
- Create: `spike/2090_trigger_discriminator.py` (temporary)
- Test: none (standalone script; run via `TORTOISE_TEST_CARVE_OUT=1 uv run python spike/2090_trigger_discriminator.py`)

**Step 1:** Read `falkordblite`/`redislite` client source (`redislite/client.py` `_connection_count`/`_cleanup`) for CLIENT LIST semantics.

**Step 2:** Write the spike implementing the scoping doc's redesigned construction verbatim:
1. Anchor held (lifespan pin via registry-mode TestClient enter) → seed dropped → drift-evict → `gc.collect()` between evict-close and new-anchor connect — construction only, NO kill assert.
2. Control: `gc.collect()` with anchor held → daemon survives, seed data intact.
3. Discriminator: force the enter-pin to fail (anchor ABSENT at seed-collection time) → assert survival immediately after scope exit (NO `gc.collect()`), then `gc.collect()` and re-assert.
4. Instrument: `_gc_close` spy (connection count, action) + eviction counter; unit-check the counter's `_db_path`-vs-computed classification on one drift pair vs one probe-failure pair.

**Step 3:** Run on the embedded carve-out shape — `TORTOISE_FAST_ATEXIT=1 uv run python spike/2090_trigger_discriminator.py` (⚠️ the spike is a STANDALONE script and does NOT load tests/conftest.py, the only place that sets `TORTOISE_FAST_ATEXIT=1` locally; WITHOUT the flag the last-client GC takes redislite's normal `_cleanup` = SAVE (data survives) instead of the #1371 NOSAVE fast-close, so the discriminator would vacuously answer "survives" every time — misattributed to timing-B. Set the flag inside the spike (`os.environ.setdefault("TORTOISE_FAST_ATEXIT", "1")`) at module top or on the command line. `TORTOISE_TEST_CARVE_OUT=1` is a pytest opt-in — inert for a bare script; drop it. URI must be UNSET (a URI redirects constructions to server mode and vacates the experiment). Document the SAVE-fallback semantics so results are interpretable either way.)

**Step 4:** Record the resolution in the scoping doc §Fix-phase experiment; delete or leave untracked the spike file.

---

### Task 1: RED — Eviction Counter + Warn Probe-Failure Counter (no fix yet)

**Intent:** Deterministically demonstrate the churn enabler (per-call `_anchor_usable` path-drift eviction) pre-fix — the RED leg of TDD and the permanent 0-guard skeleton.

**Acceptance:** `reg_client` counter asserts `evictions >= _EXPECTED_DRIFT_EVICTIONS` (constant 1) and the assert PASSES with actual ≥1 on the embedded carve-out lane (deterministic demonstration of the churn enabler — G1); the run MAY flake on the pre-existing intermittent 403 — that is the bug being fixed, not a gate failure; warn-only probe-failure count reported.

**Files:**
- Modify: `tests/test_export_delete.py` (module constants + fixture)

**Step 1:** Add module constants `_EXPECTED_DRIFT_EVICTIONS: int = 1` and `_DRIFT_EVICTION_COUNT` counter mechanics (a `dict` subclass installed as `ha_mod._FALLBACK_KEEPALIVE` AFTER `_patch_tortoise_sdk_init`'s clear, overriding `pop()`):
- **Signature (pinned):** `def pop(self, key, default=None)` — must accept the default arg (all production/helper pops pass it: `pop(namespace or "", None)` at hosted_api.py:162/:205, `pop(ns, None)` in `_close_keepalive_anchors`); missing key → return default, count nothing; `None` value → count nothing.
- At `pop()`: if `enabled` and the popped value's `_db_path` (survives `close()` — `_proj` is nulled, `_db_path` is set in `__init__`) != the env-computed path (replicate `_make_sdk`: `TORTOISE_DB_PATH` else `/data/tortoise.db`, makedirs-OSError fallback `tempfile.gettempdir()/tortoise.db`, `:memory:` same-path; compare with the same abspath normalization `_anchor_usable` uses, hosted_api.py:114-125) → count as drift-eviction. Equal → count as probe-failure in the warn-only counter. No `_db_path` → ignore. Pin the replication to source lines (`# mirrors tortoise/hosted_api.py:141-149`).
- **Fresh instance per test (pinned):** construct the counting dict FRESH inside the `reg_client` fixture at install (or reset `enabled=True` + zero both counters in the install step) — a shared module-level instance would carry the prior test's `enabled=False` and degrade RED determinism.
- `enabled` flag; `enabled=False` BEFORE `_restore_sdk_init` so restore-time pops (via `_close_keepalive_anchors`) are uncounted. Then restore the original dict. **Never swap-then-close** (strands anchors — the #1950 leak).

**Step 2:** In `reg_client` fixture: capture `_orig_fallback = ha_mod._FALLBACK_KEEPALIVE` at install; install the fresh counting dict AFTER `_patch_tortoise_sdk_init`'s clear (and before `with TestClient`); inside the test body after `yield` (still in try), and **the ENTIRE teardown block must live in the fixture's `finally`** (body-failure path included): assert `drift_evictions >= _EXPECTED_DRIFT_EVICTIONS` → `enabled=False` → `_restore_sdk_init` → `_close_seed_sdks()` → `ha_mod._FALLBACK_KEEPALIVE = _orig_fallback`. On a test-body failure the exception re-raises at `yield` and the `finally` still runs the full teardown — the counting dict must NEVER remain the module attr. Log the probe-failure count (warn-only).

**Step 3:** Run embedded: `env -u TORTOISE_DB_URI TORTOISE_TEST_CARVE_OUT=1 uv run pytest tests/test_export_delete.py -q`  # env -u: a URI-set shell would silently run the docker lane
Expected: counter assert PASSES with actual ≥1 (every reg_client test does ≥1 registry op → ≥1 drift-pop pre-fix; export/delete produce ≥2: `_team_node` + `_require_owner` both hit `_registry_anchor`). Record the actual counts as G1 evidence.

**Step 4 (note):** Do NOT add the masked-pin assert yet — its `_registry_count` SDK-drop is itself the drop class until Task 3's hold lands.

---

### Task 2: GREEN — b1 `TORTOISE_DB_PATH` Pin + Deterministic Restore-Close

**Intent:** Kill the deterministic churn enabler: make `_anchor_usable` see the anchor's path as usable so the #1607 keepalive anchor is REUSED, not evicted+closed, per registry access (the #1950 pattern verbatim).

**Acceptance:** Counter asserts `drift_evictions == 0` on embedded (GREEN); `_close_keepalive_anchors` closes deterministically at restore; #1502 regression pair stays green (zero production change); supabase-mode + #1502 non-interference documented.

**Files:**
- Modify: `tests/test_export_delete.py` (sb_client/reg_client fixtures, `_restore_sdk_init`, new `_close_keepalive_anchors`)

**Step 1:** In `sb_client` AND `reg_client`, immediately after `_orig = _patch_tortoise_sdk_init(db_path)`:
```python
# #1950: pin TORTOISE_DB_PATH to the SAME temp DB the patched init forces,
# so _make_sdk's keepalive anchor path matches and the anchor is REUSED
# instead of evicted + closed on every registry access (each eviction shut
# the redislite daemon down mid-test, losing the seed between this fixture's
# write and the gate read → 403).
os.environ["TORTOISE_DB_PATH"] = db_path
```

**Step 2:** Extend `_restore_sdk_init` (pop-then-close order; both fixtures share ONE restore):
```python
def _restore_sdk_init(_orig):
    os.environ.pop("TORTOISE_DB_PATH", None)
    ha_mod.TortoiseSDK.__init__ = _orig
    _close_keepalive_anchors(ha_mod)   # #1950: deterministic SHUTDOWN SAVE, not GC-timed
    app.dependency_overrides.clear()
```

**Step 3:** Add local `_close_keepalive_anchors(ha_mod)` — verbatim copy of test_hosted_api.py:144-153 with a source-pin comment (`# mirrors tests/test_hosted_api.py:144 — keep in sync`); each `pop().close()` wrapped in try/except (test_dr_endpoints.py:34-39 precedent). Same copy used in Task 4 (same mechanism in both files).

**Step 4:** Flip the counter assert to `== 0` (set `_EXPECTED_DRIFT_EVICTIONS = 0` and use `==`); keep the operator semantics explicit (RED was `>=`, GREEN is `==` — keep both in one diff hunk).

**Step 5:** Run embedded gate: `env -u TORTOISE_DB_URI TORTOISE_TEST_CARVE_OUT=1 uv run pytest tests/test_export_delete.py -q`  # env -u: a URI-set shell would silently run the docker lane
Expected: PASS (counter 0; exposed set green). ⚠️ A rare 403 here is the enter-pin `_get_proj()`-failure variant (Task 3's seed-hold covers it), NOT a regression — rerun and proceed to Task 3. The #1502 cross-test eviction semantics are preserved by the #1502 tests' in-test `TORTOISE_DB_PATH` monkeypatch — within this file the per-test pin+clear means no eviction.

**Step 6:** Document (one line each): supabase-mode no-breakage (authz control-plane-only; boot purge early-returns → no anchor at enter); #1502 tests unaffected (they monkeypatch `TORTOISE_DB_PATH` in-test, overriding the pin; pin popped at teardown). **Coverage statement:** the env pin is process-wide — it covers TestPurge's direct `ha_mod._purge_deleted_teams()` calls (no TestClient involvement); the enter-time lifespan purge runs after the pin so the enter anchor is created pinned.

**Gate G2:** fresh-context verifier on the fixture change (#1502/#1950 invariants).

---

### Task 3: Seed-Hold (family a) + Function-Scoped Close + Masked-Pin Assert

**Intent:** Neutralize the nondeterministic trigger — the dropped-seed GC-NOSAVE — INCLUDING the enter-pin-`_get_proj()`-failure variant (seed-hold covers it via path-aligned reads; the pin alone cannot fix a `proj is None` failure).

**Acceptance:** `_seed_registry` + `_registry_count` append their SDKs to `_SEED_SDKS`; per-test `_close_seed_sdks()` runs in fixture finally AFTER `_restore_sdk_init`; masked-pin assert lands; gate green ×10 embedded (G3).

**Files:**
- Modify: `tests/test_export_delete.py` (`_seed_registry`, `_registry_count`, fixtures, `test_export_requires_owner_registry`)

**Step 1:** Add module `_SEED_SDKS: list[TortoiseSDK] = []` (typed — suspension_parity:156 precedent).

**Step 2:** `_seed_registry` (:195-214): append the SDK BEFORE the writes (suspension_parity:163 precedent); keep `-> None` signature (all 9 call sites inherit the hold). Comment mirrors suspension_parity (documents the dropped-SDK GC-NOSAVE mechanism + "flaky registry-mode 403s").

**Step 3:** `_registry_count` (:219-230): same append (covers :581/:648-652/:676-677).

**Step 4:** Add `_close_seed_sdks()` (while _SEED_SDKS: try: pop().close() except Exception: pass — test_dr_endpoints.py:34-39 precedent verbatim) and call it in both fixtures' `finally` AFTER `_restore_sdk_init` (pinned position in the finally: counter assert → `enabled=False` → `_restore_sdk_init` → `_close_seed_sdks()` → restore dict). Function-scoped close collapses peak daemons to ~2 concurrent (per-test tempdirs make cross-test reads impossible).

**Step 5:** Land the masked-pin assert in `test_export_requires_owner_registry` (:526-531): after `_seed_registry(db_path)`:
```python
assert _registry_count(db_path, "Team", "reg-team-1") == 1  # #1950 fail-loud self-verify
```

**Step 6:** Run the embedded gate ×10: `for i in $(seq 10); do TORTOISE_TEST_CARVE_OUT=1 uv run pytest tests/test_export_delete.py -q || break; done`
Expected: 10 consecutive PASS (0 flakes, 0 evictions). Record peak RSS + live-server count per run (best-effort never-fail sampler) AND the runtime delta vs the pre-fix baseline (the added per-test server shutdown: SAVE + pid-exit poll, typical ≤1s, worst-case ~10s per `_cleanup` wait loop).

**Step 7 (verify note):** Confirm the inline seeds already hold locally (test_export_uses_stored_graph_name :534, cascade :563, revoke :604, purge :662 — no change needed).

---

### Task 4: Cross-File Churn Representative — test_free_team_entitlement

**Intent:** Cover the second symptom class (stale-socket ConnectionError/500 — the #2049-b observed sibling) so the fix's regression gate includes a file whose failure mode is the churn/dead-socket path, not just the 403-empty-respawn path.

**Acceptance:** `client` (:59-80) and `reg_client` (:244-270) migrate to the same b1 pin + close-at-restore; green on both lanes (G4).

**Files:**
- Modify: `tests/test_free_team_entitlement.py`

**Step 1:** In `client` (:59-80, supabase): after the `_FALLBACK_KEEPALIVE.clear()` in SETUP, add `os.environ["TORTOISE_DB_PATH"] = db_path` (same #1950 comment). **In the `finally`: REPLACE the existing `ha_mod._FALLBACK_KEEPALIVE.clear()` with `os.environ.pop("TORTOISE_DB_PATH", None)` + the local `_close_keepalive_anchors(ha_mod)` copy (close-before-clear — ⚠️ appending after the existing clear would drop anchors unclosed first, silently defeating the deterministic-close half).** Same mechanism as Task 2 Step 3 (SAME source-pin).

**Step 2:** In `reg_client` (:244-270, registry): identical three-line migration — setup pin after the clear; finally: REPLACE `clear()` with pop-env + `_close_keepalive_anchors`.

**Step 3:** Note (:262) — the dropped `_make_sdk(namespace="registry")._get_registry()` SDK is benign (the same call's anchor eagerly holds a connection → count ≥ 2 at GC → disconnect-only); add a one-line comment.

**Step 4:** Run both lanes (prerequisite: FalkorDB up — `docker compose -f ../eldato/operations/memory/docker-compose.yml up -d` per AGENTS.md, or the repo's docker-compose falkordb service; currently running):
- `env -u TORTOISE_DB_URI TORTOISE_TEST_CARVE_OUT=1 uv run pytest tests/test_free_team_entitlement.py -q`
- `TORTOISE_DB_URI='docker://:falkordb@localhost:6379/tortoise_test_matrix' uv run pytest tests/test_free_team_entitlement.py -q`

**Gate G4:** fresh-context verifier on the representative migration.

---

### Task 5: Full Regression Gate — Both Lanes + #1502 Pair

**Intent:** Prove the fix on the actual failing lane (embedded carve-out) AND the docker lane (data-plane immunity via projection redirect; keepalive branch never engages), plus machine-check the zero-production-change claim via the #1502 regression pair.

**Acceptance:** All gate sets green on both lanes; ×10 embedded with zero churn-class flakes; runner IDs + peak RSS recorded (G5).

**Files:**
- Test: `tests/test_export_delete.py`, `tests/test_free_team_entitlement.py`, `tests/test_hosted_api.py` (2 nodeids)

**Step 1:** Embedded gate (the failing lane):
```bash
TORTOISE_TEST_CARVE_OUT=1 uv run pytest tests/test_export_delete.py tests/test_free_team_entitlement.py -q
TORTOISE_TEST_CARVE_OUT=1 uv run pytest tests/test_hosted_api.py -k "make_sdk_reuses_healthy_anchor or make_sdk_rebinds_stale_anchor" -q
```

**Step 2:** Docker immunity gate:
```bash
TORTOISE_DB_URI='docker://:falkordb@localhost:6379/tortoise_test_matrix' uv run pytest tests/test_export_delete.py tests/test_free_team_entitlement.py -q
```
(Annotation: docker runs DO exercise the URI data plane via the projection redirect — neither file is in TEST_NO_REDIRECT_STEMS; the keepalive-anchor branch never engages. Full-matrix URI evidence = Step 6-ii post-merge.)

**Step 3:** ×10 repeated embedded gate-set runs (restated — the FULL gate set, not just one file): `for i in $(seq 10); do env -u TORTOISE_DB_URI TORTOISE_TEST_CARVE_OUT=1 uv run pytest tests/test_export_delete.py tests/test_free_team_entitlement.py -q || break; done`; record runner ID + peak RSS + live-server count per run (best-effort NEVER-FAIL sampler — importable-gated silent-skip, never raises; makes the resource hypothesis falsifiable).

**Step 4:** Confirm the full test_export_delete file is 31/31 on both lanes.

**Gate G5:** all green, ×10 embedded, both lanes.

---

### Task 6: Docs Row + Follow-Up Issues + Streak Protocol

**Intent:** Close the loop: index the scoping doc, file the systemic follow-ups (not absorbed), and define the streak protocol that gates merge.

**Acceptance:** `docs/00_index.md` row added; follow-up issues #1 (B waves) and #2 (C2 optional) filed; streak protocol recorded; commit-workflow run (G6).

**Files:**
- Modify: `docs/00_index.md`

**Step 1:** Add a row to `docs/00_index.md` for the scoping doc (recent-convention format): `Test (b) lane 403/export-delete scoping (#2090)` → `docs/scoping/2026-09-01-2090-test-b-lane-scoping.md`.

**Step 2:** File follow-up #1 — "Systemic: shared `patched_tortoise_sdk(db_path)` helper in `tests/_http_fixtures.py` + migrate ~24 churn-affected test files (B waves 1/1b/2; wave 3 = lint tripwire with config/ allowlist, runtime-membership enforcement, warn-for-baseline)". Cite #1950 pin + close-at-restore; per-wave exposed sets; regression gate = #2090 gate.

**Step 3:** File follow-up #2 — "Defense-in-depth: SAVE/BGSAVE cascade through `_registry_anchor` so embedded daemon death loses no registry mutations" (optional — implement or close with measured evidence).

**Step 4:** Streak protocol (recorded in the plan/scoping doc): 10 consecutive green tier-2 embedded runs on the PR (the failing lane; PR selects tier-2 only — docker evidence = post-merge full-matrix runs + nightlies, count + window at close-out). Reset semantics: any failure resets to 0; unrelated flaky test → evidence-based re-run protocol (test-debt gate: flaky = passes on ≥1 of ≤2 isolated re-runs → WARN + record); cancelled runs (per-PR concurrency) don't reset; UI re-runs OK. **Classification: masked-assert-then-403 in the same test = EXTERNAL-DEATH falsifier (the fix's one uncovered class) — record + escalate per the trigger, NOT a reset flake.** Escalation trigger (post-merge only): churn-signature flake in another file → triage (signature + attribution + resource checks) → human checkpoint before pulling B forward.

**Step 5:** Run `commit-workflow` (pre-flight: full gate both lanes; code-review gate; PR).

---

## Failure Modes

- [Counter restore-time pops counted] → disable-flag before `_restore_sdk_init`; never swap-then-close → Test: counter == 0 post-fix
- [`proj._path` None at pop time] → `_db_path` discriminator (survives close) → Test: Task 1 unit-check (drift pair vs probe-failure pair)
- [Pin leaks to next file] → pop in shared restore; both fixtures one restore → Test: cross-file runs
- [Masked assert's own `_registry_count` SDK-drop] → hold lands in Task 3 BEFORE the assert → ordering pinned
- [Session-scoped `_SEED_SDKS` accumulation → external-death class] → function-scoped close after `_restore_sdk_init` → Test: peak daemon count ~2
- [Tripwire flips PR to full-matrix / reds on pre-existing files] → tripwire moved to follow-up wave 3 with config/ allowlist + runtime-membership enforcement
- [Docker-lane run treated as vacuous] → annotation: data-plane URI coverage real via projection redirect; keepalive branch inert

## Tech Stack
Python 3.12, pytest, TortoiseSDK/FalkorProjection (embedded redislite + FalkorDB docker lanes), falkordblite 0.10.0 (vendored). No new dependencies.

---

<!-- plan-review: status=clean, reviewers=structural+integration+2 plan-verifiers, cycles=1, no P0/P1; P2s (spike FAST_ATEXIT, teardown-in-finally, RED wording) fixed -->
<!-- research-path: docs/scoping/2026-09-01-2090-test-b-lane-scoping.md -->
