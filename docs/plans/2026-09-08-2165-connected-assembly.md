<!-- research-path: docs/research/2026-09-08-2165-solution-spec-v2.md -->

# Connected Assembly (what/when/why) Implementation Plan — #2165

> **For Pi:** Use `executing-plans` to implement this plan task-by-task.

**Goal:** Ship a deterministic (zero-added-LLM-call) connected-assembly read path — "what (state) + when (dated timeline) + why (evidence with EP/provenance)" — as a real product feature: private pre-retrieval shape routing in `sdk.ask()`, assembled-block evidence rendered through the existing budget machinery, plus `sdk.ask_assembled()` and an eval v2-lane arm. Pre-customer product: this SHIPS (flag default OFF is a byte-identity safety posture, not a deferral).

**Team:** epistemic-team
**Architecture:** PRCA — a private pre-retrieval shape classifier (current-state / ordering / compare / interval / date-lookup) fires a pool-REPLACING branch in `ask()`. The block materializes as ordinary annotated hit dicts (state header + chronological dated spine + evidence) flowing through the UNCHANGED `assemble_context`/`render_context` machinery (whole-hit 8k-token/32-KiB caps, alignment invariant, D8 markers) — pure `tortoise/assembly.py` core (injected projection, function-level imports) + shared `_assemble_connected()` + public `sdk.ask_assembled()`. Supersession explicit from the #2242 fold cache; superseded excluded from current-state; as-of by validity math. Env `TORTOISE_ASK_CONNECTED_ASSEMBLY` default OFF. No new reader fragments, no qtype-enum change, no wire-field change.

**Binding requirements:** `docs/research/2026-09-08-2165-solution-spec-v2.md` R1-R17 (read it — this plan encodes each; every task's Acceptance cites its R#). Research base: the four `docs/research/2026-09-08-*.md` passes + synthesis.

### Pattern Research
Skipped — plan touches zero third-party dependencies (pure graph reads over the existing schema; no new libs). Prior research intake (Step A) consumed: spec v2 constraints 1-9 + competitor/GraphRAG/temporal/eval evidence in the four research docs.

### Integration Surface Map
| Surface | Test Layer | Notes / Bug-pattern flags |
|---|---|---|
| `tortoise/assembly.py` (new, pure) | unit (no DB via dict-stubbed GraphPort) + docker integration | classifier precision negative set; resolver chain order; date tier; sort determinism (R17 P3-6 secondary key) |
| `tortoise/sdk.py` ask() seam | integration (docker, FakeReader idiom from test_ask_sdk.py) | flag-off byte-identity (R17 goldens); fired-path locals (R11); qtype untouched; W4 explicit enrich (R5/R17); synthesized rows never fake point_id |
| `sdk.ask_assembled()` | integration | single-source `_assemble_connected`; hosted-delegated boundary (R14) |
| ingest_v2 Event-aboutObject | integration | R8 ~4-line MERGE; does NOT touch ingest.py (#2578 substrate) |
| Object resolution (exact probe / docker FTS / alias) | unit + docker | R7 honest chain; embedded degrade (R10 mock) |
| Supersession/as-of | unit | `_RECALL_OBJECT_EXCLUDED_STATUS` tuple (R17 P3-3); supersededAt returned by walk query (R17 P3-1) |
| Eval v2-lane arm | integration (docker) | R3 A-vs-B-vs-A-widened + matched control; R9 geometric fidelity; R15 census |
| config/ci-surfaces.yml | manifest | R4 same-commit registration of every new test file (#1262 drift gate) |
| W4 why-layer | integration | R5/R17: enrich_items BEFORE assemble_context on fired path |
| Docs (ONTOLOGY §4.3 supersededAt, answer-surface.md, .env.example) | contract | R17 P3-1 doc duty; env master-flag semantics |

### Failure Modes
- Fired + zero-Object graph / unresolved subject → `fired=False`, legacy byte-identical, no error/empty (R1/R7) → test per shape.
- One-half resolved on a compare → fired=False (R1 both-halves gate) → test.
- False-positive fire on two-named-option preference Q → fired=False (R16 misfire fixtures + precision floor) → test.
- Undated rows / 1970 sentinel → sentinel-strip then undated-last `[session ?]`, never a fabricated date (R2/R17 P3-2) → test.
- 200-char supersededBy-name truncation → explicit truncated-successor annotation, never a fabricated link (R12/C6) → test.
- Raw object-fetch hit (flat-string superseded_by) reaching `_validity_marker` → forbidden; state rows freshly synthesized with dict shape (R17 P2-1) → guard test.
- Budget overflow → slice-level truncation authoritative (oldest/lowest-EP first); `assemble_context` whole-hit no-starvation backstop (R12/R17 P3-4) → overflow fixture.
- `hits`/`leg_trace` unbound on fired path → bound stubs before branch (R11) → test.
- W4 flag on + fired → why entries survive via explicit enrich; synthesized rows contribute none (R5/R17) → test.
- Assembler-stage raise (walker/render/date-parse/enrich) on the fired path → typed AskRetrievalUnavailable or legacy fallback, never an untyped exception; malformed stored dates → parse-fail → undated tier (Task 6 envelope + fixture).
- Validation ordering: oversized/control-char/bad question_date MUST raise AskValidationError on fired shapes (branch after _ask_validate).
- Torn state-slice read under a concurrent fold → forbidden by single-statement state read (Task 4); read-side staleness window accepted + documented.
- Successor name resolving to zero visible nodes / superseded-with-empty-supersededBy → name-only annotation, never a fabricated link (Task 5).
- As-of equality-day boundary + undated-in-as-of → pinned semantics (Task 4).
- Cross-subject shared evidence → per-section dedupe pinned (Task 5).
- Reader decay / distractor admission → pool replaced (no legacy noise), per-slice caps under ~8-12k, verbatim-over-summarize → caps property test.

**Tech Stack:** Python 3.12+, FalkorDB (docker lane) / embedded; pytest; no new deps.

---

## Task 1: Fixture graph + config ground truth + ingest_v2 Event-aboutObject fix

**Intent:** Standing synthetic multi-session Object-structured fixture mirroring the v2 lane (Objects incl. supersession chain, aboutObject Points with sparse `when`, dated sessions, distractors, deep-rank gold marks) + the env flag registered + the ingest_v2 gap fixed, so every later task has real substrate. (R2, R4, R8, R9, R15-census-seed, R17 P3-2)
**Acceptance:** `tests/_assembly_graph.py` + `tests/test_assembly_fixtures.py` exist and pass on the docker lane; fixture mirrors ingest_v2 faithfully (Points carry NO eventId; sparse `when`; createdAt sentinel-able; zero Event-aboutObject pre-fix); `TORTOISE_ASK_CONNECTED_ASSEMBLY` in `.env.example`; ingest_v2 event loop MERGEs `about_entities` (test: event with about_entities → edge exists); ci-surfaces.yml registers each new test file in the SAME commit (R4).
**Files:**
- Create: `tests/_assembly_graph.py` (underscore helper — excluded from ci-surfaces), `tests/test_assembly_fixtures.py`
- Modify: `tools/longmem_eval/ingest_v2.py` (event loop ~246-290: pass `about_entities` to create_event), `.env.example` (ask-lane block), `config/ci-surfaces.yml`
- Test: `tests/test_assembly_fixtures.py`

**Step 1:** Register the flag in `.env.example` (ask-lane `#2070` block): `TORTOISE_ASK_CONNECTED_ASSEMBLY` (ask_env_bool semantics; master flag for the eval arm; default OFF).
**Step 2 (plan-review P1):** Run the 133-Q SHAPE CENSUS (R15 — BEFORE fixture/classifier authorship; a cheap text classification of the cached LongMemEval-S temporal corpus against the 5 PRCA shapes + 'ago'/frequency/recent-past + other). Record per-sub-class counts in this plan's status note; the non-matching sub-classes become the CLASSIFIER'S MEASURED NEGATIVE SET (R16 precision floor), and the census triggers the follow-up filings (Task 7 Step 7). Task 2's pattern/negative sets CONSUME this output.
**Step 3:** Write the fixture builder `tests/_assembly_graph.py` — docker-lane idiom from `tests/test_entity_key_expansion.py` (`_fresh_uri`, `_no_embedder`): (a) ≥2 dated sessions; (b) Objects `couch`/`dog bed` (+ a supersession chain: `couch` superseded by `sofa` via `apply_supersessions` or direct fold); (c) aboutObject-linked Points across sessions with EP persisted through operator/EP-writing SDK paths (R12 EP-realism), sparse `when` (only some stamped), `createdAt` = session date (sentinel-able); (d) Events with startedAt, ZERO aboutObject edges pre-fix (v2-lane faithful); (e) ≥12 distractor points; (f) deep-rank gold marking helper (R9) + hub-Object fixture builder (rows > per-slice pre-fetch cap; Task 4) + HOSTED-VARIANT builder (Event-aboutObject present; shared by Tasks 4/6 — plan-review P2) + ≥20-same-subject-session deep-rank R9 substrate builder (Task 7) + supersession-chain variants (successor-name-absent / recall-excluded successor / superseded-with-empty-supersededBy; Task 5) + a seeded malformed-date row (garbage/wrong-format/empty `when`, sentinel/absent createdAt; Task 6) + the R16(b) OUT-OF-SUBGRAPH-GOLD builder (cycle-4 P1): a THIRD Object (neither compare subject, outside their subgraphs) with a dated point + gold mark, whose text is authored to rank inside DEFAULT-caps legacy for the canary compare question (the canary question TEXT is authored here too), plus a Task-1 Step-4 CALIBRATION assertion that flag-OFF legacy at DEFAULT caps admits ≥1 of that gold — so the A≥1/B=0 pre-registered canary (Task 6 Step 1 / Task 7 Step 4b) is guaranteed before capture, not discovered at the last task.
**Step 4:** Write `tests/test_assembly_fixtures.py` asserting fixture shape (counts, no point-eventId, sparse-when distribution, sentinel-ability) + the R16(b) CALIBRATION (cycle-5 P2 — itemized here): flag-OFF legacy ask() at DEFAULT caps admits ≥1 of the out-of-subgraph gold for the canary question (guarantees the A≥1 half of the pre-registered delta before Task 6 capture, not at late discovery).
**Step 5:** Fix ingest_v2's event loop to resolve each `about_entities` name to an `(Event)-[:aboutObject]->(Object)` edge via an ingest_v2-LOOP-LOCAL MERGE (~4 lines — cycle-6 P2: `create_event` wires ONLY singular aboutSubject/aboutObject/aboutPoint/aboutDocument props; a PLURAL `about_entities` list would pass through as a node property creating NO edges — verified sdk.py ~15695); test: (a) an ingest_v2 run whose extractor events carry `about_entities` yields the edges; (b) NEGATIVE: an event whose about_entities names match NO Object → no edge, no raise (plan-review P2).
**Step 6 (plan-review P1):** ci-surfaces.yml — register ONLY `tests/test_assembly_fixtures.py` in THIS task (same commit, R4/#1262). Each later task registers the file it CREATES in its own commit (Task 2 → test_assembly_pure.py; Task 6 → test_assembly_sdk.py; Task 7 → tests/longmem_eval/test_assembly_arm.py) — no forward/phantom registrations.
**Step 7:** Run docker-lane fixture tests green + `python3 tools/ci_selection.py --integrity` clean + ingest_v2 CONSUMER regression: `test_lme_ingest_v2_supersession.py test_ingest_v2_consolidation.py test_eval_ingest_retry.py test_lme_m6_evidence.py test_longmem_runner.py` stay green (plan-review P2).
**Step 8 (plan-review cycle-2 P1):** Append a `## Status — census (2026-09-08)` block to THIS plan doc (per-sub-class counts + the census's negative-set question ids) AND commit the census Q→classification table as `tests/_assembly_census.json` (checked-in fixture — reproducible CI base for Task 2's precision floor and Task 7's follow-up triggers; plan-review cycle-2 P2). Committed artifact = the durable handoff between Task 1 → Task 2 → Task 7.
**Step 9:** Commit: `test(fixtures): #2165 synthetic multi-session Object graph + ingest_v2 Event-aboutObject wiring + env flag + census (+ _assembly_census.json + plan status block)`.

## Task 2: Pure classifier — shape routing + subject extraction (R13, R15, R16)

**Intent:** Deterministic pre-retrieval router owns the fired decision. Shapes: current-state / ordering / compare / interval / date-lookup. 'ago'-relative + implicit-time do NOT match and fall through (R13). Precision is the primary lever (R16). CONSUMES the Task-1 census: the measured negative set (non-matching sub-classes) plus the misfire fixtures define the precision floor.
**Acceptance:** `tests/test_assembly_pure.py` green: positive set fires per shape (informed by the census — sub-classes that match the 5 shapes fire, everything else does not); NEGATIVE set never fires (preference/advice with two named options "compare X and Y which should I pick" → None (R16 misfire); single-session; "how many times"; frequency/recent-past census sub-classes; 'ago'-relative "two weeks ago" → None even with state morphology "what was the status of X two weeks ago?" — reject-on-relative-offset guard); MEASURED precision floor on the census negative set recorded (R15/R16 — target ≥0.95 on the held-out negatives); both-subject extraction works for "which came first X or Y" / "days between A and B" incl. quotes/possessives ("my cousin's wedding or Michael's engagement party").
**Files:**
- Create: `tortoise/assembly.py` (module scaffold: `classify_question`, `extract_subject_terms`), `tests/test_assembly_pure.py`
- Modify: `config/ci-surfaces.yml` (register test_assembly_pure.py — same commit, R4)
- Test: `tests/test_assembly_pure.py`

**Step 1:** Failing tests: shape table (positive per shape + negative set + relative-offset-reject + both-subject extraction table).
**Step 2:** Run → FAIL (module absent).
**Step 3:** Implement `classify_question(question) -> AssemblyShape | None` (ordered high-precision regexes, no LLM) + `extract_subject_terms(question, shape) -> list[str]` (template split for two-subject shapes; single subject for current-state/date-lookup).
**Step 4:** Green (precision floor ≥0.95 on the census negative set from `tests/_assembly_census.json`).
**Step 5 (plan-review cycle-2 P1):** Register `tests/test_assembly_pure.py` in `config/ci-surfaces.yml` (same commit — R4/#1262; Task 2's Files gains config/ci-surfaces.yml).
**Step 6:** Commit: `feat(assembly): #2165 shape classifier + subject-term extraction + ci-surfaces (RED→GREEN)`.

## Task 3: Resolver — recall-first, confidence-tagged, no LLM (R1, R7, R17 P3-3)

**Intent:** Question prose → canonical Object candidates deterministically, with both-halves gating (R1). Honest chain: exact-name/id index probe (embedded-safe) → docker Object name-FTS → second-pass alias amplifier from anchored points' search_keys. Never a silent single-match (R7).
**Acceptance:** Candidates carry `{object_id, name, confidence: high|med|low, source}`; both-subject shapes admit BOTH halves or fire nothing (R1); exact-probe path runs on a dict-stubbed GraphPort (no DB); docker FTS leg covered docker-lane; embedded degrade (FTS missing/empty → [] + no raise) via mock (R10); multi-candidate admitted with tags; collision case returns both with ids (R12/C7 — the renderer sectioning requirement is Task 5).
**Files:**
- Modify: `tortoise/assembly.py`
- Test: `tests/test_assembly_pure.py` (resolver section)

**Step 1:** Failing tests (stub-GraphPort exact probe; two-subject admit-both; one-half → fired=False signal; FTS absent → []; POSITIVE docker-FTS paraphrase: a subject term differing from the stored Object name but matching via name-token FTS — e.g. "the couch I bought in March" → `couch` — resolves with `source="fts"` (RESOLUTION-ONLY at Task 3 — final-verify P1-1: the fired-path assembly half of this assertion has no home before Task 4-6, so it is scoped out here and asserted in Task 6 Step 2's docker test list);
**Step 2:** RED. **Step 3:** Implement `resolve_subjects(port, terms) -> list[SubjectCandidate]` with the R7 chain + confidence tags.
**Step 4:** Green (docker + stub). **Step 5:** Commit.

## Task 4: Typed walker + slice builder (R2, R3-8, R12, R17 P3-1/P3-6)

**Intent:** One batched typed walk (never N+1, never blind BFS — SubgraphExpander NOT used) over the connected subgraph: state slice (Object status/supersededBy/supersededAt incl. `RETURN o.supersededAt` R17 P3-1, exclusion by `_RECALL_OBJECT_EXCLUDED_STATUS` R17 P3-3), dated spine (aboutObject Points ∪ post-R8 Event-aboutObject edges ∪ product-lane eventId join), evidence slice (points with validity + EP). Per-lane date precedence when → createdAt (sentinel-stripped) → eventId-joined startedAt (R2); tier-tag rows; bounded pre-fetch caps (R12/C8); deterministic tiebreak on same-date rows (R17 P3-6).
**Acceptance:** slice builder returns typed rows with tier tags + admission metadata `{rows_requested, rows_admitted, truncated}`; v2-lane-faithful fixture (zero point-eventId) assembles a dated spine from tier-2 createdAt; hosted variant (Event-aboutObject present) superset; as-of window excludes post-D rows; undated → last, no sentinel masquerade; hub fan-out cap binds AT THE QUERY (plan-review P2: hub fixture with rows > per-slice pre-fetch cap → `rows_requested == cap, truncated=True, rows_admitted < total`); state slice reads status+supersededBy+supersededAt in ONE Cypher statement (no torn supersession header under a concurrent #2242 fold — plan-review P2: mock-port test pins single-statement read); the WALK carries NO cross-slice snapshot — documented tolerance + a mock-port interleave test (inject a fold between the state read and the spine read → deterministic render of whichever window won, no crash) (cycle-2 P2); as-of boundary pinned: equality-day inclusive/exclusive normalized to UTC date (superseded on-or-after D excluded / before D retained) + test at supersededAt == question_date (plan-review P2); undated row in an as-of query → admitted undated-last (pinned, no validity check — plan-review P2); malformed `when`/`startedAt` (garbage/wrong-format/empty) → parse-fail FALLS THROUGH THE R2 LADDER (a row with garbage `when` but a REAL non-sentinel createdAt dates tier-2 — never undated while a usable date exists); undated tier is reserved for rows with NO usable date after sentinel-strip (absent/sentinel createdAt + absent/unparseable when); never a raise (cycle-3 P1 — the parse-fail rule falls to the NEXT TIER per R2, it does not skip to undated). Task 1's seeded malformed-date row carries sentinel/absent createdAt so Task 6's `[session ?]` pin holds; add the explicit case "garbage when + valid createdAt → tier-2 (createdAt)"; TERMINAL-POINT semantics in the spine pinned SEPARATELY from the Object-status tuple (cycle-2 P2): superseded/retracted POINTS in a current-state render retain-with-D8-markers (legacy parity via `TERMINAL_EXCLUDED_STATUSES`/`include_terminal=True`) — one test distinct from the Object-chain tests (the Object tuple `_RECALL_OBJECT_EXCLUDED_STATUS` is never misapplied to Points).
**Files:**
- Modify: `tortoise/assembly.py`
- Test: `tests/test_assembly_pure.py` (walker section, docker fixture)

**Step 1:** Failing tests per acceptance. **Step 2:** RED. **Step 3:** Implement `collect_slices(port, subjects, shape, question_date) -> AssemblySlices`. **Step 4:** Green. **Step 5:** Commit.

## Task 5: Renderer — synthesized hits, date-sorted block, deterministic ordering/diff (R1, R2, R3-5, R6, R17 P2-1)

**Intent:** Slices → ordinary annotated hit dicts the unchanged `assemble_context`/`_render_block` render (constraint 1/5). State header + per-subject sectioning (R12/C7) + chronological dated spine + evidence lines with dict-shaped `superseded_by={"content_snippet":…}` (R17 P2-1), real point rows carry honest `session_date`/`speaker`/point_id/validity keys) under the CANONICAL `id` key — second-model P2-5: `why.enrich_items` and `item_to_why_entry` key on `id` (the FTS-hit shape); `point_id` is a W4-OUTPUT-only key — a literal `point_id` key on synthesized rows would silently no-op W4 enrichment; synthesized state/section rows carry NO `id` and are skipped by the enrich guard; ordering/interval shapes precompute the ordering/diff line deterministically (constraint 6); dedupe by point id (constraint 4); SUPERSEDED OBJECT/successor STATE rows excluded from the current-state view, history retained for as-of (constraint 3) — scoped to Object-state rows ONLY; TERMINAL POINT rows follow Task 4's retain-with-D8-markers pin (never dropped from a current-state render — legacy parity; cycle-3 P1); under budget the slice-level truncation is authoritative (oldest/lowest-EP), `assemble_context` the no-starvation backstop (R12/R17 P3-4).
**Acceptance:** rendered line list byte-parity with the same point through the ask FTS path (docker, marker test); ordering line matches a SHARED date-normalization helper's math (second-model P2-3): one helper — parse → sentinel-strip → UTC DATE truncation (date-only semantics) — is the SINGLE source for BOTH the as-of equality-day boundary (Task 4) AND the ordering/diff arithmetic (Task 5); the test computes its expected value THROUGH that helper (not an independent date-lib call) so two implementers emit identical day-differences for full-ISO timestamps near day boundaries (2026-06-10T23:30:00Z → 2026-06-10); wrong-order / +superseded / duplicate matched-control arms produce the expected deltas (R3/constraint 8); per-candidate sectioning keeps two same-named entities separate (R12/C7); HEADER/SECTION RENDERING CONTRACT PINNED (second-model P2-1): `_render_block` prepends `[session ?]` to ANY lme_session_index-less hit — a standalone state-header hit renders as `[session ?] as of now: …` — so the state/section lines materialize as synthesized hits whose CONTENT embeds the label (e.g. content = `STATE (couch): superseded by sofa on 2026-09-01`), and Task 6 Step 2 asserts the EXACT rendered state-header + section text as goldens (not just "state header + ≥2 dated lines"); two implementers must produce identical reader-visible evidence; 200-char supersededBy annotated, not fabricated (R12/C6); successor-absent variants render a NAME-ONLY annotation with no fabricated link: (a) fold's supersededBy name resolves to ZERO visible successor nodes (successor never created / recall-excluded per `_RECALL_OBJECT_EXCLUDED_STATUS`), (b) `status='superseded'` with EMPTY supersededBy (hand-written/torn row) (plan-review P2); CROSS-SUBJECT dedupe pinned: a Point aboutObject-anchored to BOTH subjects of a compare renders once per subject-section (per-section dedupe, not global) — expected line count asserted (plan-review P2).
**Files:**
- Modify: `tortoise/assembly.py`
- Test: `tests/test_assembly_pure.py` (render section — pure over slices, no DB)

**Step 1:** Failing render tests (incl. byte-parity + controls). **Step 2:** RED. **Step 3:** Implement `synthesize_hits(slices, shape, question_date) -> list[dict]` (pure) + sorter + diff synthesis + dedupe. **Step 4:** Green. **Step 5:** Commit.

## Task 6: ask() branch + ask_assembled + W4 + byte-identity (R5, R6, R11, R14, R17)

**Intent:** The product seam. `_assemble_connected()` single-source for `ask()`'s branch (before retrieval) and public `sdk.ask_assembled()`. Fired path: classify → resolve (both-halves gate R1) → walk → render → decorate real rows (EP/D8) → explicit `why.enrich_items` when W4 on (R5/R17) → `assemble_context(caps)` → `render_context` → existing qtype detect (unchanged) → ONE reader call. Bound `hits`/`leg_trace` stubs (R11). Flag OFF / unrouted / unresolved → legacy byte-identical by construction (branch precedes retrieval). FIRED-PATH EXCEPTION ENVELOPE (plan-review P1): the branch slots AFTER the `_post_ask` delegation return (sdk.py ~12713) AND after `_ask_validate` (~12719), wrapped in its OWN try envelope mapping ANY assembler-stage raise (classify/walk/render/decorate/enrich/date-parse) → `AskRetrievalUnavailable` or legacy fallback — never an untyped exception; the env gate is read inside that envelope (validation ALWAYS precedes the branch: oversized/control-char/bad-question_date inputs raise `AskValidationError` on fired shapes too).
**Acceptance:** flag-OFF golden evidence equality for fired-shape Qs (captured pre-branch at Task-6 Step 1 against clean HEAD, committed with the branch; invalidation policy WRITTEN into the test module docstring — ANY change affecting ask-lane evidence selection or rendering invalidates the goldens and forces re-capture + re-review, never reflexive regeneration: the #2070 knob series (A1-A7), retrieval query/embedding/ranking-algorithm changes, annotate/dedup changes, assemble_context caps/order semantics, and _render_block/render_context text (second-model P3-1 — the goldens are the rendered output of the whole pipeline, not just the named knobs); flag-ON + fired → assembled evidence under caps + alignment invariant + exactly ONE model call; READER-RAISE-ON-FIRED SHAPE (plan-review cycle-2 P2): a reader raise after a successfully-fired block maps to AskReaderUnavailable with ZERO legacy re-run and ZERO second model call — the fired envelope wraps ONLY classify→walk→render→decorate→enrich (content stages); the shared reader call stays under the legacy AskReaderUnavailable envelope (no double metering); both-flags-on → why survives on real evidence points, synthesized rows contribute none; enrich/decorate FAILURE degrade pinned: W4-flag-on fired path with enrich_items raising (or a real point row missing) → typed degrade (assembled block without why keys / AskRetrievalUnavailable via the fired envelope), never a raw raise or partial block (plan-review P2) — this is ALSO the test that exercises the envelope's typed-raise branch (cycle-2 P2: the malformed-date row is NOT the raise vehicle — Task 4 pins its outcome as undated-tier fallback); fired-but-unresolved → legacy fallback no error; malformed-date row → SINGLE pinned outcome: undated-tier fallback rendering `[session ?]`, never a raise (cycle-2 P2 — deterministic contract); retrieval_degraded=False on fired paths (R11 — binding restored; the fired path ALWAYS passes `[]` to `_ask_d8_decoration_unavailable` — never the assembled hits — because the empty-`supersededBy` state row (Task 5 case b) would otherwise flip it True via TERMINAL_EXCLUDED_STATUSES (second-model P2-6: mandatory, no "or skips it" alternative; test: the empty-supersededBy row renders name-only AND yields retrieval_degraded=False); a fired as-of render containing an intentionally-annotated orphan superseded row → retrieval_degraded=False, cycle-2 P2); qtype on the wire unchanged (incl. caller override case pinned — R12 bullet — cycle-1 P2); hosted Event-aboutObject-present graph test (R6); R16(b) OUT-OF-SUBGRAPH gold (cycle-1 + cycle-2 P1/P2): one named fired-shape question whose gold evidence lives OUTSIDE the resolved subjects' subgraphs, fixture pinned so DEFAULT-caps legacy ADMITS the gold (pre-registered canary delta: A≥1 gold row, B=0 — BOUNDING the out-of-subgraph harm of pool replacement; the NON-starvation proof is the separate in-subgraph ≥2-gold assertion, Task 7 Step 1 — cycle-3 P2 wording fix), included in the Step-1 capture set + Step-2 failing tests + carried into Task 7's A-default/A-widened/B admission table; MCP tortoise_ask + hosted /v1/ask exposure surfaces inherit the branch via in-process sdk.ask() — REQUIRED exposure-level tests bound to EXISTING test modules (cycle-4 P2: NO new test module — avoids the Task-1-per-task ci-surfaces registration collision): hosted test in `tests/test_ask_api.py` (the `_register_ask_route()` pattern is live there) + MCP `tortoise_ask` test in an existing MCP module (e.g. `test_mcp_server_auth_modes.py`), BOTH added to Task-6 Step 2's failing-test list (RED→GREEN): flags ON → fired shape returns 200 with assembled evidence; a forced stage-raise maps to the retrieval-unavailable code (never 500); the byte-identical-by-default note is a doc SUPPLEMENT, not an alternative; regression: test_ask_sdk.py / test_ask_api.py / test_reader.py / test_retrieval.py / test_ask_retrieval_levers.py green; ASK_ASSEMBLED CONTRACT pinned (cycle-1 P2): signature (question, *, question_date=None, question_type=None, caps override?, _reader_factory seam for the eval arm's pinned/stub reader), return shape RESOLVED NOW (cycle-4 P2 — not deferred to implementation): `AssemblyAnswer` dataclass — `{fired: bool, shape: str|None, question_type: str, subjects: list[{object_id,name,confidence}], slices: dict[state_rows, timeline_rows, evidence_rows], post_cap_lines: list[dict] (the exact reader-visible evidence lines), admission: {rows_requested, rows_admitted, truncated}, evidence: str (render_context output when capped), context_tokens: int, answer: str|None, retrieval_degraded: bool}` — pinned with a shape-asserting failing test; with a reader supplied via `_reader_factory` the ONE reader call fills `answer`; without a reader the assembly fields are populated and `answer=None` (pure-assembly mode for the eval arm); the eval arm reads `post_cap_lines` for gold-id admission and `answer` for conversion — field names mirrored in Task 7, no re-reading of Task 6 implementation; performs the ONE reader call ONLY when a reader is supplied (pure assembly otherwise → returns slices + post-cap lines + admission counts for the eval arm), honors the same fired-gate (fired=False → returns assembly=False + no block), fires regardless of caller question_type but the RESPONSE reports the caller/detector value unchanged, and in hosted-delegated client mode (TORTOISE_API_URL set, no local graph) raises AskRetrievalUnavailable with a clear message (documented in answer-surface.md) — with a TORTOISE_API_URL-set test; R14 drift pin (cycle-2 P2): a drift test asserting `_assemble_connected` is referenced ONLY by ask()'s branch and ask_assembled.
**Files:**
- Modify: `tortoise/sdk.py` (ask() seam ~12606 + new `ask_assembled`), `tortoise/assembly.py`
- Create: `tests/test_assembly_sdk.py`
- Modify: `config/ci-surfaces.yml` (register test_assembly_sdk.py — same commit, R4)
- Test: `tests/test_assembly_sdk.py`

**Files (exposure tests):** Modify `tests/test_ask_api.py` + an existing MCP test module (e.g. `test_mcp_server_auth_modes.py`) — no new test modules (R4 collision avoided).

**Step 1:** Capture golden evidence strings (flag OFF, pre-branch) for the fired-shape question set on the fixture — INCLUDING the R16(b) out-of-subgraph-gold question (deterministic text; no LLM in the evidence path) → commit them with this task's code (R17).
**Step 2:** Failing tests: byte-identity-off (golden equality + flag-ON-unrouted A/B), fired routing (EXACT rendered state-header/section golden text — final-verify P1-2: e.g. `[session ?] STATE (couch): superseded by sofa on 2026-09-01` — pinned/captured at Step 1, NOT the weak "state header + ≥2 dated lines" phrasing, so two implementers produce identical reader-visible evidence; plus the structural check ≥2 dated lines from ≥2 sessions) + POSITIVE-FTS FIRED ASSEMBLY (the Task-3 scoped-out half of P2-2: the docker paraphrase "the couch I bought in March" resolves via name-token FTS AND the fired path assembles a block — final-verify P1-1), R16(b) OUT-OF-SUBGRAPH B=0 (assembled admits ZERO of the out-of-subgraph gold while legacy admits ≥1 — cycle-5 P2 itemization), one-half compare → legacy, misfire two-option preference → legacy (R16), validation-order (oversized/control-char/bad question_date on a FIRED-SHAPE question → AskValidationError, never a fired-envelope outcome — cycle-3 P2), caps property (oversized history → ≤32 KiB / ≤8k tokens AND the THIRD cap — `context_item_cap` item count (default 40) — pinned for the fired path: slice-level truncation is authoritative BEFORE the item cap binds, and a test asserts the survivor set when the item cap binds before the byte/token caps; second-model P3-2 — post_cap_lines means post-ALL-caps), W4 both-flags, qtype pin, exactly-one-call spy, R6 hosted-shape (Event-aboutObject graph), HOSTED /v1/ask EXPOSURE (in tests/test_ask_api.py via `_register_ask_route()`: flags ON → 200 with assembled evidence; forced stage-raise → retrieval-unavailable code, never 500), MCP tortoise_ask EXPOSURE (existing MCP test module, same two assertions), RAW-OBJECT-HIT GUARD (a raw `entity_type='object'` hit with flat-string superseded_by forced into the assembled list / walker-monkeypatched → typed degrade or guard, NEVER an AttributeError in _validity_marker — R17 P2-1, cycle-4 P2), NO-STARVATION SURVIVOR SEMANTICS (extend the caps-property test: an over-budget mid-list row is dropped while a LATER high-value row is still admitted — whole-hit-skip no-starvation, not strict tail-drop — R17 P3-4, cycle-4 P2).
**Step 3:** RED. **Step 4:** Implement `_assemble_connected`, the ask() branch (function-level imports; env gate first statement), `ask_assembled`. **Step 5:** Green + full ask regression. **Step 6:** Commit: `feat(ask): #2165 connected-assembly branch + ask_assembled (flag-off byte-identical)`.

## Task 7: Eval v2-lane arm + geometric fidelity + controls + docs (R3, R9, R15, R17 P3-1)

**Intent:** Measure assembly honestly: does the needed evidence reach the reader AND does the pinned reader convert it — assembled (B) vs legacy at DEFAULT harness caps (A) vs legacy at lane-1-parity WIDENED caps (A-widened), plus ≥1 matched control (R3/R15). R9 geometric-fidelity fixture (built in Task 1's ≥20-same-subject-session substrate builder) reproduces the deep-rank diagnosis ON the Object lane — control for the A-widened arm, not default-legacy (R15). Eval matched-control arm enumerated with pinned delta direction (plan-review P2).
**Acceptance:** arm reports per-slice admission (which gold point ids reached the post-cap line list) + reader-conversion delta (B vs A vs A-widened via the ask_assembled `_reader_factory` seam) — PRE-REGISTERED metric-b semantics (second-model P2-4): metric (a) gold-id admission is always measured (reader-independent); metric (b) conversion is measured ONLY with a real pinned reader and is pre-registered ABSTAINED ("stub reader — conversion not measured") when only the FakeReader/stub is available; the synthetic-graph arm's default is metric-b abstained; no stub-reader "delta" is ever reported as conversion evidence; concrete knob values PINNED for this harness (plan-review P2 — `tools/longmem_eval/retrieve.py` knob universe differs from ask(): A-default vs A-widened must be configured explicitly, e.g. pool/`tr_top_k`/per-session caps at defaults vs widened, with an assertion that A-default admission ≠ A-widened admission on the real harness — otherwise the strawman control collapses); R9: legacy admits <2 gold rows AT WIDENED KNOBS, assembled admits ≥2 on the same graph (substrate built in Task 1 — NOT modified here; cycle-2 P2 dual-ownership resolved); matched control: order-shuffle → expected NO reader delta; +superseded-rows → pre-registered direction or report-only; oracle tier (evidence-sessions-only upper bound) IF the harness supports it, else the omission is pre-registered in the arm (R3 — plan-review P2); 133-Q census result recorded + FOLLOW-UP ISSUES FILED for the deferred classes the census quantifies ('ago'-relative, N-ary, LLM-resolution-fallback) with #2578 filing precedent (plan-review P2 — named owner/trigger tied to the census); supersededAt added to docs/ONTOLOGY.md §4.3 (R17 P3-1 doc duty); answer-surface.md additive note INCLUDING the real-lane caveat + write-side dependencies (neutral-0.5 EP evidence until write-side EP lands; D3-sparse `when`; alias cold-start — R12/R17 DA P2-4, plan-review P2); plan's out-of-scope list recorded as follow-ups.
**Files:**
- Modify: `tools/longmem_eval/retrieve.py` (arm gated on the SINGLE master flag `TORTOISE_ASK_CONNECTED_ASSEMBLY` per spec R14 — cycle-2 P1: a separate TORTOISE_LME_ASSEMBLY toggle is NOT introduced; if an arm selector is needed it couples to the master flag with a hard-guard that REFUSES to run the B arm when the master flag is OFF — a master-flag-OFF eval run must never silently collapse B to legacy A-only), `docs/ONTOLOGY.md`, `docs/product/answer-surface.md`
- Create: `tests/longmem_eval/test_assembly_arm.py` (layout precedent `test_vector_arm.py`; register in ci-surfaces.yml eval surface THIS task — same commit, R4)
- Test: `tests/longmem_eval/test_assembly_arm.py`

**Step 1:** Failing test: gold turns across ≥2 sessions both in the post-cap line list on the fired shape (v2-lane graph); legacy control admits <2 AT THE PINNED WIDENED KNOBS; R9 substrate (≥20 same-subject sessions, gold marked deep-rank) built from the Task-1 builder. **Step 2:** RED. **Step 3:** Wire the arm + register the file in ci-surfaces.yml (same commit). **Step 3b:** MASTER-FLAG GUARD test: invoke the B-arm selector with `TORTOISE_ASK_CONNECTED_ASSEMBLY` unset/0 → the run REFUSES (explicit abort/non-zero), never emits an A-shaped result (cycle-3 P2). **Step 4:** Green incl. A-default vs A-widened admission inequality assertion + matched-control deltas + oracle-tier-or-pre-registered. **Step 4b:** Include the R16(b) out-of-subgraph-gold question in the A-default / A-widened / B admission table (pre-registered delta: A≥1 gold, B=0) (cycle-2 P2). **Step 5:** Docs (ONTOLOGY supersededAt; answer-surface note incl. real-lane caveat). **Step 6:** Full acceptance: docker-lane targeted suites + `uv lock --check` + ruff + `ci_selection.py --integrity`. **Step 7:** File the census-driven follow-up issues (census = `tests/_assembly_census.json`, committed in Task 1 — reproducible trigger data); commit + plan-status note.

---

## Out of scope (recorded follow-ups, per R13/R14/spec)
- **'ago'-relative / implicit-time resolution** (needs LLM anchor+offset — constraint 6): filed as a follow-up issue in Task 7 Step 7, owner epistemic-team, TRIGGER = census result (quantifies the class) — not silent rot.
- **N-ary decomposition** (beyond two named subjects): follow-up issue, Task 7 Step 7, same trigger.
- **LLM resolution fallback** (constraint 7 second tier): follow-up issue, Task 7 Step 7 (fires only if the measured paraphrase recall gap is real on docker-FTS).
- Per-shape reader fragments: post-#2013 un-gate (named gate).
- Fold-time snapshot store: post-#2164/#2349 (named gates).
- Append-legacy knob: behind #2578 measurement (named gate).
- 13-Q measurement + admission-widening ablation: lane 1 (#2578). Density→reader-answers link measured but gated honestly (R3/R17).

## Accepted divergences (from issue body)
- Re-scoped per human gate 2026-09-08 (dual-lane); assembler acceptance is synthetic-graph + no-regression, NOT the 13-Q score. ask() ships (de-provisionalized same date). No new reader fragments/qtype/wire fields in v1.

<!-- plan-review: cycles=6, status=clean, version=2.3.0 -->


## Status — census (2026-09-08)
133-Q LongMemEval-S temporal corpus classified (artifact: `tests/_assembly_census.json`, committed Task 1):
- **ordering/compare 34 · interval 19 · current-state 2** → the deterministic both-halves/state family PRCA v1 fires on ≈ **55/133 (41%)** — CORRECTS R13's implied "~76% in scope" (assumed, never measured — R15's census exists to catch exactly this).
- recency/current-state 8 + recency 2 (12): "most recent" sub-set partially deterministic (date-max) — candidate v1.5.
- **Negative set for Task 2's precision floor (measured, from the census):** ago-relative 31 · frequency/count 12 · duration-state 7 ("how long had I been X when Y") · relative-date-lookup 6 (Valentine's/last Saturday) · nary-ordering 4 · other 6 · pattern/recurring 1 · offset-comparison 1 ≈ **68 non-firing** + the misfire fixtures (two-option preference with compare syntax).
- Follow-up triggers (Task 7 Step 7 filings): ago-relative (31 — LLM anchor+offset, constraint 6), duration-state (7), N-ary ordering (4), relative-date-lookup (6). LLM-resolution-fallback trigger: the positive-FTS paraphrase tests (Task 6).

## Status — implementation (2026-09-09)
- **Task 1 (fixture + R8 fix + census) COMMITTED** `1dfbe43db` — `tests/_assembly_graph.py`, `tests/test_assembly_fixtures.py` (8), ingest_v2 R8 Event-aboutObject MERGE + 4 tests, census `tests/_assembly_census.json`, ci-surfaces registration. 67 pass docker.
- **Task 2 (classifier) COMMITTED** `cff6774fb` — `classify_question`/`extract_subject_terms` (AssemblyShape StrEnum), 51 pure tests. Census-anchored precision 76/78 negatives rejected (≥0.95 floor); recall 50/55 in-scope fires (5 non-fires = pinned duration/count mis-files). 4 review rounds converged.
- **Task 3 (resolver) COMMITTED** `d92c40a5a` — 3-leg chain (exact→high / FTS→med / alias→low), dict-stub + docker legs. 63 docker / 59+4skip carve-out. 2 review rounds.
- **Task 4 (typed walker + slice builder) COMMITTED** `aaad1dbd4` — `collect_slices`/`AssemblySlices`/`_norm_date` (UTC truncation — host-independent as-of boundary)/`docker_walker_port` (single-statement state read, per-subject ORDER BY id LIMIT spine = deterministic + fair, never row-level N+1). As-of equality-day-inclusive UTC, terminal-point retain separate from the Object tuple, torn-read interleave, hub per-subject cap. 77 docker / 69+8skip. 2 review rounds.
- **Task 5 (renderer) COMMITTED** `08e5a3373` — `synthesize_hits` + helpers; headers embed the label (`STATE (couch): superseded by sofa on 2026-09-01`), dict-shaped superseded_by, successors_verified probe → NAME-ONLY for never-created/recall-excluded/torn, ordering/diff read the walker's OWN ladder date (never re-derived), interval suppressed on degenerate windows, per-section dedupe not global, (date,id) internal re-sort, headers follow candidate order. 97 docker / 89+8skip. 2 parallel reviewers (correctness + plan coverage) → both P1s + 6 P2s fixed, 11 coverage tests added.
- **Task 6 (ask() branch + ask_assembled + W4 + byte-identity) PENDING** — next: pre-branch golden capture (flag-OFF, this clean HEAD is the capture point) → RED fired-path tests → branch.
- **Task 7 (eval arm + docs + follow-up filings) PENDING.**
- #2578 (measurement lane) scope plan awaiting human approve (files untracked in this worktree by design).
- **Task 6 COMMITTED** `27d55a63f` — ask() pre-retrieval branch (env `TORTOISE_ASK_CONNECTED_ASSEMBLY`, flag OFF → legacy byte-identical; fired → assembled=post_cap_lines + hits=[]/leg_trace=[] → D8 gate sees [] → retrieval_degraded never flips), shared steps 5-8 (ONE reader call, no double metering, 12-field response), ask_assembled + AssemblyAnswer contract, _assemble_connected (R14: referenced only by the ask branch + ask_assembled), goldens captured pre-branch (current/compare/interval/misfire/ago + R16(b) canary flag-OFF + fired current/compare/interval + hosted variants), hosted /v1/ask + MCP tortoise_ask exposure tests, 20 docker tests. 2 parallel reviewers → P1-1 empty-fired fallback + P1-2 validity/epistemic marker parity fixed.
- **Task 7 COMMITTED** `6ea05f0fe` — tests/longmem_eval/test_assembly_arm.py (6 docker): master-flag guard (B refuses w/o flag), R16(b) A≥1/B=0 table, R9 A-default/A-widened/B admission + non-vacuous widened reach, matched controls (order-shuffle zero delta, +superseded direction), metric-b pre-registered ABSTAINED. Docs: ONTOLOGY §4.3 supersededAt + supersededBy probe note; answer-surface.md connected-assembly + real-lane caveats. ci-surfaces registration.
- **Task 7 Step 7 filings DONE** — follow-ups filed: #2652 (ago-relative, census 31), #2653 (N-ary ordering, 4), #2654 (duration-state, 7), #2655 (relative-date-lookup, 6) — team:epistemic-team, complexity:standard, census trigger pinned.
- **NEXT:** merge origin/main (~21 behind), VGATE full branch diff, PR + multi-agent code review, auto-merge. #2578 scope still awaits the human approve gate (untracked artifacts stay in this worktree by design).
