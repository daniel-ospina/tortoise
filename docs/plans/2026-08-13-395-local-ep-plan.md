# Plan: Local EP Propagation (affected-subgraph computation)

> **Issue:** #395 · **Status:** planned (decision locked 2026-08-15: keep canonical standalone) · **Scoping:** verified S2 approach (2+2 cycles clean, hard-gate wiring check zero gaps)

## Goal

Replace global extraction with affected-subgraph computation per the verified scoping: Deltas A/B/C, `max_hops: int | None = 2` in both BFS impls, degeneration guard, no-arg local contract, tolerance harness, 10 acceptance criteria.

## Key code state (verified)

- `sdk.py:5455/5465` - no-arg `compute_confidence()` runs global `proj.extract_svbp_factors()` twice (before/after dream) -> replace with local.
- `ep.py:565` (`_affected_claims`) / `ep.py:965` (`run`) - `max_hops: int = 2`; live `None` crash sites at `ep.py:635/637`.
- `analyze.py:368/421` - second pair of `max_hops` sites.
- `extract_factors_for_operators` - does not exist; `_last_affected` not on TortoiseEP.

## Tasks (per scoping order; Task 1 is the Phase-3 profiling hard prerequisite)

1. **Phase-3 profiling gate** - measure current global-extraction cost profile; confirm subgraph wins. (Hard prerequisite.)
2. **Delta A - affected-subgraph BFS** - `_affected_claims` supports `max_hops: None` (unbounded) + `int`; both BFS impls (`ep.py`, `analyze.py`) aligned.
3. **Delta B - factor extraction** - `extract_factors_for_operators` for operator-bearing subgraphs; drop global `extract_svbp_factors()` from no-arg path.
4. **Delta C - no-arg local contract** - `compute_confidence()` no-arg runs local EP over affected subgraph; `_last_affected` recorded; degeneration guard.
5. **Tolerance harness** - seeded, wipe-rebuild isolation, split interior/boundary/capped assertions, vectors a-g, canonical-BFS consistency.
6. **Acceptance criteria (10)** - per scoping; AC6 re-pin after shuffle-seeding.
7. **Regression** - `test_ep_sources.py` stays green unmodified.

## Non-goals

- BFS merge -> #901 (scoped out; two BFS impls stay separate by design).
- Follow-ups #1162 (api.py:152 SVBP global-extract) / #1163 (graph-persisted dirty state / ep_version) - tracked separately, not here.
- No new deps; no schema changes; no global `random.seed()`; rejected S1/S3/S4 alternatives.

## Dependency notes

- #901 (Connect workflow) consumes the landed no-arg/local contract - #395 stays canonical standalone (owner decision 2026-08-15).
- Keep the two BFS implementations separate; single-pass fallback recorded in scoping.

## Test plan

- Tolerance harness (above) + acceptance-criteria tests + regression (`test_ep_sources.py` green).
- Integration: MCP `tortoise_compute_confidence` no-arg path; HTTP contract per scoping.
