---
title: "Tortoise #1765 Ops Runbook — identity surface prerequisites"
type: engineering
domain: platform
doc_status: live
ownedBy: organisation-design-team
aboutSubjects: identity, auth, supabase-ops
aboutObjects: teams, api_keys, auth.identities
created: 2026-08-26
---

# #1765 — Ops Runbook: Supabase project + staging verification

> Project: `ybetwichurajbfswfeqa` · Dashboard: `https://supabase.com/dashboard/project/ybetwichurajbfswfeqa`
> Live status (3rd draft — earlier drafts were cleaned by a parallel process on the shared main checkout; this copy lives in the 1765 worktree).

## ✅ Done

- **Step 1 — Enable Manual Linking:** ON. Server-verified via Management API (`security_manual_linking_enabled = True`).
- **Step 2 — Redirect URLs:** done (human 2026-08-26): `app.premiselabs.co`, `tortoise.premiselabs.co`, `tortoise.premiselabs.co/welcome.html`, `premiselabs.co`.
- **Step 3 — Email confirmations:** ON. Server-verified (`mailer_autoconfirm = False`).
- **Step 5 — Fly env + log hygiene:** secrets set on `tortoise-y4mjjq` (restart healthy): `TORTOISE_LINK_INTENT_SECRET` (generated 64-hex), `TORTOISE_REAUTH_WINDOW_SECONDS=900`. No proxy change needed (uvicorn behind Fly edge; edge logs don't capture headers; code guard = Task 3).
- **Step 4 — staging checks (a)-(f):** agent-run 2026-08-26 via `POST /v1/projects/{ref}/database/query/read-only` (runs as `supabase_read_only_user`; 7-day PAT; sources: supabase.com/docs/reference/api/introduction + api.supabase.com/api/v1).

| Check | Result | Implication |
|---|---|---|
| (a) encrypted_password | 47 SET / 4 NULL / 0 `''` — OAuth users are NULL (research assumed `''`) | Formula handles both; Task 1 seeds both shapes ✅ |
| (b) email identity rows for OAuth | **NO** — 0 users with OAuth+email rows | Confirms `email_method` fallback (auth.users.email + confirmed_at) ✅ |
| (b2) OAuth email confirmation | all 4 OAuth users confirmed → login_methods=2 | no banner for them (recoverable) ✅ |
| (e) last_sign_in_at | 38/51 signed in, most recent 2026-08-25 | signal works; before/after sign-in = manual staging item ✅ |
| (f) reauthentication_time | NOT queryable (no auth.config; not in API schema) | accept code default 900; our gate = last_sign_in_at + ReauthDialog ⚠️ |
| Census | 0 multi-method users; 47×1, 4×2 login methods | banner population today = 0 (feature is forward-looking) |
| Config note | `security_captcha_provider = hcaptcha` (local config.toml says turnstile) | drift, harmless |

## ✅ Verified 2026-08-28 (production)

> Both checks below were verified against production on **2026-08-28**
> ([#1765 verification comment](https://github.com/daniel-ospina/tortoise/issues/1765#issuecomment-5452818879)).
>
> **Keep this table in step with the checks.** A status board that is not refreshed when a check
> passes cannot distinguish "not done" from "done but unrecorded", and a stale `⬜ manual` row reads
> as an open ship-blocker.

- **(c)** `double_confirm_changes` change-email behavior — ✅ **ON**: an email change requires confirmation on **BOTH** the old and the new address (verified live); does not block flipping `TORTOISE_MANUAL_LINKING_ENABLED=1`.
- **(d)** `CreateEmailIdentityOnPasswordSetEnabled` — ✅ **OFF** (supabase/auth#2085 default).

## Ship-blocker summary

| Check | Status | Blocks |
|---|---|---|
| Step 1 manual linking | ✅ server-verified | add-method flows |
| Step 2 redirect URLs | ✅ | OAuth link round-trip |
| Step 3 confirm email | ✅ server-verified | verified-email gate |
| Fly env | ✅ | link-intent, re-auth |
| (a)/(b)/(e) | ✅ | Task 1 seeds + email_method |
| (f) | ⚠️ not queryable | accept default |
| (c)/(d) | ✅ verified 2026-08-28 | change-email guardrail, flag semantics |
