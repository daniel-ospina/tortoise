---
title: "Legal pages implementation plan (#657)"
type: legal
domain: legal
doc_status: draft
subjects.team: epistemic-team
created: 2026-08-08
---
<!-- research-path: docs/research/2026-08-08-657-legal-pages.md -->

# Privacy Policy + ToS Pages for tortoise.premiselabs.co — Implementation Plan

> **Plan revision:** 9 — G-gate ⑧ RESOLVED (self-service deletion primary) + DNS fixed by operator (2026-08-08). See "Plan Review Cycle Log" at the end.

> **G-gate decisions (2026-08-08):** owner answered the G-gate items in a session — decisions locked into scope rev 5 + this plan rev 7 (see T3 Step 1 decision log + Cycle 6 log). **Item ⑥ (fee clause) RESOLVED 2026-08-08 (owner + legal research) — LOCKED into scope rev 6 + this plan rev 8 (see Cycle 7 log)**. **Item ⑧ (rights-request intake) RESOLVED 2026-08-08 (owner + legal research — GDPR Art. 12/17 + EDPB guidance): self-service deletion is the PRIMARY rights mechanism; private intake form DEFERRED — LOCKED into scope rev 7 + this plan rev 9 (see Cycle 8 log). ALL G-gate items now LOCKED.** **DNS (2026-08-08): `tortoise.premiselabs.co` CNAME FIXED by the operator — T7 Step 0 is now a VERIFICATION step (propagation pending confirmation; the `TORTISE_HOST_CHECK` contract stays as the propagation guard).** Per cycle-5 P1-4, T3's OWNER-APPROVAL comment on #657 is still required at execution (this revision records the decision log, not the approval).

> **For Pi:** Use `executing-plans` to implement this plan task-by-task.

**Goal:** Ship owner-approved Privacy Policy and Terms of Service pages at `tortoise.premiselabs.co/privacy` and `/tos` (plus `dpa.html` and `license.html` — **REQUIRED, G-gate ③/⑨ LOCKED 2026-08-08**), wired into the site footer and signup flow, grounded in Common Paper CSA v2.1, asserting Tortoise's no-sale / no-AI-training / minimal-PII commitments in both documents.

**Team:** epistemic-team
**Role:** (session role not set — omit)

**Architecture:** Self-contained static HTML pages (`privacy.html`, `tos.html`) using the existing design tokens (`--bg:#060b14`, cyan accent, mono/serif stack) — zero build step, zero runtime, served automatically by Cloudflare Pages at `/privacy` and `/tos` because `_middleware.ts` only rewrites `/`. Legal text mirrors Common Paper CSA v2.1 (CC BY 4.0, attribution kept) with the Machine Learning clause **negated** to a no-training pledge and a BUSL-1.1 license-boundary section added — all text in-repo under git history. E2E verification is **negation-safe**: canonical-sentence checks + anti-pattern regex guards, so a page that merely avoids the banned phrase (e.g., "we do not sell or share") cannot pass.

> **Host-mapping reality (plan-review finding, #540) — RESOLVED 2026-08-08:** `tortoise.premiselabs.co` CNAME was **FIXED by the operator on 2026-08-08** (live evidence at the time: `onboarding-prompt.md` served `text/markdown` — correct project — but root still served a legacy h1; mid-propagation, proxied CNAME ~300s TTL + edge variance). **`tortoise.premiselabs.co` is the canonical host again (per middleware design); `premiselabs.co` secondary.** The deploy token cannot edit DNS (auth error; CI sync runs `continue-on-error: true` per #540) — manual Cloudflare DNS edit remains the fallback if propagation regresses. E2E baseline host = **premiselabs.co** (always-green signal); `tortoise.*` checks stay gated on the `TORTISE_HOST_CHECK` contract — now a propagation VERIFICATION, not a blocking unknown (T7 Step 0: `dig` + root-marker poll every 30s up to 10 min; fresh gate at T8 start). See surface-map S10.

> **Prerequisite note (verified 2026-08-08):** the machine-readable `<!-- issue-scoping: v5.1 double diamond + verify -->` HTML-comment signature IS now present on #657 (posted post-hoc as an issue comment by housekeeping). Issue label `scoped` + `implementing`, verification-gate comment ("✅ Scoping finalized — issue-scoping v5.1 output", verdict SCOPED), and scope doc `docs/scoping/2026-08-08-657-legal-scope.md` revision 4 (status: scoped) corroborate. No re-verify needed.

### Pattern Research

Skipped — plan touches zero third-party code dependencies (static HTML/CSS only; Playwright/wrangler are tooling, not shipped dependencies). Template + content research is complete in the research brief (36 sources; Common Paper CSA v2.1 selected as drafting base; comparables Mem0/Zep/Letta/Supabase fetched; Meta Pixel GDPR/CPRA enforcement precedent documented). No library-version or API-surface verification needed. `workflow/02` research-intake gate skipped per skip rules; `workflow/03` surface map follows.

### Integration Surface Map

| # | Surface | Boundary crossed | Test layer | Bug-pattern flags / checklist notes |
|---|---|---|---|---|
| S1 | **Signup form acceptance** — `signup.html` clickthrough statement between `.providers` (line ~275) and `#email-form` (line 278) | User consent contract; spans OAuth (GitHub/Google) + email paths; `.providers` sits OUTSIDE the form | pytest E2E (same-viewport assertion) | Placement must be between `.providers` close and form — in-form-only placement would not be same-viewport with OAuth buttons. Assert same-viewport, not mere DOM presence. |
| S2 | **Footer links ×5 pages** — `product.html` (create, before `</body>` line 768), `welcome.html` (create, no footer exists), `signin.html` (.footer line 290), `signup.html` (.footer), `self-hosted.html` (footer line 130) | Navigation surface; **Privacy · Terms · License · DPA (G-gate ⑨ LOCKED — all four ship)** | pytest E2E link presence + curl 200s (T7) | product/welcome have NO footer today — must be **created**, not edited (scope Phase 1 finding; issue indicator 2's parenthetical resolved). |
| S3 | **Middleware passthrough** — `functions/_middleware.ts` | Static serving of `/privacy`, `/tos` on both hosts | Verify only — NO change | Read confirmed: non-`/` paths `return context.next()`. Regression check: `/` still rewrites to product.html on `tortoise.*` (T7 #6). |
| S4 | **Cloudflare Pages deploy** — `npx wrangler pages deploy .` | Upload scope: whole `website/` tree incl. `apps/dashboard` (separate project) | Post-deploy curl (T8) | Add `.wranglerignore` with `apps/` so only site pages + legal pages + `functions/` ship. Keep README line 12 as source of truth. |
| S5 | **Analytics #528 adjacency** | No code dependency; content must stay true when instrumentation ships | Content review (drafting rule: conditional/future wording, never present-tense) | PostHog residency line is CONDITIONAL on #528 outcome; re-check step at #528 resolution; consent banner owned by #658, not this issue. **Failure modes:** #528 merges DURING T7/T8 → re-run the #528 re-check (PostHog residency line + tool-name disclosures) before merge; present-tense claim slips into served content → grep guard (`grep -nE 'we (use|collect|share|sell|process|retain|store|transfer)\b' website/privacy.html website/tos.html` must only hit the pinned expected-match subset — canonical/future-framed lines per T2 Step 2 / T4 Step 5 / T7 #10(e)). |
| S6 | **Billing #432 adjacency** | No code dependency; ToS fees section anticipatory | Content review | Fee clause LOCKED (G-gate ⑥, 2026-08-08): general fee-structure framing + hyperlinked Pricing Page + 30-day notice/renewal mechanics; free tier: no payment method, no auto-renewal/invoicing wording until #432; NO dollar figures in the ToS body (guard `== 1`). **Failure modes:** #432 merges DURING T7/T8 → re-check fees wording + no-auto-renewal guard; pricing-page drift → assert the fees sentence references the pricing tiers textually (no hardcoded dollar amounts in the ToS body) + the Pricing Page href resolves (T4 Step 6 / T7 #2); free-tier numbers conflict unresolved → reconcile before the ToS hyperlinks the pricing page (scope §6.2 follow-up 4). |
| S7 | **Contact channel** — GitHub issues (D2); `docs.html` mailto (line 217) → GitHub-issues link | Legal contact must not invent an email | Manual render + grep (`grep -n mailto docs.html` must return nothing) | Absorbed in-scope one-line fix, same deploy. |
| S8 | **Design tokens / mobile render** | Brand consistency on long-form pages | pytest mobile render check @375px (T7 #9) | Evidence source for mono-vs-serif legal-body decision; no horizontal scroll, readable type. |
| S9 | **Existing E2E suite (regression)** — `tests/e2e/test_welcome_page.py` (#648) | New legal suite must not break welcome tests (shared `tests/` pytest layout, `page` fixture) | pytest regression (`python -m pytest tests/e2e/ -q`) | Includes `test_live_signup_redirects_to_welcome_with_key` (skipped without creds) — the signup acceptance block sits directly above `#email-form` and must not disturb the live-signup flow. |
| S10 | **DNS/CNAME host mapping (#540) — FIXED 2026-08-08 (operator):** `tortoise.premiselabs.co` CNAME → `premise-labs.pages.dev` (propagation pending at the time — root still served a legacy h1 mid-propagation); deploy token lacks Zone:DNS edit scope | Which host serves THIS project's pages; middleware `/` rewrite surface | Pre-flight DNS gate (T7 Step 0) + post-deploy root-marker curl (T8) | **Now a VERIFICATION step, not a blocking unknown:** `dig +short` + root-body marker + `onboarding-prompt.md` content-type; poll every ~30s up to 10 min; re-run FRESH at T8 start. If propagation regresses → manual Cloudflare DNS edit (NOT the continue-on-error CI sync) + note in #657 thread; do NOT proceed to T7/T8 production `tortoise.*` checks until the gate is green. |
| S11 | **CI deploy workflow** — `.github/workflows/deploy-pages.yml` | Push to main + `website/**` → wrangler@4 deploy; stages `website/onboarding-prompt.md`; best-effort DNS sync (continue-on-error) | Post-deploy CI assertion job (T8) | wrangler pinned @4 in CI — local commands must match; merge-triggered CI deploy + manual deploy would DOUBLE-deploy (concurrency group `cancel-in-progress: false`) → manual deploy removed as primary path (P1-3). |

### Journey Test Map

```markdown
### Journey: Evaluator evaluates Tortoise's legal posture before connecting an API key
1. **Step:** Visit `tortoise.premiselabs.co/` → **Acceptance:** product page renders; new footer shows Privacy · Terms · License links → **Test:** e2e footer presence (T7 #4)
2. **Step:** Click Privacy Policy → **Acceptance:** `/privacy` returns 200, styled, discloses Meta Pixel + GA4 + PostHog by name, no-sale + no-training + minimal-PII canonical sentences, processors list, rights incl. "one month" (Art. 12(3)) → **Test:** e2e negation-safe assertions (T7 #1)
3. **Step:** Click Terms of Service → **Acceptance:** `/tos` returns 200, styled, Business Source License boundary, Delaware, liability cap, 18+ eligibility, DPA trigger, same negation-safe commitments → **Test:** e2e negation-safe assertions (T7 #2)
4. **Step:** Click License / DPA → **Acceptance:** `/license` and `/dpa` return 200; BUSL explainer + "not legal advice" line on `/license`; DPA page present (G-gate ③/⑨ LOCKED) → **Test:** e2e (T7 #3)
5. **Step:** Sign up on `/signup` → **Acceptance:** "By creating an account, you agree to the Terms of Service and Privacy Policy" with working links, visible in the SAME viewport as OAuth buttons + email submit → **Test:** e2e same-viewport assertion (T7 #7)
6. **Step:** Sign in on `/signin` → **Acceptance:** footer legal links present → **Test:** e2e footer presence (T7 #4)

### Failure Modes
- `/privacy` or `/tos` returns 404 → **Expected:** static file missing or middleware interfering; 200 required on both hosts (premiselabs.co + tortoise.premiselabs.co) → **Test:** curl status checks (T7 #1-3, #5)
- Page contains "do not sell or share" anti-phrase or a training-positive phrase → **Expected:** negation guards fail loudly; wording must use canonical sentences exactly → **Test:** regex anti-pattern guards (T7 #1-2)
- Footer dead link (link target 404) → **Expected:** 0 broken links in production → **Test:** link crawl (T7 #8)
- Mobile viewport horizontal scroll / unreadable type on legal pages → **Expected:** mono 14px + ~720px max-width renders clean at 375px → **Test:** pytest mobile render screenshots (T7 #9)
- Acceptance statement present in DOM but scrolled out of view of OAuth buttons → **Expected:** same-viewport assertion fails; re-place between `.providers` and form → **Test:** pytest same-viewport (T7 #7)
- Broken design tokens (page unstyled) → **Expected:** visual regression via screenshot vs product.html token palette → **Test:** mobile render + manual render (T7 #9)
```

### UX Design Decisions

| # | Decision Type | User Choice | Rationale |
|---|---|---|---|
| 1 | Legal-body font (mono vs serif) | **Deferred to design pass** — recommend mono 14px to match brand; **decision must be resolved with evidence from E2E mobile render check (T7 #9)** per scope §3.2 | Long-form legibility vs brand consistency; E2E screenshot at ~375px is the evidence source |
| 2 | Footer link set | **Privacy · Terms · License · DPA** on product/welcome footers (**G-gate ⑨ LOCKED 2026-08-08: `/license` + `/dpa` both ship — no conditional**); secondary links in auth footers | Scope §5.3 sub-decisions (locked 2026-08-08); security/legal-hub slots reserved for later |
| 3 | Signup acceptance placement | Between `.providers` cluster and `#email-form` — same viewport as BOTH OAuth buttons and email submit | Common Paper clickthrough pattern; pytest-asserted (scope §5.7 #7) |
| 4 | Canonical host + "Back to product" behavior | **Canonical host = `tortoise.premiselabs.co`** — each legal page ships `<link rel="canonical" href="https://tortoise.premiselabs.co/{privacy|tos|dpa|license}">`; "Back to product" uses RELATIVE links (`/`) with the canonical decision documented; per-host resolution tested in T7 #4/#6 | Single canonical URL for the legal documents (SEO + link integrity) regardless of which host serves them (P2-7) |

**Pending:** mono-vs-serif resolution at T7 (evidence-driven).

### Tech Stack

Static HTML/CSS with existing design tokens · no build step · no runtime · Common Paper CSA v2.1 (CC BY 4.0) as drafting base · **pytest-playwright** (E2E — repo convention per #648: `python -m pytest tests/e2e/`; NO TS Playwright, no `playwright.config.*`/npm in this repo) · `npx wrangler@4 pages dev` (local preview) + `npx wrangler@4 pages deploy` (CI-only, deploy-pages.yml) · curl (post-deploy checks) · grep (negation/keyword assertions where specified).

---

## Tasks

> **Standard tier** — descriptive steps; TDD not applicable (content + static markup). G-gate task T3 is a **mandatory human stop**; no build task may begin before it passes.

### Task T1: Draft Terms of Service content (per scope §5.6 checklist)

**Intent:** Produce the full ToS legal text — the document the BUSL-1.1 Additional Use Grant defers to for the hosted product — accurate, disclosure-complete, and carrying Tortoise's stricter-than-market commitments. **Parallelizable with T2** (both feed T3 only).
**Acceptance:** Draft covers all 12 CSA sections + additions (below) with exact canonical wording; saved as a drafting artifact the G-gate can review; zero fabricated entity/fee facts.
**Files:**
- Create: `docs/drafts/2026-08-08-657-tos-draft.md` (draft artifact for G-gate — NOT `website/tos.html`; final markup is rendered in T4 from the approved draft)

**Step 1: Assemble the 15-section ToS content outline** (scope §5.6):
1. Definitions · 2. License grant + API keys · 3. Acceptable use/restrictions (no reverse engineering, no competing product, benchmarking restriction; **users must not upload special-category/sensitive data — health, biometrics, etc.**) · 4. Customer content ownership · 5. Fees & billing (**APPROVED 2026-08-08 — G-gate ⑥ LOCKED, fee clause):** general fee-structure framing, NO dollar figures — **"we may charge fees for the Service based on a combination of subscription, usage-based (metered), and/or per-seat pricing, as defined by the pricing tiers published on the Pricing Page"**; **Pricing Page reference HYPERLINKED from the ToS** — the pricing section on `tortoise.premiselabs.co` (no dedicated /pricing page yet; **pricing-reference URL finalized at build** in T4 Step 6: dedicated /pricing route or product-page pricing-section anchor); mechanics (standard, number-free): **price changes require 30 days' notice, effective at the next renewal term (never mid-term); continued use after notice = acceptance; free tier subject to published limits** and requires **NO payment method** (mirrors product page "No credit card"); **fees non-refundable except as required by law; paid plans may require a payment method; NO auto-renewal/recurring-subscription terminology until #432**; the amounts (solo $9 / pro $25 / team $149 + overage) are **CONTEXT ONLY** for the drafter/reviewer and must **NOT appear as dollar figures anywhere in the ToS body** (**P2-4**; dollar guard `== 1` unchanged); free-tier usage-limit placeholder `[free-tier usage limits to be published]`) · 6. IP ownership (+ trademark line: **"Tortoise" name/logo reserved to Premise Labs**) · 7. Suspension/termination (+ survival) · 8. Confidentiality · 9. Indemnification · 10. Warranty disclaimer · 11. Liability cap (paid tiers: "fees paid in prior 12 months"; **free tier: flat placeholder cap — resolved at G-gate**; **CSA §8 carve-outs retained: statutory/GDPR liability, fraud, willful misconduct NOT capped** — canonical carve-out sentence pinned at Step 2 below) · 12. Governing law (Delaware) + venue · 13. **No-AI-training clause** (replaces CSA v2.1 ML clause — exact wording below) · 14. **No-data-sale statement** · 15. **Minimal-PII wording** + eligibility 18+ + license-boundary section (BSL cross-ref) + DPA trigger (CSA §3) + effective date + contact (GitHub issues) + Common Paper attribution + revision-history block + **parties clause (Daniel Ospina (d/b/a Premise Labs)** — natural-person contracting party per D4 LOCKED; no entity-type placeholder).

**Step 2: Lock the canonical commitment wording (use EXACTLY — E2E-asserted):**
- No-training (negation of CSA v2.1 Machine Learning clause): **"we do not use your content or usage data to train AI models"** — unconditional, all tiers; must NOT contain any of: `may train`, `we train`, `to train our (AI )?models`, `train(ing)? (on|using) (your|user|customer)`. **Owner-confirmed canonical wording (G-gate ④ LOCKED 2026-08-08).**
- No-sale: **"we do not sell your personal information"** AND **"we share limited data with advertising/analytics providers as disclosed"** — co-occurring; NEVER "do not sell or share" (except the statutory CCPA link label, which does not ship this release).
- Minimal-PII: **"we do not intentionally collect sensitive personal information"**.
- Eligibility (P2-1): **"you must be at least 18 years old"** — canonical sentence pinned VERBATIM (this exact string is E2E-asserted at T7 #2) + a visible **"18+"** age marker on the ToS page; NEVER assert bare "18"/"age" (vacuous — "age" is a substring of "usage").
- Parties clause: **Daniel Ospina (d/b/a Premise Labs)** — natural-person contracting party; Premise Labs is **NOT a registered entity** (D4 LOCKED 2026-08-08); **no entity-type placeholder** (the `[ENTITY TYPE, JURISDICTION OF FORMATION TO BE CONFIRMED]` placeholder is REPLACED); personal-liability exposure acknowledged (scope P3-2); jurisdiction of residence stated in the drafts; **LFPDPPP consideration flagged (non-blocking)** if operator is Mexico-resident.
- Liability-cap carve-outs (CSA §8, cycle-5 P2-5): **"statutory/GDPR liability, fraud, and willful misconduct are not capped by this limitation of liability section"** — canonical sentence pinned VERBATIM (E2E co-occurrence asserted at T7 #2: "willful misconduct" + "not capped"); the paid-tier cap ("fees paid in prior 12 months") and the free-tier flat placeholder cap stay as in Step 1 ⑪.
- Fees (G-gate ⑥ LOCKED 2026-08-08): **"we may charge fees for the Service based on a combination of subscription, usage-based (metered), and/or per-seat pricing, as defined by the pricing tiers published on the Pricing Page"** — canonical sentence pinned VERBATIM (E2E-asserted at T7 #2); the phrase **"Pricing Page"** is hyperlinked in `tos.html` to the pricing source (T4 Step 6 asserts the href resolves; T7 #2 asserts the href is present and not `#`); the price-change sentence must contain **"30 days"** + **"renewal"** (co-occurrence-asserted at T7 #2) and **"non-refundable"** (fees non-refundable except as required by law); NO `$` figures in or near the sentence (the whole-ToS dollar guard `== 1` at T7 #2 covers it).

**Step 3: Cross-check the license boundary against the worktree LICENSE (truth source):**
Self-hosted governed by **BUSL-1.1** (link `github.com/daniel-ospina/tortoise/LICENSE`; $5M AUG; 4y → **MPL 2.0**; hosted explicitly excluded from grant); hosted service = separate commercial product, free tier; **$5M-AUG dollar exemption (cycle-4 P1-4; count pinned `== 1` at cycle-5 P2-2):** the "$5M" AUG threshold in this license-boundary section is the SOLE allowed `$[0-9]` occurrence in `tos.html` — the T4 Step 5 / T7 #2 dollar-figure guards assert count EXACTLY 1 (`== 1` — never zero, never more); `license.html` keeps "$5M" unguarded (the guard targets `tos.html` only); **do NOT describe Tortoise as "open source"** (source-available). Source: `.worktrees/657-legal/LICENSE` + `docs/license-notes.md` — NOT local `main`'s stale AGPL/pyproject MIT. **License-drift note (P2-3):** post to the #657 thread — and include in the PR description at T8 Step 4 — that the local `main` tree still carries the stale AGPL LICENSE + pyproject MIT while this worktree's truth source is BUSL-1.1, so reviewers/CI license-surface checks don't mistake drift for intent.

**Step 4: Verify the draft against the checklist:** 12 required CSA sections + all §5.6 additions present; Common Paper CC BY 4.0 attribution line planned for `tos.html` footer ("Based on the Common Paper Cloud Service Agreement v2.1, CC BY 4.0"); material-change notice language included.

**Step 5 (optional, ~30 min): Clause-level sanity check vs comparables** — spot-check the drafted clauses against the LangSmith/Pinecone/Supabase ToS terms already fetched in the research brief (comparables section) for clause-level consistency. **Decision (P2-4): carried as optional** — rationale: the comparables are already in-repo from research (cheap to consult), and the drafting base is the audited CSA v2.1 template, so this is a sanity net, not a blocker. If skipped, note the skip + rationale in the draft header.

**Expected output:** Complete ToS content draft (sections 1–15 with canonical wording) ready for G-gate review.

---

### Task T2: Draft Privacy Policy content (per scope §5.6 checklist)

**Intent:** Produce the full Privacy Policy — the disclosure-complete document that stays true when #528 ships Meta Pixel + GA4 + PostHog (disclose-now, instrument-later) and enshrines the no-sale/no-training/minimal-PII commitments for GDPR/CCPA defensibility. **Parallelizable with T1** (both feed T3 only).
**Acceptance:** Covers all 9 required areas + processors + additions (below) with canonical wording and conditional/future analytics framing; rights-request mechanics (Art. 12(3)/12(6), Art. 27 line) included; **erasure path = self-service deletion primary with scoped claim per the T2 Step 5 build-time capability check (G-gate ⑧ LOCKED)**; zero present-tense claims for undeployed instrumentation.
**Files:**
- Create: `docs/drafts/2026-08-08-657-privacy-draft.md` (draft artifact for G-gate — NOT `website/privacy.html`; final markup is rendered in T4 from the approved draft)

**Step 1: Assemble the Privacy content outline** (scope §5.6): ① controller identity + contact (**Daniel Ospina (d/b/a Premise Labs)** — natural-person controller, D4 LOCKED 2026-08-08; jurisdiction of residence stated; LFPDPPP non-blocking note) + **GDPR Art. 27 line — EXACT: "We are not currently established in the EU/EEA. If we become subject to GDPR Art. 27, we will designate and disclose an EU representative here."** (resolution = G-gate ⑦ — **LOCKED 2026-08-08: conditional line accepted**; must be settled BEFORE EU onboarding post-#533 GA) ② data categories (account/email/login, usage data, analytics; **expectation line: "we expect users not to place sensitive data in the service"**) ③ analytics/cookies — **Meta Pixel + Google Analytics (GA4) + PostHog named by name**; conditional/future framing ("when you use the hosted service we may use [tools]; activated with your consent") + current deployment status (none deployed today) ④ GDPR legal bases + **purposes of processing (cycle-5 P1-3, pinned — PASSIVE phrasing so it never matches the present-tense guard): EXACT: "your email address and account information are used to deliver the service, respond to requests, and manage billing; usage data is used to improve the product"** + ePrivacy consent — **EXACT: "consent will be obtained via a banner when these tools are activated"** (banner = follow-up #658) ⑤ CCPA/CPRA: statutory **"We do not sell your personal information"** + **"sharing" disclosure for Meta** + **Meta joint-controller disclosure (Fashion ID theory — name Meta; note shared data may be used by Meta for its own purposes)**; **NO dedicated "Do Not Sell or Share My Personal Information" link this release** (deferred until real opt-out tooling via #658 — checklist ensures no dead link ships); **Shine the Light (Cal. Civ. Code §1798.83) one-liner** + **Meta sharing/joint-controller disclosure CONDITIONAL: "Meta is a joint controller; shared data may be used by Meta for its own purposes" — gated on #528, re-check at #528 resolution (mirrors ⑭ wording, P1-4)** ⑥ retention ⑦ security ⑧ user rights (GDPR + CCPA/CPRA) ⑨ DPA availability ⑩ **processors / third-party sharing** (tied to DPA Art. 28 + transfers): **Supabase** (auth+DB — real PII), **Cloudflare** (hosting), **GitHub + Google** (OAuth identity; GitHub also processes rights-request submissions), **Meta/GA4/PostHog** (analytics when activated), **Stripe** (billing when live, #432) ⑪ no-AI-training pledge ⑫ international transfers (DPF + SCCs) ⑬ no-intentional-sensitive-PII wording ⑭ **PostHog residency CONDITIONAL: "PostHog is a data processor; EU residency (Frankfurt) used when configured" — gated on #528, re-check at #528 resolution** ⑮ version/effective date + revision-history block ⑯ **user rights / erasure — G-gate ⑧ LOCKED 2026-08-08: self-service deletion is the PRIMARY rights mechanism.** Privacy page primary erasure path = **in-product self-service "delete account"** (authenticated by construction — solves identity verification; instant, no 1-month clock on that path). **Long-tail channel (GitHub-issues, D2) — legal honesty guardrails:** (a) instruct users to file requests WITHOUT exposing personal data publicly — redact; use a GitHub account not tied to identity (tracker is public); (b) **Art. 12(3) one-month response window + 2-month extension-with-notice language**; (c) **long-tail applies to**: non-account data, users who can't log in, access/export/object requests — **private intake form NOT needed now — DEFERRED (G-gate ⑧); revisit if long-tail volume grows** (D2's "no invented email" does not preclude a form later); (d) **Art. 12(6) EXACT: "we may request identity verification before acting on any request"**; (e) GitHub-as-processor tie to ⑩. **Retention carve-outs stated honestly:** billing/transactional records retained as required by law (Art. 17(3)); analytics/usage data per the analytics section — never silently claimed deleted. **Build-time capability check → T2 Step 5** (policy claims the self-service path ONLY if the product has a working delete-account; else scoped claim + product follow-up). Optional: self-service data export/access button (nice-to-have, not launch-required).

**Step 2: Lock the canonical wording** — same sentences as T1 Step 2 (no-training, no-sale + sharing co-occurrence, minimal-PII, eligibility) + Art. 27 + consent-banner + Art. 12(6) lines verbatim from Step 1 + **repo-state sentence (P2-2): EXACT wording "no analytics tools are currently deployed"** — T4 renders THIS string verbatim on `privacy.html`, and T7 #10(c1) asserts it IFF the local tree has no instrumentation markers, so the IFF assertion is guaranteed by construction (the sentence ships alongside exactly the state it describes). **Classification rule (cycle-4 P1-5, cycle-5 P1-3 + P2-6, pinned — stated IDENTICALLY in T2 Step 2, T4 Step 5, and T7 #10(e)):** (1) REQUIRED live, true disclosures are EXPECTED content and are drafted truthfully in present tense, but phrased PASSIVELY/IMPERSONALLY so they never trip the guard: the processors section (Supabase, Cloudflare, GitHub, Google — operating today; `signup.html` makes real `supabaseClient` calls) and the collected-data categories (email/login, usage data). (2) Only NEW present-tense claims OUTSIDE the approved pinned set fail the guard. (3) The chosen mechanism is the RECOMMENDED passive-voice option (pinned; no allowlist drift): processors, data-categories, retention, and transfer statements use passive/impersonal phrasing — "data is processed by Supabase for authentication", "account data includes your email address", "data is retained only for as long as needed" — so they NEVER match the guard regex `we (use|collect|share|sell|process|retain|store|transfer)\b`; no allowlist additions needed.

**Step 3: Drafting rule check** — every analytics disclosure uses conditional/future wording + states current deployment status; nothing asserts instrumentation that doesn't exist.

**Step 4 (optional, ~30 min): Clause-level sanity check vs comparables** — same as T1 Step 5: spot-check against LangSmith/Pinecone/Supabase privacy terms already fetched in research. **Decision (P2-4): carried as optional** with the same rationale; skip + note if not needed.

**Step 5: Delete-account capability build-time check (G-gate ⑧ LOCKED 2026-08-08) — run BEFORE finalizing the draft for the G-gate (informs ⑯ wording):**
VERIFY the product actually has a working self-service delete-account capability before the policy claims it (the primary erasure path per G-gate ⑧). Check the app surfaces — `website/welcome.html` (post-signup flows), `apps/dashboard/` (dashboard UI — deployed separately to `tortoise-dashboard`), `apps/graph-viz/` (app UI + `server/main.py` endpoints) — for any delete-account / account-settings / deactivate-user UI or a user-deletion API (Supabase Auth admin `deleteUser`).
- **2026-08-08 repo check (informs the expected outcome):** **NO delete-account capability exists today** — `apps/dashboard` is a shell only (`index.html` + built dist; no account-management UI); `apps/graph-viz` has no account settings/delete/profile UI and `server/main.py` exposes only content-level deletes (`DELETE /api/points/{id}` / `/api/edges/{id}`) with NO user/account deletion endpoint; `welcome.html` links only to key management.
- **IF the check confirms ABSENCE at build (expected):** the privacy policy MUST scope the claim — primary erasure path = **"request deletion via GitHub issues"** + **Art. 12(3) one-month commitment** + **Art. 12(6) identity verification**; do NOT promise an in-product delete button; **FILE a product follow-up issue** for the self-service delete feature (Supabase Auth admin `deleteUser` + dashboard account UI — reference scope §6.2 #5) and note it in the T3 decision log.
- **IF the check finds a working capability:** the policy claims the in-product path (self-service delete account) as primary, with the long-tail channel as secondary; record the evidence (URLs/paths) in the draft header.
- Record the check result + chosen branch in the draft header (`docs/drafts/2026-08-08-657-privacy-draft.md`).

**Expected output:** Complete Privacy content draft (16 areas) ready for G-gate review.

---

### Task T3: OWNER G-GATE — mandatory human approval stop

**Intent:** Legal text is a binding commitment by a natural person (entity unknown); the owner must approve final wording and resolve 9 locked decision points (①–⑧ + ⑩ carve-outs; ⑨ is the link-set record) before ANY build work. This task is the hard gate from scope Phase 3 (Plan step 8) and issue indicator 5.
**Acceptance:** Owner (Daniel) responds with explicit approval of (a) the FULL draft texts (`docs/drafts/2026-08-08-657-tos-draft.md` + `docs/drafts/2026-08-08-657-privacy-draft.md` — reviewed as complete documents, not diffs), (b) one decision per checklist item below, and (c) the LINK-SET DECISION RECORD (per page: `/license` page vs GitHub LICENSE URL; `/dpa` page vs omit); decisions recorded in the issue thread — and (d, **cycle-5 P1-4**): completion REQUIRES recording the URL of the OWNER-APPROVED comment on #657 — an OWNER-AUTHORED comment containing the explicit approval statement + the decision log + the link-set record; if no such owner comment exists, T3 REMAINS OPEN and sub-agents must NOT mark it complete (no "owner approval assumed" paths); plan may proceed to T4 ONLY after this task is marked complete with that comment URL recorded.
**Files:**
- Modify: none (decision record → issue thread comment)

**Step 1: G-gate decision log — owner answered 2026-08-08 (session); recorded in the #657 thread by the decisions recorder. ALL items LOCKED (2026-08-08): ⑥ RESOLVED (fees clause) + ⑧ RESOLVED (self-service deletion primary; private intake form deferred).** (Re-confirmation at approval per scope §5.4 step 8 — scope rev 7):
① **effective date = actual deploy date at T8** — NOTE (unchanged; bump + changelog entry if deploy slips); D1–D8 current per scope §0 (rev 5) ② **Entity — LOCKED:** Premise Labs is **NOT a registered entity**; contracting party/controller = **Daniel Ospina, individual operating as "Premise Labs" (d/b/a)**; ToS parties clause names the natural person — **acknowledge personal-liability exposure (P3-2)**; **`[ENTITY TYPE, JURISDICTION OF FORMATION TO BE CONFIRMED]` placeholder REPLACED** with the individual designation (T1 Step 2 / T2 Step 1); jurisdiction of residence stated in the drafts; **LFPDPPP consideration flagged (non-blocking)** if operator is Mexico-resident ③ **DPA — LOCKED: ship now** — `dpa.html` WILL ship (D3); fallback ("DPA available on request") no longer applicable; CSA §3 trigger clause stays in ToS ④ **no-training wording — CONFIRMED by owner:** canonical sentence "we do not use your content or usage data to train AI models" (T1 Step 2), unconditional all tiers (D5) ⑤ **docs.html mailto fix — APPROVED** (→ GitHub-issues link, in scope, D2) ⑥ **FEE CLAUSE — LOCKED (2026-08-08, owner + legal research, G-gate ⑥ RESOLVED):** fees described **generally, NO dollar figures** — canonical sentence (T1 Step 2): **"we may charge fees for the Service based on a combination of subscription, usage-based (metered), and/or per-seat pricing, as defined by the pricing tiers published on the Pricing Page"**; the **Pricing Page reference is HYPERLINKED** from the ToS — pricing section on `tortoise.premiselabs.co` (no dedicated /pricing page yet; **URL finalized at build** — dedicated /pricing route or product-page pricing-section anchor; T4 Step 6 verifies the href resolves); mechanics (standard, number-free): **price changes require 30 days' notice, effective at the next renewal term (never mid-term); continued use after notice = acceptance; free tier subject to published limits; fees non-refundable except as required by law; paid plans may require a payment method; auto-renewal/invoicing deferred until #432 ships**; the amounts (solo $9 / pro $25 / team $149 + overage) are **CONTEXT ONLY** for the drafter/reviewer; the ToS body contains **NO dollar figures** (**P2-4**; dollar guard `== 1` unchanged — sole exemption the "$5M" AUG in the license-boundary section); **placeholder shape pinned (cycle-5 P2-3):** the free-tier usage-limit placeholder is pinned to LOWERCASE PROSE — exactly `[free-tier usage limits to be published]` — so it never matches the D4-shape regex `\[[A-Z][A-Z ,]{8,}\]` (uppercase-only); if the free-tier cap resolves later, replace the placeholder with the actual cap wording (still lowercase prose, no dollar figures); **pricing-page consistency dependency (NEW):** product-page free-tier numbers conflict ("5,000 Points free" vs "1,000 write ops/mo") — reconcile BEFORE the ToS references the pricing page (under #432 or in this issue's build step; T4 Step 6 + T7 #2 checks; scope §6.2 follow-up 4) ⑦ **Art. 27 — LOCKED:** conditional line ("we are not currently established in the EU/EEA; if we become subject to GDPR Art. 27 we will designate and disclose an EU representative here") + resolve before EU onboarding post-#533 GA ⑧ **private rights-request intake — LOCKED (2026-08-08, owner + legal research — GDPR Art. 12/17 + EDPB guidance): self-service deletion is the PRIMARY rights mechanism** — NO private intake form now. (1) Privacy policy primary erasure path = **in-product self-service "delete account"** (authenticated by construction — solves identity verification; instant, no 1-month clock on that path); (2) **build-time dependency check (T2 Step 5 + T7):** VERIFY the product has a working delete-account capability BEFORE the policy claims it — 2026-08-08 repo check: **ABSENT today** (dashboard shell only; graph-viz no account mgmt; server deletes only user points/edges) → expected branch: scope the claim ("request deletion via GitHub issues" + Art. 12(3) 1-month + Art. 12(6) verification) + file the product follow-up (scope §6.2 #5); do NOT promise a feature that doesn't exist; (3) **long-tail channel remains**: non-account data, can't-log-in users, access/export/object requests → GitHub issues + redaction guidance + 1-month commitment + extension-notice + identity verification (Art. 12(6)); (4) **retention carve-outs stated honestly**: billing/transactional as required by law (Art. 17(3)); analytics/usage per the analytics section; (5) **private Supabase intake form → NOT needed now — DEFERRED** (revisit if long-tail volume grows; no build pressure); (6) optional self-service data export/access button (nice-to-have, not launch-required).
⑨ **LINK-SET DECISION RECORD — LOCKED per recommendation (2026-08-08):** License → ship `license.html` (BSL explainer); DPA → ship `dpa.html`. **Both pages ship; footers link them.** No fallbacks apply (the "footer links directly to GitHub LICENSE" and "omit dpa page" options are no longer available). Recorded so T4/T5 render the approved link set (P2-1).
⑩ **LIABILITY-CAP CARVE-OUTS — LOCKED (2026-08-08):** CSA §8: statutory/GDPR liability, fraud, and willful misconduct are NOT capped by the limitation-of-liability section (canonical sentence per T1 Step 2 — co-occurrence-asserted at T7 #2); carve-outs ship in the ToS body.

**Step 2: Record outcomes in the issue thread** — the G-gate DECISION LOG was posted to #657 on 2026-08-08 by the decisions recorder (owner answered the G-gate items in a session; decisions locked into scope rev 5 + this plan rev 7). Per cycle-5 P1-4, the OWNER's OWNER-AUTHORIZED APPROVAL comment on #657 is **STILL REQUIRED** for T3 completion — an owner-authored comment containing: (i) the explicit approval statement for (a) the full draft texts + (b) the checklist decisions + (c) the link-set record; **record the URL of that comment in this task's output (cycle-5 P1-4)**. The decision-log comment is the RECORD, not the approval.

**Step 3: STOP — do not proceed to T4 until Step 2 completes with explicit owner approval.** If any item is deferred, record the fallback from scope §5.3 (DPA defer → "available on request"; license-page defer → footer links directly to GitHub LICENSE).

**Expected output:** Owner-approved decision log; **entity resolved (2026-08-08: individual d/b/a "Daniel Ospina (d/b/a Premise Labs)" — placeholder REPLACED)**; **DPA + license pages locked to ship (G-gate ③/⑨)**; **fee clause LOCKED (G-gate ⑥ RESOLVED 2026-08-08 — general fee-structure framing + hyperlinked Pricing Page + 30-day notice/renewal mechanics, no dollar figures)**; **item ⑧ LOCKED (2026-08-08 — self-service deletion primary, private intake form deferred; T2 Step 5 build-time capability check result recorded — expected ABSENT → scoped claim + product follow-up scope §6.2 #5)**; owner-approval comment URL recorded (cycle-5 P1-4).

---

### Task T4: Build privacy.html + tos.html (+ dpa.html, license.html — REQUIRED, G-gate ③/⑨ LOCKED)

**Intent:** Render the G-gate-approved content (from the T1/T2 drafts, NOT re-drafted from scratch) as token-consistent, mobile-safe static pages with revision history — the deliverables that serve at `/privacy` and `/tos`.
**Acceptance:** Both pages render with design tokens (mono meta-labels, serif headings, mono 14px body, ~720px max-width, `--bg:#060b14` palette), include a "Back to product" link (pattern from `self-hosted.html`), a revision-history block ("Document history / revisions" + effective date + changelog), Common Paper CC BY 4.0 attribution in `tos.html` footer, pass local content-checklist completeness, **and the Pricing Page reference resolves + is hyperlinked in `tos.html` (G-gate ⑥ — T4 Step 6)**; **`dpa.html` + `license.html` ship unconditionally (G-gate ③/⑨ LOCKED 2026-08-08 — no longer conditional)**. **G-gate proof (cycle-5 P1-4):** BEFORE any build work, this task's output must record the URL of the OWNER-APPROVED comment on #657 (per T3 Step 2 — owner-authored, containing explicit approval + decision log + link-set record); if no owner comment exists, T3 remains OPEN and T4 must NOT begin build work.
**Files:**
- Create: `website/privacy.html`
- Create: `website/tos.html`
- Create: `website/dpa.html` (**REQUIRED** — G-gate ③ LOCKED 2026-08-08: ship now; Common Paper DPA, CC BY 4.0)
- Create: `website/license.html` (**REQUIRED** — G-gate ⑨ LOCKED 2026-08-08: BUSL explainer: $5M AUG, 4y → MPL 2.0, hosted = commercial, contact = GitHub issues; **"not legal advice" one-liner**)

**Step 1: Scaffold the page chrome** — copy the `<head>` token block + page chrome from `product.html` (tokens at lines ~20–100) / `self-hosted.html` back-link pattern; legal pages get `max-width: 720px`, mono meta-labels ("LAST UPDATED", "EFFECTIVE DATE", version), serif headings, mono 14px body. **Effective date format (P2-9):** a visible text line **`Effective date: YYYY-MM-DD`** is MANDATORY and must appear EXACTLY once per page — the visible line is the canonical extractable string, NOT an alternative to an attribute; a `data-effective-date` attribute is OPTIONAL and purely additive (never a substitute for the visible line), so T8 can extract and compare the date deterministically from the served body text. **Add `<link rel="canonical" href="https://tortoise.premiselabs.co/{page}">` to each legal page (P2-7)**; "Back to product" uses relative links (`/`) so it resolves correctly on either host.

**Step 2: Author `tos.html`** from the T1 draft — all 15 sections, canonical wording, revision-history block, material-change notice language, Common Paper attribution line.

**Step 3: Author `privacy.html`** from the T2 draft — all 16 areas, canonical wording + conditional analytics framing, revision-history block.

**Step 4: Author `dpa.html` + `license.html` (REQUIRED — G-gate ③/⑨ LOCKED 2026-08-08, no longer conditional):** `dpa.html` (Common Paper DPA template, CC BY 4.0 attribution) and `license.html` (BUSL explainer + "not legal advice" line + link to GitHub LICENSE). **license.html dollar exemption (cycle-4 P1-4):** the "$5M" AUG figure in the BUSL explainer is UNGUARDED — the dollar-figure guard applies to `tos.html` only (T4 Step 5 / T7 #2), never `license.html`.

**Step 5: Local sanity checks** — `grep -c 'train AI models' website/tos.html website/privacy.html` ≥ 1 per file; `grep -n 'do not sell' website/tos.html website/privacy.html`; confirm NO `mailto:` anywhere in new files; broad template-placeholder grep — `grep -nE '\{\{|\$\{|<INSERT|TODO' website/privacy.html website/tos.html` must return NOTHING (D4-shape placeholder handling is the T7 #10(d2) concern — **entity resolved 2026-08-08, placeholder replaced → d2 now asserts ABSENCE (count 0)**; not a build-time failure, **P2-1**); **HTML parse gate (cycle-4 P1-2 — stdlib `HTMLParser` is LENIENT: `feed()` NEVER raises on unbalanced tags):** pin the concrete mechanism — a STACK-TRACKING `HTMLParser` subclass (small checked-in script, e.g. `scripts/check_html_balance.py`, or inline) that maintains an OPEN-TAG STACK and exits NONZERO on: (i) a closing tag that does NOT match the stack top (mismatched/unbalanced close), (ii) a NON-EMPTY stack at EOF (unclosed tags), (iii) a premature `</html>`/`</body>` (document closed before remaining content). Applied to ALL tags. **Void-element handling (cycle-5 P1-2 — `HTMLParser` has NO void-elements attribute — it exposes only `CDATA_CONTENT_ELEMENTS`/`interesting_normal`; `handle_startendtag` fires ONLY for self-closed `<tag/>`):** pin an explicit `VOID = {"meta","link","br","img","input","hr","source"}` set and check it in BOTH overridden handlers — in `handle_starttag(tag, attrs)`, `if tag in VOID: return` (do NOT push), and in `handle_startendtag(tag, attrs)`, NEVER push; this guarantees void tags never leave stack residue (product.html's head uses slash-less `<meta>`/`<link>` — 0 self-closed void tags — those fire `handle_starttag` and are covered by the VOID check). html5lib/tidy are an OPTIONAL enhancement only (not required — the stack gate is meaningful standalone); **dollar-figure guard (cycle-4 P1-4, pinned `== 1` at cycle-5 P2-2):** `grep -cE '\$[0-9]' website/tos.html` must be EXACTLY 1 (`== 1` — never 0, never more) — the SINGLE allowed occurrence is the "$5M" AUG threshold in the license-boundary section (T1 Step 3 exemption); no other dollar figures anywhere in the ToS body ("fees as published" textually only — amounts solo $9 / pro $25 / team $149 are CONTEXT ONLY for the drafter, never rendered); **anti-phrase guard (P2-4):** `grep -n 'do not sell or share' website/privacy.html website/tos.html` must return NOTHING (exit 1) — the CCPA link label does not ship this release; **eligibility guard (P2-1):** `grep -n 'you must be at least 18 years old' website/tos.html` ≥ 1 AND `grep -c '18+' website/tos.html` ≥ 1; **carve-out co-occurrence guard (cycle-5 P2-5):** `grep -nE 'willful misconduct' website/tos.html` ≥ 1 AND `grep -nE 'not capped' website/tos.html` ≥ 1 — the CSA §8 carve-outs (statutory/GDPR liability, fraud, willful misconduct NOT capped) must be stated, not just implied; **present-tense guard (cycle-4 P1-5, regex widened at cycle-5 P1-3):** `grep -nE 'we (use|collect|share|sell|process|retain|store|transfer)\b' website/privacy.html website/tos.html` must return ONLY the pinned expected-match subset — the SUBSET of canonical sentences (locked at **T1/T2 Step 2**: no-training, no-sale co-occurrence, minimal-PII, Art. 27, consent-banner, Art. 12(6), repo-state, carve-outs) that actually MATCH the guard regex (most canonical sentences contain "we do" and do NOT match — the expected-match subset is typically just the no-sale sharing sentence) — the SAME set T7 #10(e) asserts against (P2-8), so T4 and T7 can never disagree — any other hit = re-draft with conditional wording. **Classification rule (cycle-4 P1-5, cycle-5 P1-3 + P2-6, pinned — stated IDENTICALLY in T2 Step 2, T4 Step 5, and T7 #10(e)):** (1) REQUIRED live, true disclosures are EXPECTED content and are drafted truthfully in present tense, but phrased PASSIVELY/IMPERSONALLY so they never trip the guard: the processors section (Supabase, Cloudflare, GitHub, Google — operating today; `signup.html` makes real `supabaseClient` calls) and the collected-data categories (email/login, usage data). (2) Only NEW present-tense claims OUTSIDE the approved pinned set fail the guard. (3) The chosen mechanism is the RECOMMENDED passive-voice option (pinned; no allowlist drift): processors, data-categories, retention, and transfer statements use passive/impersonal phrasing — "data is processed by Supabase for authentication", "account data includes your email address", "data is retained only for as long as needed" — so they NEVER match the guard regex `we (use|collect|share|sell|process|retain|store|transfer)\b`; no allowlist additions needed. **effective-date canonical form (P2-9):** exactly one visible `Effective date: YYYY-MM-DD` string per page. **Section-title assertion lists (cycle-4 P2-4):** commit `docs/drafts/2026-08-08-657-tos-section-titles.txt` and `docs/drafts/2026-08-08-657-privacy-section-titles.txt` (one section title per line) DERIVED VERBATIM from the G-gate-approved drafts — NOT invented at test time — so T7 #10(f) imports the list from disk as the source of truth for the 15/16 section assertions. **Draft↔render fidelity check (cycle-5 P1-5 — re-run in T7 #10(g)):** normalize the approved draft (strip markdown formatting, lowercase, collapse whitespace) vs the rendered page body text (strip tags, lowercase, collapse whitespace) and assert per-section containment — EVERY draft sentence ≥ 40 chars (after normalization) appears in the rendered text; this closes the approve-render gap (paraphrase/drop/reorder detection) and also covers the processors ⑩ name presence (Supabase, Cloudflare, GitHub, Google, Meta, GA, PostHog, Stripe are all ≥ 40-char sentences in the draft). Sub-40-char fragments (e.g., the pinned `[free-tier usage limits to be published]` placeholder — G-gate ⑥ LOCKED 2026-08-08) are EXEMPT — they may legitimately be resolved/replaced at the G-gate.

**Step 6: Pricing Page reference check (G-gate ⑥ LOCKED 2026-08-08):** confirm `tos.html` renders the canonical fees sentence (T1 Step 2) AND the **"Pricing Page" phrase is HYPERLINKED** — `grep -n 'Pricing Page' website/tos.html` must show the phrase inside an `<a href="...">` (never bare text); **finalize the pricing-reference URL at build:** the pricing section on `tortoise.premiselabs.co` — there is NO dedicated /pricing page yet (the pricing tiers live in `product.html`'s JS, ~line 690), so either (a) create a dedicated `/pricing` page in this issue (same design tokens) and link that, or (b) link the product-page pricing-section anchor (e.g. `https://tortoise.premiselabs.co/#pricing` — verify the anchor id exists in `product.html`); the chosen href must RESOLVE — fetch it from the local preview (`npx wrangler@4 pages dev`) and confirm 200, or pre-deploy confirm the anchor exists in local `product.html`. **Pricing-consistency note (pre-merge check / #432 dependency):** the product page's free-tier numbers CONFLICT today ("5,000 Points free" vs "1,000 write ops/mo" — scope §1.3 / §6.2 follow-up 4) — reconcile the pricing section text (in this issue's build step or under #432) BEFORE the ToS hyperlinks it; the T8 Step 4 **G-PricingRef** pre-merge gate re-verifies the referenced source is live + consistent; T7 #2 asserts the fee keywords on the served page.

**Expected output:** 2 (or 4) styled, complete legal pages passing local grep checks.

---

### Task T5: Site markup edits (footers ×5 + signup acceptance + docs.html mailto) — merged from former T5+T6 (P2-2)

**Intent:** All same-file markup changes in ONE task to eliminate concurrent-edit risk: wire the legal pages into navigation (creating footers where none exist — scope finding: product.html and welcome.html have NO footer — and appending links where they do), create the Common Paper clickthrough acceptance moment on signup (visible same-viewport with OAuth + email submit), and absorb the D2 contact-consistency fix in `docs.html`.
**Acceptance:** `product.html` has a footer link cluster (Privacy · Terms · License · DPA — **G-gate ⑨ link set LOCKED: all four ship, no conditional**) before `</body>` (line 768); `welcome.html` has a minimal footer; `signin.html`/`signup.html`/`self-hosted.html` footers carry the legal links; all links resolve to `/privacy`, `/tos`, `/license`, `/dpa` (link set per T3 record); `/signup` shows "By creating an account, you agree to the Terms of Service and Privacy Policy" with working links, placed between `.providers` (ends ~line 275) and `#email-form` (line 278), visible in the same viewport as BOTH OAuth buttons and the email submit button (pytest-asserted in T7); `docs.html` contains NO `mailto:` (GitHub-issues link only).
**Files:**
- Modify: `website/product.html` (create footer, insert before `</body>` line 768)
- Modify: `website/welcome.html` (create minimal footer — top `<nav>` at line 297 exists, no footer)
- Modify: `website/signin.html` (append to `.footer` div, line 290)
- Modify: `website/signup.html` (append to `.footer` div AND insert acceptance block between `.providers` close and `<div class="divider">`, ~line 276)
- Modify: `website/self-hosted.html` (append to `<footer>`, line 130)
- Modify: `website/docs.html` (line 217: `mailto:hello@premiselabs.co` → `https://github.com/daniel-ospina/tortoise/issues`)

**Step 1: Create footer cluster in `product.html`** — insert before `</body>` (line 768): `<footer class="legal-footer">` with links `Privacy Policy → /privacy`, `Terms of Service → /tos`, `License → /license`, `DPA → /dpa` (**G-gate ⑨ LOCKED — no fallback, no conditional**); style with `.legal-footer` matching `.selfhost-link` styling (lines 323–330) or card tokens.

**Step 2: Create minimal footer in `welcome.html`** — same link set, styled to the page's design.

**Step 3: Append links to existing footers** — `signin.html` (.footer line 290), `signup.html` (.footer), `self-hosted.html` (line 130) — same hrefs, compact inline style.

**Step 4: Insert the acceptance block in `signup.html`** — after the `.providers` closing `</div>` (line ~275), before `<div class="divider">or</div>` (line 276):
```html
<p class="legal-accept">
  By creating an account, you agree to the
  <a href="/tos" target="_blank" rel="noopener">Terms of Service</a> and the
  <a href="/privacy" target="_blank" rel="noopener">Privacy Policy</a>.
</p>
```
Style `.legal-accept` at ~14px dim text (`.text-dim` token) with margin; keep the block compact so it fits the same viewport as OAuth buttons + submit (T7 #7 asserts this). DOM requirement for T7 (P1-2): `.legal-accept` is a SIBLING of `.providers` (and `.divider`), PRECEDES `<form id="email-form">` in document order, and is NOT a descendant of the form — never assert sibling-of-`#email-form` (`#email-form` is the `<form>` element itself).

**Step 5: Fix `docs.html`** — replace line 217 `Questions? Email <a href="mailto:hello@premiselabs.co">hello@premiselabs.co</a>` with `Questions? Open an issue at <a href="https://github.com/daniel-ospina/tortoise/issues" target="_blank" rel="noopener">github.com/daniel-ospina/tortoise/issues</a>`.

**Step 6: Verify** — `grep -n 'privacy.html\|/privacy\|tos.html\|/tos' website/product.html website/welcome.html website/signin.html website/signup.html website/self-hosted.html` returns the expected link lines; `grep -c '<footer' website/product.html website/welcome.html` ≥ 1 each; `grep -n 'legal-accept' website/signup.html` returns the block; `grep -n 'mailto' website/docs.html` returns NOTHING (exit 1).

**Expected output:** Legal links in footers on all 5 pages with resolvable hrefs (per T3 link set), same-viewport acceptance block on signup, no mailto in docs.html.

---

### Task T7: E2E verification — pytest-playwright, negation-safe, pre-merge local preview (per scope §5.7)

**Intent:** Prove the contract — 200s on both hosts (primary = premiselabs.co), canonical commitments with negation guards (a page that merely avoids banned phrases must FAIL), same-viewport acceptance, mobile readability — against a LOCAL preview pre-merge. No production URLs are asserted pre-merge (P1-3).
**Acceptance:** All scope §5.7 checks 1–9 + served-content assertions (check #10) pass as pytest tests; negation-safe assertion blocks pass on both `privacy` and `tos`; 0 broken links; no horizontal scroll at 375px; effective-date vs served content is asserted in T8 (moved, P1-2); existing `tests/e2e/test_welcome_page.py` suite still passes (regression).
**Files:**
- Create: `tests/e2e/test_legal_pages.py` (pytest-playwright — repo E2E convention per #648; reuse patterns from `tests/e2e/test_welcome_page.py`: sync `page` fixture, `page.route()` interception helper style. NO TS Playwright: there is no `playwright.config.*`/npm in this repo)
- Env: `BASE_URL` overrides the target (default `https://premiselabs.co` — the always-green E2E baseline; **DNS CNAME FIXED by operator 2026-08-08, `tortoise.premiselabs.co` is the canonical host again — propagation verified at T7 Step 0**); `TORTISE_HOST` for `tortoise.*` checks (default `https://tortoise.premiselabs.co`); `TORTISE_HOST_CHECK` gates the tortoise.* tests (skipif contract below — kept as the propagation guard: tortoise.* tests RUN once propagation is confirmed green at T7 Step 0, SKIP green-with-annotation otherwise). **Opt-in short-circuit (cycle-4 P1-1, pinned — a RUNTIME statement, not pytestmark):** the FIRST executable statement after imports is `if not os.environ.get("RUN_LEGAL_E2E"): pytest.skip("legal suite: opt-in via RUN_LEGAL_E2E=1")` — a plain module-level runtime call; `pytest.skip()` in module code skips the whole module at collection WITHOUT erroring. NEVER `pytest.exit()` (it aborts the WHOLE pytest session → red `welcome-e2e` on every PR). **Invariant, stated honestly (cycle-4 P1-1):** bare collection NEVER ERRORS — skip is the GUARANTEED outcome on every surface that collects this module without `RUN_LEGAL_E2E=1`. **Analyzed collection surfaces (cycle-4 P1-1):** (1) `ci.yml` `welcome-e2e` PR job (`python -m pytest tests/e2e/ -q -p no:cacheprovider` on every `pull_request` — collects the whole dir; auto-skips), (2) `post-merge-validation.yml` (`python -m pytest tests/ -q --timeout=45` on merge — collects the new module too; auto-skips), (3) the deploy-pages.yml post-deploy job — the ONLY surface that sets `RUN_LEGAL_E2E=1`. **Harness guard (cycle-4 P1-1):** BELOW the opt-in short-circuit — at module import, if `BASE_URL` or `TORTISE_HOST` is `https://` and `ALLOW_PROD != 1`, `pytest.skip("no production assertions pre-merge — set ALLOW_PROD=1 to test production")` — preserves the no-production-assertions-pre-merge invariant by construction (local runs pass `http://127.0.0.1`/`localhost` and never trip it). `pytestmark` is demoted to belt-and-suspenders ONLY (repeating the same conditions is harmless, but the runtime statements above are authoritative). **Tortoise-host check contract (cycle-4 P2-1; gated set EXTENDED to #4's tortoise-host half at cycle-5 P1-1):** the tortoise.* tests (#4 tortoise-host footer half, #5 cross-host, #6 middleware regression, #8 crawl) carry `pytestmark = pytest.mark.skipif(os.environ.get("TORTISE_HOST_CHECK") != "1", reason="DNS propagation pending — verify per #657/#540")` — check #4 is SPLIT so `BASE_URL/product.html` footer presence is asserted UNCONDITIONALLY while the `TORTISE_HOST/` footer assertion runs only when `TORTISE_HOST_CHECK == "1"` — the CI job sets `TORTISE_HOST_CHECK: '1'` ONLY when its DNS preflight is green; a skipped tortoise.* test/half is GREEN-WITH-ANNOTATION (skip/xfail = green-with-annotation) — the stale-DNS branch has NO red path by design (the premiselabs.co half is the green signal) — there is NO "expected-red" state for DNS staleness.
- Run (explicit FULL env — the bare invocation shown previously would silently skip everything): `RUN_LEGAL_E2E=1 BASE_URL=http://127.0.0.1:8788 TORTISE_HOST=http://127.0.0.1:8788 python -m pytest tests/e2e/test_legal_pages.py -v`; full suite (regression; legal auto-skips): `python -m pytest tests/e2e/`

**Step 0: DNS pre-flight gate — applies to PRODUCTION-host checks only (P2-6); VERIFICATION step as of 2026-08-08 (CNAME FIXED by the operator):** local-preview `tortoise.*` checks (Host-spoofed, Step 2) run REGARDLESS of this gate; only production `tortoise.*` checks (T7 #4 tortoise-host footer half / #5 / #6 / #8 in prod mode + T8) are gated. **The gate is now a propagation VERIFICATION, not a blocking unknown** — the CNAME fix was applied 2026-08-08; this step confirms propagation before production `tortoise.*` checks run (HARD — do not proceed to production `tortoise.*` checks if it fails). **CI relaxation (cycle-4 P2-1, explicit):** the CI post-deploy job CANNOT hard-block the way this manual gate does — it runs post-merge on a push trigger and cannot hold the release; CI instead gates the tortoise.* tests via the `TORTISE_HOST_CHECK` env contract (skip = green-with-annotation per the T7 Env block). The HARD block binds the MANUAL workflow (this Step 0 + T8 Step 5); the skip-based gate binds CI.
1. `dig +short tortoise.premiselabs.co` — record the CNAME target (**FIXED 2026-08-08 → this project's `premise-labs.pages.dev` = correct/expected; legacy `tortoise-landing-v2.pages.dev` = stale — regression if seen**).
2. `curl -sI https://tortoise.premiselabs.co/` — assert it serves product.html from THIS project (check for the pinned product-page marker — h1 `Your agents remember <span>why</span>, not just what.`; a legacy landing/welcome-v1 page means propagation incomplete or regression).
3. If stale/regressed (unexpected — the operator fix of 2026-08-08 is the expected state): remediation = manual Cloudflare DNS edit of the `tortoise.premiselabs.co` CNAME → `premise-labs.pages.dev` (the deploy token cannot edit DNS — auth error; the deploy-pages.yml DNS sync runs `continue-on-error: true`, do NOT rely on it as the fix path). Post the finding + remediation note in the #657 thread. Do NOT run T7 production `tortoise.*` checks or T8 production checks until the gate is green.
4. **Canonical host = `tortoise.premiselabs.co` (per middleware design); `premiselabs.co` secondary.** E2E baseline host = **premiselabs.co** (always-green signal — production `tortoise.*` checks run only when this gate passes).
5. **DNS-propagation procedure (P2-6):** poll `dig +short tortoise.premiselabs.co` + root-marker curl every ~30s for up to 10 min (300s TTL + margin); assert CNAME target == `premise-labs.pages.dev` AND the product-page marker is served; re-run this gate FRESH at T8 start (DNS state may change between T7 and T8).

**Step 1: Write `tests/e2e/test_legal_pages.py`** covering scope §5.7 checks 1–9 as pytest tests (each a `def test_*` using the `page` fixture; a `_clean(text)` helper lowercases and strips HTML tags/entities BEFORE matching — the guard regexes are lowercase, so normalize BOTH sides):
1. **privacy 200 + negation-safe block:** GET `/privacy` → 200; normalized canonical includes: "we do not use your content or usage data to train AI models" AND guard `not re.search(r"may train|we train|to train our (ai )?models|train(ing)? (on|using) (your|user|customer)", cleaned)`; no-sale co-occurrence: "we do not sell your personal information" AND "we share limited data with advertising/analytics providers"; minimal-PII pair: "do not intentionally collect" + "sensitive personal information"; keywords: "meta", "processor", "one month", "sharing"; **anti-phrase (P2-4):** `assert "do not sell or share" not in cleaned` — the CCPA link label does not ship this release, so the banned phrase must be ABSENT.
2. **tos 200 + negation-safe block:** GET `/tos` → 200; contains "Business Source License", "Delaware", "liability" + the SAME negation-safe block + "DPA" + **eligibility (P2-1):** the pinned canonical sentence `"you must be at least 18 years old"` present VERBATIM (normalized via `_clean`) AND visible `"18+"` — NOT bare "18"/"age" (vacuous: "age" is a substring of "usage"); **anti-phrase (P2-4):** `assert "do not sell or share" not in cleaned`; **carve-out co-occurrence (cycle-5 P2-5):** `assert "willful misconduct" in cleaned` and `assert "not capped" in cleaned` — the CSA §8 carve-outs (statutory/GDPR liability, fraud, willful misconduct NOT capped) must be stated in the ToS body; **dollar-figure guard (cycle-4 P1-4, pinned `== 1` at cycle-5 P2-2):** `assert len(re.findall(r"\$[0-9]", cleaned)) == 1` — EXACTLY ONE allowed occurrence (never 0, never more) is the "$5M" AUG threshold in the license-boundary section (T1 Step 3 exemption); no other dollar figures in the ToS body (fee clause is general framing — G-gate ⑥); **fee-clause keywords (G-gate ⑥ LOCKED 2026-08-08):** the canonical fees sentence present — normalized `"we may charge fees for the service based on a combination of subscription, usage-based (metered), and/or per-seat pricing, as defined by the pricing tiers published on the pricing page"`; **"30 days"** AND **"renewal"** co-occur (price-change notice → next-renewal mechanics); **"non-refundable"** present (fees non-refundable except as required by law); **"Pricing Page" hyperlink assertion:** extract anchors from `page.content()` and assert one has link text including "Pricing Page" with a resolvable `href` that is not `#` (target verified live at T8; the dollar guard above already rules out $ figures in the fees section);
3. **legal pages:** `/license` AND `/dpa` → 200 (both **REQUIRED** — G-gate ⑨/③ LOCKED 2026-08-08; no skip-if-not-shipped marker). The dollar-figure guard (P1-4) does NOT apply to `/license` — its "$5M" AUG explainer is unguarded by design (the guard targets `tos.html` only).
4. **Footer link presence (cycle-4 P2-8 paths pinned; cycle-5 P1-1 SPLIT):** (a) UNCONDITIONAL half — product footer asserted on `BASE_URL/product.html`, plus `welcome.html`, `signup.html`, `signin.html`, `self-hosted.html`; **link-set assertion (G-gate ⑨ LOCKED): footer links to BOTH `/dpa` AND `/license` are present on the product/welcome footers — unconditional, no IFF (both pages ship)**; (b) TORTISE-HOST half — product footer ALSO asserted on `TORTISE_HOST/` (the tortoise host serves product.html at root via the middleware rewrite) but ONLY when `TORTISE_HOST_CHECK == "1"` (same skipif as #5/#6/#8 — stale-DNS runs skip this half, never red). (On premiselabs.co, `/` serves `index.html` — no footer there, by design.)
5. **Cross-host:** `premiselabs.co/privacy` → 200 (same project; middleware only rewrites `/`).
6. **Middleware regression (pinned root markers, P2-2):** `tortoise.*` root serves the product.html marker — normalized `your agents remember why, not just what` (file h1: `Your agents remember <span>why</span>, not just what.`); `premiselabs.co` root serves the index.html marker — normalized `the primary function of memory is not recall, but learning` (file h1: `The primary function of memory is not <span>recall</span>, but <span>learning</span>.`); fallback: normalize-and-compare served root vs the local files.
7. **Same-viewport acceptance:** `/signup` shows the acceptance statement with working links, visible in the same viewport as BOTH `.providers` OAuth buttons and `#btn-submit` — bounding-box overlap asserted (not mere presence) at BOTH 1280×720 AND 375×667 viewports (parametrize); **pinned text assertion (cycle-4 P2-7a):** `_clean(await page.inner_text(".legal-accept"))` must contain "by creating an account, you agree to the" AND the link labels "terms of service" + "privacy policy" (the T5 Step 4 pinned statement, normalized); working links asserted by resolving the `.legal-accept` hrefs to `/tos` and `/privacy`; DOM parentage (P1-2): `.legal-accept` is a SIBLING of `.providers` (and `.divider`), PRECEDES `<form id="email-form">` in document order, and is NOT a descendant of the form — never assert sibling-of-`#email-form`; mocked-Supabase OAuth click test: intercept `auth/v1/*` per the `test_welcome_page.py` route-interception pattern, click "Continue with GitHub", assert the OAuth path still fires (request to `auth/v1/*` with provider=github, no acceptance-block interference). **Mocked email-path signup test (P1-3 — the ONLY un-mocked modified surface):** mirror the OAuth test — `page.route("**/auth/v1/signup*", ...)` (fulfill a session-less response body per the `_mock_supabase_success` route-interception pattern), fill `#email` + `#password` (the `#email-form` at signup.html line 278 submits via `signUpWithEmail(event)` → `supabaseClient.auth.signUp` line 407), click `#btn-submit`, then assert the `auth/v1/signup` request FIRED with the expected payload AND the acceptance block is present (`.legal-accept` visible) + zero console errors (`page.on("console")`); **MANDATORY inbox-state assertion (cycle-4 P2-7b — NOT optional):** the post-submit check-your-inbox state must render — `#confirmation-required` (signup.html ~line 291) VISIBLE and `#confirm-email` (signup.html) contains the TYPED email address — the email path is the ONLY un-mocked modified surface, so its success state is asserted, not assumed. ~20 lines, consistent with the `_mock_supabase_success` pattern.
8. **0 broken links (cycle-4 P2-8 crawl items pinned):** crawl the ENUMERATED page set — `product` = BOTH `TORTISE_HOST/` (middleware rewrite) AND `BASE_URL/product.html` (two distinct URLs; `BASE_URL/` is NOT a product crawl item — the premiselabs.co root serves index.html by design), `welcome`, `signup`, `signin`, `self-hosted.html`, `docs.html`, `privacy`, `tos`, `dpa`, `license` (all REQUIRED — G-gate ③/⑨ LOCKED; no IFF) — per-host base URL (BASE_URL + TORTISE_HOST); follow redirects and assert final status 200; INCLUDE external links (e.g., `github.com/daniel-ospina/tortoise/LICENSE` 301→blob is OK) with timeout tolerance (15s per request); collect all hrefs from each page.
9. **Mobile render @375px:** screenshots of `/privacy`, `/tos`, and footers on `product.html`/`welcome.html`/`signup.html`/`docs.html` — no horizontal scroll (`document.documentElement.scrollWidth <= 375`), readable type; **minimum-content assertion (P1-4):** per legal page assert `page.evaluate("document.body.innerText.length")` ≥ a floor (e.g. privacy ≥ 8000 chars, tos ≥ 12000 — the 15/16 sections must ACTUALLY render, not just the page shell) AND `page.evaluate("document.querySelectorAll('h1,h2').length")` ≥ N per page (privacy ≥ 16, tos ≥ 15 + title heading) — a page passing scrollWidth alone (empty shell, zero horizontal scroll) must FAIL; **this evidence resolves the mono-vs-serif decision (§3.2)**.
10. **Served-content assertions on BOTH legal pages (P1-1 addition to §5.7):** (a) revision-history block present — heading matches `/revision|document history/i` AND ≥1 changelog entry (a `li`/list item or dated line after the heading); (b) EXACT consent-banner sentence present on `privacy` — normalized `"consent will be obtained via a banner when these tools are activated"` (exact match, not substring-of-substring); (c) conditional-tense drafting rule on `privacy` — **BOTH-AND, no vacuous pass (P1-4):** (c1) REPO-STATE assertion — the test scans the LOCAL `website/` tree under test for instrumentation/consent-banner markers (script tags for posthog/meta-pixel/gtag, or the Klaro/consent-banner markup from #658) and asserts the page wording matches ACTUAL state: the explicit sentence "no analytics tools are currently deployed" (canonical wording locked at T2 Step 2 — P2-2) must be present IFF no markers are found — the IFF is guaranteed by construction because T2 Step 2 pins the exact sentence T4 renders verbatim; per-tool conditional/consent framing is required IFF markers exist; (c2) PER-TOOL assertion — EACH tool name matches via WORD-BOUNDARY regexes (`\bmeta pixel\b`, `\bposthog\b`, `google analytics` — NOT bare "meta", which false-matches "metadata") and appears within 120 chars of a consent/conditional word (`consent|activated|will|may|when|if`); (d) placeholder-leak checks — SPLIT into two rules (P2-1; the old combined regex `\[[A-Z][^\[\]]{4,}\]` false-matches legitimate citations like `[GDPR]`, `[BUSL-1.1]`, `[CSA §3]`, `[Art. 12(3)]`, `[EU/EEA]`): (d1) BROAD template-placeholder check — `\{\{|\$\{|<INSERT|TODO` must match NOTHING on both pages; (d2) D4-SHAPE placeholder check — **entity RESOLVED at G-gate ② (2026-08-08): individual d/b/a, placeholder REPLACED — assert the D4-shape regex `\[[A-Z][A-Z ,]{8,}\]` (uppercase multi-word, comma-separated) matches NOTHING on both pages (count 0, unconditional)** — the former `[ENTITY TYPE, JURISDICTION OF FORMATION TO BE CONFIRMED]` placeholder no longer appears; the "exactly once per page" branch is DEAD (placeholder gone — no count-conditional remains); **allowlist note (cycle-5 P2-3):** the free-tier usage-limit placeholder is pinned to lowercase prose `[free-tier usage limits to be published]` (G-gate ⑥ LOCKED 2026-08-08 — fee clause approved, placeholder stands as pinned until limits publish) so it never matches this uppercase-only regex — no allowlist entry needed; if the free-tier cap resolves later, the replacement wording is also lowercase prose; (e) **present-tense anti-pattern (promoted from the S5 grep guard, P1-4; regex widened + expected-match-subset semantics pinned at cycle-5 P1-3):** over the normalized served text, the SET of `we (use|collect|share|sell|process|retain|store|transfer)\b` word-boundary matches must equal EXACTLY the pinned expected-match subset — defined as **the SUBSET of canonical sentences that actually MATCH the guard regex** (NOT the full canonical set — most canonical sentences contain "we do" and do NOT match; e.g., "we share limited data with advertising/analytics providers as disclosed" IS an expected match, present-tense by design); ANY other match (a new present-tense claim, e.g., "we use posthog" while uninstrumented) fails the test. **The AUTHORITATIVE pinned set = the canonical sentences locked at T1/T2 Step 2 (P2-8)**, and the test computes the expected-match subset by filtering that set through the guard regex — T4 Step 5's build-time grep uses the SAME set and regex, so T4 and T7 can never disagree on what is expected. The pinned set lives in the test module and changes only when the approved draft legitimately changes. **Classification rule (cycle-4 P1-5, cycle-5 P1-3 + P2-6, pinned — stated IDENTICALLY in T2 Step 2, T4 Step 5, and T7 #10(e)):** (1) REQUIRED live, true disclosures are EXPECTED content and are drafted truthfully in present tense, but phrased PASSIVELY/IMPERSONALLY so they never trip the guard: the processors section (Supabase, Cloudflare, GitHub, Google — operating today; `signup.html` makes real `supabaseClient` calls) and the collected-data categories (email/login, usage data). (2) Only NEW present-tense claims OUTSIDE the approved pinned set fail the guard. (3) The chosen mechanism is the RECOMMENDED passive-voice option (pinned; no allowlist drift): processors, data-categories, retention, and transfer statements use passive/impersonal phrasing — "data is processed by Supabase for authentication", "account data includes your email address", "data is retained only for as long as needed" — so they NEVER match the guard regex `we (use|collect|share|sell|process|retain|store|transfer)\b`; no allowlist additions needed.
(f) **HTML structural assertions (cycle-4 P1-2 — FULL-document tag balance):** per legal page — run the SAME stack-tracking `HTMLParser` subclass as the T4 Step 5 gate over `page.content()` in pytest: EVERY tag (not just `<section>`/`<h2>`) must balance — closing tag matches the stack top, stack EMPTY at EOF, no premature `</html>`/`</body>`; **void elements via the SAME explicit `VOID = {"meta","link","br","img","input","hr","source"}` set as the T4 Step 5 gate (cycle-5 P1-2 — `HTMLParser` has NO void-elements attribute; `handle_startendtag` fires ONLY for self-closed `<tag/>`):** in `handle_starttag`, `if tag in VOID: return` (never push); in `handle_startendtag`, never push (slash-less `<meta>`/`<link>` fire `handle_starttag` and are covered by the VOID check — product.html's head has 0 self-closed void tags); additionally every `<section>`/`<h2>` opening tag has a matching closing tag; the body text of each of the 15 (tos) / 16 (privacy) sections appears as TEXT NODES — the SECTION TITLE LIST is DERIVED from the G-gate-approved drafts at T4 (cycle-4 P2-4): the test imports `docs/drafts/2026-08-08-657-tos-section-titles.txt` / `docs/drafts/2026-08-08-657-privacy-section-titles.txt` (committed alongside the drafts) as the assertion source — NEVER invented at test time; each title's first sentence asserted via `document.body.innerText` (an attribute-value match is NOT sufficient, which is why `_clean` on served text alone cannot prove structure); `</body></html>` present in `page.content()`; `page.evaluate("document.querySelectorAll('h1,h2').length") >= N` per page (same N as #9).
(g) **Draft↔render fidelity (cycle-5 P1-5 — re-run of the T4 Step 5 check):** normalize the approved draft vs the rendered page body text (strip markdown/tags, lowercase, collapse whitespace) and assert per-section containment — every draft sentence ≥ 40 chars (after normalization) appears in the rendered text; sub-40-char fragments (e.g., the pinned `[free-tier usage limits to be published]` placeholder) are exempt (may be resolved/replaced at the G-gate).

**Step 2: Run the suite against the local preview (pre-merge; P1-3)** — **compatibility-date capture (cycle-4 P2-3):** FIRST run `cd website && npx wrangler@4 pages dev . --port 8788` ONCE WITHOUT the flag and RECORD the effective default compatibility date wrangler reports (there is NO `wrangler.toml` and no date set anywhere — production runs wrangler@4's default, so the reported default IS production's effective value); THEN pin the local run to that recorded value (`--compatibility-date=<recorded>`) so local/prod runtime semantics don't diverge, and note the recorded value in the task output. (`2024-11-01` was an unverified hardcode — dropped; any specific date is "example — verify".), then `RUN_LEGAL_E2E=1 BASE_URL=http://127.0.0.1:8788 TORTISE_HOST=http://127.0.0.1:8788 python -m pytest tests/e2e/test_legal_pages.py -v` (BOTH env vars set explicitly per P1-1; the harness guard passes because both are `http://127.0.0.1`). **Cross-host mechanism (P2-7) with emulation-gap discrimination (P1-2):** browsers forbid setting the `Host` header on `page.goto` (forbidden header), and `wrangler pages dev` middleware/`context.env.ASSETS.fetch` emulation has DOCUMENTED gaps (workers-sdk#165) — so the fallback path must DISCRIMINATE an emulation gap from a real regression, in this order: (1) FIRST `git diff --quiet HEAD -- website/functions/_middleware.ts` — if the middleware is UNCHANGED (it is — verified-only surface S3), proceed; (2) probe emulation with a THROWAWAY canary function in the preview (temp `functions/_canary.ts` exporting a handler that calls `context.env.ASSETS.fetch` against a known path and returns its status **AND echoes the received `Host` header in the response body — cycle-5 P2-4**) — canary returning 200 proves the ASSETS binding works under `wrangler pages dev`; **assert the echoed `Host` equals the spoofed value (`tortoise.premiselabs.co` / `premiselabs.co`) BEFORE trusting ANY Host-spoofed page-level run (#5/#6/#8 + the #4 tortoise-host footer half)** — a canary that 200s but echoes the wrong Host means the `Host` header is NOT actually reaching the worker (the middleware `/`-rewrite branch is not exercised), so Host-spoofed page-level assertions are untrustworthy; (3) pre-smoke assertion that Functions actually EXECUTE in the preview: assert a known passthrough path serves AND the canary/marker is observable (curl the canary route; DELETE the canary after the probe). Only then smoke-test the middleware with curl `-H "Host: tortoise.premiselabs.co" http://127.0.0.1:8788/` (assert the product.html root marker) and `-H "Host: premiselabs.co" http://127.0.0.1:8788/` (assert the index.html root marker). If the local middleware emulation is reproducible (curl Host-spoof works AND canary 200s), run cross-host checks #5/#6/#8 against the same preview using Playwright's Chromium-only `context.set_extra_http_headers({"Host": ...})` per-host. **Deferral is DISCRIMINATED, not conflated (P1-2):** ONLY if the canary/emulation probe actually FAILS (not merely "not verified") may cross-host checks be deferred — and ONLY after confirming the middleware file is unchanged via `git diff --quiet` (a CHANGED middleware = real regression risk and must NOT be deferred to production). When checks are deferred, the task output must LIST EXACTLY which checks were deferred (e.g. "#4 tortoise-host footer half, #5 cross-host privacy 200, #6 middleware root markers, #8 tortoise.* crawl — deferred to T8 production-only; cause: wrangler pages dev ASSETS emulation gap workers-sdk#165, canary status 500 or Host-echo mismatch") — not merely "state that decision". NO production URLs pre-merge.

**Step 3: Regression** — `python -m pytest tests/e2e/` must still pass, incl. the live-signup test (`test_live_signup_redirects_to_welcome_with_key`, skipped without SUPABASE creds) — the acceptance block sits directly above `#email-form` and must not disturb it. (Note: in this run the legal suite auto-skips — `RUN_LEGAL_E2E` unset — so this verifies welcome-page regression only, per P1-1.)

**Expected output:** Passing pytest run (checks 1–10 incl. served-content assertions, both viewports for #7, per-host crawl) + green regression suite + mobile screenshots; effective-date vs served content deferred to T8.

---

### Task T8: Post-merge deploy (CI) + production verification + commit/PR

**Intent:** Deploy via PR merge → CI (`deploy-pages.yml`), then run the same legal-pages assertions against production post-deploy. The manual `wrangler pages deploy` is REMOVED as the primary deploy path (P1-3) — CI owns the single deploy. Note the CI double-deploy concern explicitly: deploy-pages.yml triggers on push to main (`website/**`) AND `workflow_dispatch`; a manual deploy would race the CI deploy (concurrency group `deploy-pages`, `cancel-in-progress: false`), so production deploys must ONLY happen through CI.
**Acceptance:** PR merged → CI deploys → post-deploy production assertions green (curl + pytest against prod with baseURL env); effective date == deploy date asserted against SERVED content; `.wranglerignore` scoping verified via local file-set simulation (Step 3) + post-deploy negative curl checks (Step 5, P1-3); commit-workflow passes (typecheck not applicable — static content; tests = T7 suite; code-review gate).
**Files:**
- Create: `website/.wranglerignore` (content: `apps/` — excludes the separately-deployed dashboard project from the `premise-labs` upload; verify `deploy.sh`, `dist/`, `node_modules/` under `apps/` no longer upload)
- Modify: `.github/workflows/deploy-pages.yml` — add a post-deploy job (or extend the deploy job) that runs the legal-pages pytest assertions against production after the deploy step. **Mirror the `welcome-e2e` job conventions from `ci.yml` VERBATIM (P2-9):** `actions/checkout@v4`; `actions/setup-python@v5` with `python-version: '3.12'`; `run: pip install -e '.[test]'`; `run: python -m playwright install --with-deps chromium`; `run: python -m pytest tests/e2e/test_legal_pages.py -q -p no:cacheprovider` with env `RUN_LEGAL_E2E: '1'`, `ALLOW_PROD: '1'`, `BASE_URL: https://premiselabs.co`, `TORTISE_HOST: https://tortoise.premiselabs.co`. Add `timeout-minutes: 20`. **Deploy-completion poll BEFORE pytest (cycle-4 P2-9):** after the deploy step, poll with retry/backoff (every ~30s, up to ~5–10 min) until `curl -s -o /dev/null -w '%{http_code}' https://premiselabs.co/privacy` → 200 — mirrors the manual T8 Step 5 poll; only then run the DNS preflight + pytest (a fresh deploy can take a minute past the deploy step). **DNS preflight in the CI job (P2-6):** BEFORE the pytest run, run the SAME pre-flight as the manual gate — `dig +short tortoise.premiselabs.co` (assert CNAME target == `premise-labs.pages.dev`) + root-marker curl of `https://tortoise.premiselabs.co/` (assert the product-page marker); if GREEN → set `TORTISE_HOST_CHECK: '1'` on the pytest step so the tortoise.* tests (#4 tortoise-host footer half, #5/#6/#8) RUN; if STALE → leave `TORTISE_HOST_CHECK` UNSET so those tests/halves SKIP via their module skipif (`"DNS propagation pending — verify per #657/#540"`) — the #4 `BASE_URL/product.html` half still runs and is the green signal (cycle-5 P1-1: the stale-DNS branch has NO red path by design). A skipped tortoise.* test is GREEN-WITH-ANNOTATION — NEVER "expected-red" (cycle-4 P2-1: there is no red state for DNS staleness by design; the premiselabs.co checks are the green signal) — do NOT block the release on stale DNS, but DO leave the job summary unambiguously annotated (which tests skipped and why). **Failure response (P2-9):** on red (non-DNS causes), re-run once; if still red → fix + comment the failure on the #657 thread; reopen the issue if closed; do NOT merge-forward silently. **Date comparison is WARN-ONLY in this automated job (cycle-4 P2-2 — deploy-pages.yml is push-triggered, so there is NO PR-description access):** the canonical date is recorded in the MERGE COMMIT MESSAGE (T8 Step 4 instructs) AND in the pages' changelog entry; the CI job compares the SERVED effective date (`Effective date: YYYY-MM-DD` extracted from the fetched /privacy + /tos bodies) against the DEPLOY COMMIT DATE (`git log -1 --format=%cs` at the checked-out HEAD) — warn-only (annotate, never fail the job); when either value is UNAVAILABLE (unparseable served date, no changelog entry, git metadata missing), WARN + SKIP the comparison (never error). The hard effective-date==deploy-date assert is the manual T8 Step 5. **Idempotence note (P2-9):** this job fires on EVERY `website/**` push to main (including later unrelated deploys); a green run means the legal pages still pass — it does NOT replace the manual T8 Step 5 verification for this release.
- Modify: `website/README.md` (one line, P2-10: deploys are CI-owned — push to main with `website/**` triggers deploy-pages.yml; the README `wrangler pages deploy` command is for local troubleshooting only)

**Step 1: Create `.wranglerignore`** in `website/`:
```
# Exclude the separately-deployed dashboard project (deployed to tortoise-dashboard via apps/dashboard/deploy.sh)
apps/
```

**Step 2: Stage onboarding prompt BEFORE any deploy (mirror CI; P2-6)** — `cp tortoise/onboarding/AGENT_ONBOARDING.md website/onboarding-prompt.md && test -s website/onboarding-prompt.md` (same step deploy-pages.yml runs; guarantees the local/CI tree is deploy-consistent and `premiselabs.co/onboarding-prompt.md` stays markdown).

**Step 3: Verify upload scope deterministically (P1-3)** — `wrangler@4 pages deploy` (and `pages upload`) have NO `--dry-run`/`--list-files` flags (verified via `--help`), so verify scope TWO ways:
(a) LOCAL file-set simulation (P2-5 — `git ls-files` lists only TRACKED files; the new pages are UNTRACKED until Step 4 commits, so DON'T gate presence on bare `git ls-files`): verify presence FIRST with `test -f website/privacy.html && test -f website/tos.html && test -f website/functions/_middleware.ts && test -f website/README.md && test -f website/onboarding-prompt.md` (onboarding-prompt.md is created UNTRACKED BY DESIGN in Step 2's `cp`; the Step 2 `test -s` already guarantees it; **README.md is in the expected set because it ships with EVERY `wrangler pages deploy .` upload — verified at site root**) — then run a NORMALIZED RELATIVE-PATH SET COMPARISON (cycle-4 P1-3; `.wranglerignore` exclusion pinned at cycle-5 P2-1): build the found set from `find website -type f -not -path 'website/apps/*' ! -name '.wranglerignore'` minus `.wranglerignore` patterns, normalized to relative paths (`website/` prefix stripped) — `.wranglerignore` itself is EXCLUDED from the found set via `! -name '.wranglerignore'` (it is a build-config artifact, not a deployable page; excluding it keeps set equality unambiguous regardless of whether wrangler uploads the ignore file itself); expected set = site pages (index.html, product.html, welcome.html, signin.html, signup.html, self-hosted.html, docs.html) + legal pages (privacy.html, tos.html, dpa.html, license.html — **all REQUIRED, G-gate ③/⑨ LOCKED**) + `functions/_middleware.ts` (**a FILE, not the dir — the upload ships the whole `functions/` tree, but the gate asserts the concrete middleware file**) + README.md + onboarding-prompt.md; assert `found ⊆ expected AND expected ⊆ found` (set equality BOTH directions — every found file in the expected list AND every expected file found); **junk-file guard (P2-5):** `find website -type f -not -path 'website/apps/*' \( -name '.DS_Store' -o -name '*~' -o -name '*.tmp' -o -name '*.swp' \)` must return NOTHING. If the git-based diff is preferred instead, STAGE the new files first (`git add website/privacy.html website/tos.html ...`) or use `git ls-files --others --exclude-standard` to include untracked files — do NOT rely on bare `git ls-files` before Step 4 commits. Confirm `.wranglerignore` (`apps/`) is what excludes the dashboard project from the upload.
(b) POST-DEPLOY negative check (executed in Step 5): `curl -s -o /dev/null -w '%{http_code}' https://premiselabs.co/apps/dashboard/index.html` → expect 404 (no `apps/` may ship under this project), plus positive checks for `/_middleware.ts` behavior (root rewrite per host) and `/privacy`, `/tos` → 200.

**Step 4: Commit + PR via commit-workflow** — `@commit-workflow` (pre-flight: T7 suite green against local preview + regression suite green; no typecheck needed — zero TS/JS changed; markup-only). **Named pre-merge gates — run immediately BEFORE the commit-workflow pre-flight; owner = implementer; pass/fail record posted to the #657 thread (P1-4/P2-8):**
- **G-528/#658 re-check:** re-run the #528 re-check — PostHog residency line (T2 ⑭), Meta-sharing/joint-controller disclosure (T2 ⑤), tool-name disclosures — plus the #658 consent-banner markers scan (T7 #10(c1)); if #528/#658 merged DURING T7/T8, update wording before merge; record pass/fail on #657. **Post-release ownership (cycle-4 P2-5, pinned):** when #528 or #658 merges AFTER this release, the privacy-page re-draft (remove the repo-state sentence, add per-tool conditional framing) is a REQUIRED COMPANION CHANGE to THAT issue's PR — verified by the T7 #10(c1) assertion; the post-deploy CI job turning RED on such a merge is the INTENDED fail-loud signal (served page stale vs new instrumentation), NOT a regression — record this ownership on #528/#658 so their PRs carry the companion change.
- **G-PricingRef (G-gate ⑥, NEW 2026-08-08):** assert the pricing-reference href chosen at T4 Step 6 resolves in production (dedicated /pricing route OR product-page pricing-section anchor) AND the free-tier numbers conflict ("5,000 Points free" vs "1,000 write ops/mo" — scope §6.2 follow-up 4) is reconciled (here or under #432) BEFORE the ToS hyperlinks the pricing page; the served `/tos` fees sentence must match the G-gate-approved canonical wording (no dollar figures); record pass/fail on #657.
- **G-EffectiveDate:** assert the G-gate-approved effective date (`Effective date: YYYY-MM-DD` per T4) == expected deploy date. **Clock/TZ pinning (P2-10; recording mechanism per cycle-4 P2-2):** "deploy date" is DEFINED as the author's local calendar date at merge — run `date +%F` at T8 Step 4 and RECORD that value in the MERGE COMMIT MESSAGE (the CI job is push-triggered and has NO PR-description access; it reads the SERVED date vs the DEPLOY COMMIT date via `git log -1 --format=%cs`) AND in the pages' changelog entry (a dated entry in the revision-history block carries the same value on the served pages); BOTH the CI warn-only comparison AND the T8 Step 5 hard assert compare against these recorded values (never a wall-clock re-read that can drift across TZ/date boundaries mid-deploy). If the deploy slipped, BUMP the effective date + append a changelog entry BEFORE merge (never after).
PR includes this plan + scope + research briefs + the deploy-pages.yml post-deploy job + the license-drift note (stale AGPL local `main` LICENSE + pyproject MIT — see T1 Step 3, P2-3) + the `website/README.md` one-liner (deploys are CI-owned — push to main with `website/**`; the README `wrangler pages deploy` command is for local troubleshooting only, P2-10). On merge, CI deploys (deploy-pages.yml: stage onboarding prompt → best-effort DNS sync → `wrangler@4 pages deploy`).

**Step 5: Post-deploy production verification (issue I1 + P1-2 additions)** —
0. **Deploy-completion poll (P2-11):** after merge → CI deploy, poll with retry/backoff (every ~30s, up to 10 min) until `curl -s -o /dev/null -w '%{http_code}' https://premiselabs.co/privacy` → 200 — only then run the suite below (the CI deploy can take a minute past merge).
1. Run the T7 pytest suite against PRODUCTION: `RUN_LEGAL_E2E=1 ALLOW_PROD=1 BASE_URL=https://premiselabs.co TORTISE_HOST=https://tortoise.premiselabs.co python -m pytest tests/e2e/test_legal_pages.py -v` (production DNS gate from T7 Step 0 must be green first — gate re-run FRESH at T8 start per P2-6).
2. curl checks:
- `curl -s -o /dev/null -w '%{http_code}' https://premiselabs.co/privacy` → 200; same for `/tos`, `/license`, `/dpa` on both hosts (all REQUIRED — G-gate ③/⑨ LOCKED; tortoise.* conditional on gate).
- **Root markers on BOTH hosts (pinned strings, P2-2):** `https://tortoise.premiselabs.co/` contains the product.html marker — h1 `Your agents remember <span>why</span>, not just what.` (normalized `your agents remember why, not just what`); `https://premiselabs.co/` contains the index.html marker — h1 `The primary function of memory is not <span>recall</span>, but <span>learning</span>.` (normalized `the primary function of memory is not recall, but learning`); fallback: normalize-and-compare served root vs the local files — catches middleware/upload regression.
- **Upload-scope negative check (P1-3):** `curl -s -o /dev/null -w '%{http_code}' https://premiselabs.co/apps/dashboard/index.html` → expect 404 (no `apps/` may ship under this project); positive checks: `/_middleware.ts` behavior (root rewrite per host) and `/privacy`, `/tos` → 200.
- **Negation-safe content assertions ALSO against `premiselabs.co/privacy` (P1-2)** — `curl -s https://premiselabs.co/privacy` piped through the same canonical-sentence + anti-pattern checks (or rely on the pytest cross-host test #5).
- **Cache headers (P1-2):** `curl -sI -H 'Cache-Control: no-cache' https://premiselabs.co/privacy` — assert `etag` AND `cache-control` headers present (no-cache forces a fresh fetch past Cloudflare edge cache).
- **Effective date == deploy date against SERVED content — HARD assert (manual step, not warn-only; P2-8/P2-10):** fetch served `/privacy` + `/tos` bodies, extract the VISIBLE `Effective date: YYYY-MM-DD` string, compare to the `date +%F` value recorded in the MERGE COMMIT MESSAGE at T8 Step 4 + the served changelog entry (the author's local calendar date at merge — the single canonical deploy-date value; cycle-4 P2-2); on mismatch, bump effective date + append changelog entry per scope P4-1 and redeploy via CI (the automated CI job's date comparison is warn-only — this manual check is authoritative).
- **Onboarding + welcome (P2-6):** `curl -s -o /dev/null -w '%{http_code} %{content_type}' https://premiselabs.co/onboarding-prompt.md` → 200 + text/markdown; `https://premiselabs.co/welcome` → 200.
- Footer link spot-check on `https://premiselabs.co` and `https://tortoise.premiselabs.co`.

**Step 6: Rollback / failure classification (cycle-4 P2-6)** — classify ANY post-deploy red BEFORE acting:
- **REVERT-WORTHY (revert the merge):** negation-rule violation on SERVED content (a canonical no-training/no-sale/minimal-PII sentence missing or an anti-phrase present), 404 on any legal URL (/privacy, /tos, /dpa, /license — all REQUIRED), effective-date mismatch on SERVED content. Reverting the merge (git revert / revert-PR) RESTORES the prior served content automatically via the existing push-triggered deploy-pages.yml CI deploy — no manual deploy needed.
- **FIX-FORWARD (do NOT revert):** environmental/transient failures — DNS staleness (covered by the TORTISE_HOST_CHECK skip), cache edge, transient 5xx, poll timeouts, CI infra. Fix forward: re-run the failed check, document on #657.
- While a fix is in flight, record the KNOWN-ISSUE state on #657 (what's broken, revert vs fix-forward classification, expected resolution) so the thread carries the operational truth.

**Expected output:** Single CI-owned deploy; post-deploy pytest + curl green on both hosts (gate-permitting); effective date matches deploy date on served content; upload scope verified via local file-set simulation + post-deploy negative checks; PR merged with review signatures.

---

## Appendix A — Verification Plan (acceptance criteria from scope §5.5, verbatim)

- [ ] `tortoise.premiselabs.co/privacy` and `/tos` return 200 and render with existing design system (I1)
- [ ] Footer links to both pages on `product.html` **and** `welcome.html` (I2 — requires creating footers, see Phase 1 finding)
- [ ] All 3 analytics tools disclosed **by name** in Privacy (T1)
- [ ] All 3 commitments (no sale / no AI training / minimal PII) stated in **BOTH** ToS and Privacy (T2) — **asserted negation-safe per §5.7 E2E #1/#2: canonical sentences + anti-pattern guards, not bare substring checks (P2-1)**
- [ ] ToS covers all 12 sections **+ eligibility/age (18+)** (T3, §5.6)
- [ ] Privacy covers all 9 areas + processors item (T4, §5.6)
- [ ] Owner (Daniel) reviewed + approved legal text (I5)
- [ ] 0 broken links in production (T7 #8 / T8 post-deploy crawl) (P2-7)
- [ ] Both pages include a "Document history / revisions" section (T4 acceptance + T7 #10(a)) (P2-7)
- [ ] Liability-cap carve-outs reviewed (CSA §8: statutory/GDPR liability, fraud, willful misconduct NOT capped)
- [ ] Signup shows the ToS/Privacy acceptance statement **visible in the same viewport as OAuth buttons + email submit** (pytest-asserted, §5.7 #7) (P3-5)
- [ ] docs.html contains **no mailto** — GitHub-issues link only (D2) (P3-5)

**Verification layers (test-routing):** E2E (pytest-playwright, scope §5.7 checks 1–9, pre-merge local preview + post-deploy CI job) · post-deploy curl (both hosts) · content domain (scope §5.6 checklists — content-verification gate) · config low (deploy path only) · UX low (mobile render + same-viewport, T7). No unit/integration/pgTAP layers apply (zero backend code changed; middleware verified-only).

## Appendix B — Rejected Alternatives (scope §5.2, summary)

- **B — Cover Page + Standard Terms incorporated by reference (Zep pattern):** silent-change + link-rot risk on a compliance document for a static host with no backend — rejected on immutability grounds.
- **C — Generator services (iubenda/Termly/TermsFeed):** recurring cost, generic text, cannot express the BSL boundary or the no-training differentiator with the precision D5 requires — rejected on fit.
- **Copying Mem0/Zep verbatim:** imports promises never intended (Mem0 trains on Free-plan content; Zep terms predate CSA v2.1 ML clause) — rejected on accuracy (research §3.5, 4 independent practitioner sources).

---

## Execution Handoff

- **Task count:** 7 (T1/T2 parallelizable — both feed T3 only; T4/T5 parallelizable after the G-gate — both depend only on T3 and touch DISJOINT file sets: T4 creates new `website/*.html` files, T5 edits existing pages (**P2-5**); T3 is the hard human stop; T5 merges former T5+T6 per P2-2 → ≤ 8 → subagent-driven execution in this session). **G-gate verifiability (cycle-5 P1-4):** the OWNER must post the approval comment on #657 (owner-authored, containing explicit approval + decision log + link-set record); the G-gate DECISION LOG (decision record) was posted by the decisions recorder on 2026-08-08 — the owner's approval comment must still be OWNER-AUTHORED and may reference/endorse that log; the executor fetches that comment and link-verifies it BEFORE dispatching any T4/T5 build work — if no owner comment exists, T3 stays OPEN and sub-agents must NOT mark it complete (no build work may begin without the recorded comment URL).
- **Next gate:** `plan-review` on this plan (mandatory for Standard tier) → then `executing-plans` task-by-task; T3 is the hard human stop.
- **Post-plan:** apply `planned` label to #657 (remove `planning` if present) after plan-review passes clean.
- **Key files:** scope `docs/scoping/2026-08-08-657-legal-scope.md` · research `docs/research/2026-08-08-657-legal-pages.md` · truth source `.worktrees/657-legal/LICENSE` (BUSL-1.1) + `docs/license-notes.md`.

---

## Plan Review Cycle Log

- **Cycle 1 (2026-08-08):** plan-review dispatched parallel reviewers; controller merged 3 P1s + P2s. Fixes applied in revision 2:
  - **P1-1** — T7 rewritten for pytest-playwright (`tests/e2e/test_legal_pages.py`, `python -m pytest tests/e2e/test_legal_pages.py -v`); checks 1–9 as pytest tests with normalization (`text.lower()` + strip tags/entities before matching), served-content + placeholder-leak assertions, same-viewport at BOTH 1280×720 and 375×667, DOM parentage (`.legal-accept` sibling, not child), mocked-Supabase OAuth click test, enumerated per-host link crawl incl. external links with redirect-follow, welcome-suite regression. TS Playwright (`e2e-legal.spec.ts`, `critical-paths` project) removed.
  - **P1-2** — DNS pre-flight gate added as T7 Step 0 (hard gate; remediation = manual Cloudflare DNS edit, NOT continue-on-error CI sync); E2E primary host = premiselabs.co, `tortoise.*` conditional; T8 post-deploy adds root markers on BOTH hosts, negation-safe assertions against `premiselabs.co/privacy`, `Cache-Control: no-cache` + etag/cache-control assertions, effective-date == deploy-date against SERVED content (moved from T7); host-mapping reality noted in Architecture + surface map S10.
  - **P1-3** — verification split: T7 runs full pytest suite against `npx wrangler@4 pages dev` local preview with explicit Host headers, NO production URLs pre-merge; T8 = merge → CI deploy (manual `wrangler pages deploy` removed as primary path) → post-deploy production assertions (pytest with baseURL env + curl); deploy-pages.yml extended with post-deploy assertion job; CI double-deploy concern documented.
  - **P2-1** — T1/T2 write `docs/drafts/2026-08-08-657-tos-draft.md` / `...-privacy-draft.md` (NOT website/tos.html); T3 = owner reviews FULL draft texts + 8-item checklist + LINK-SET DECISION RECORD (per page: `/license` vs GitHub LICENSE URL; `/dpa` vs omit); T4 renders final pages from approved drafts.
  - **P2-2** — T5/T6 merged into one "site markup edits" task (footers ×5 + signup acceptance + docs.html mailto); T1/T2 marked parallelizable (both feed T3).
  - **P2-3** — prerequisite note updated: signature comment IS present (post-hoc 2026-08-08); re-verify instruction removed.
  - **P2-4** — residual research carried: clause-level sanity check vs LangSmith/Pinecone/Supabase comparables as OPTIONAL ~30-min step in T1/T2 (rationale stated: comparables already in-repo, drafting base is audited CSA v2.1).
  - **P2-5** — surface map rows added: S9 (existing e2e suite regression), S10 (DNS/CNAME host mapping #540), S11 (CI deploy workflow deploy-pages.yml); S5 (#528) and S6 (#432) given explicit failure modes (mid-cycle merge re-check, present-tense grep guard, pricing drift guard).
  - **P2-6** — T8 deploy hygiene: stage `website/onboarding-prompt.md` (copy + `test -s`, mirror CI) BEFORE any deploy; `premiselabs.co/onboarding-prompt.md` (200 + text/markdown) + `/welcome` 200 in post-deploy checks; wrangler pinned @4 (match CI); upload scope verified via `wrangler pages deploy --dry-run` (no `apps/` entries + `functions/_middleware.ts` + legal pages present). **⚠ SUPERSEDED by cycle-2 P1-3** — `wrangler@4 pages deploy` has no `--dry-run`/`--list-files`; see cycle-2 log.
  - **P2-7** — canonical-host decision: `<link rel="canonical">` = `tortoise.premiselabs.co` on each legal page; "Back to product" uses relative links; per-host resolution tested (T7 #4/#6); documented in UX Decisions row 4 + T4 Step 1.
  - **P2-8** — task numbering/structure preserved (T7/T8 unchanged; T5 merged); frontmatter bumped to revision 2; this cycle log appended.

- **Cycle 2 (2026-08-08):** plan-review cycle 2 dispatched parallel reviewers; controller merged 4 P1s + P2s. Fixes applied in revision 3:
  - **P1-1 (CI collection deadlock)** — module-level opt-in guard `pytestmark = pytest.mark.skipif(not os.environ.get("RUN_LEGAL_E2E"), reason="legal suite: opt-in via RUN_LEGAL_E2E=1")` in `tests/e2e/test_legal_pages.py` so the `ci.yml` `welcome-e2e` PR job (collects the whole `tests/e2e/` dir) never runs the legal suite pre-merge; plus a HARNESS GUARD refusing `https://` BASE_URL/TORTISE_HOST unless `ALLOW_PROD=1` (no production assertions pre-merge, by construction). T7 Step 2 sets BOTH `BASE_URL=http://127.0.0.1:8788` and `TORTISE_HOST=http://127.0.0.1:8788`; the deploy-pages.yml post-deploy job sets `RUN_LEGAL_E2E=1` + `ALLOW_PROD=1`.
  - **P1-2 (unsatisfiable DOM parentage)** — replaced "sibling of `.providers` and `#email-form`" with: `.legal-accept` is a SIBLING of `.providers` (and `.divider`), PRECEDES `<form id="email-form">` in document order, and is NOT a descendant of the form (T5 Step 4 + T7 #7); never assert sibling-of-`#email-form`.
  - **P1-3 (no --dry-run/--list-files on wrangler pages deploy)** — verified via `wrangler@4 pages deploy/upload --help`; T8 Step 3 upload-scope verification replaced with (a) local file-set simulation (`git ls-files website/ | grep -v '^website/apps/'` + assert middleware/legal pages present + no `apps/` entries) and (b) post-deploy negative check (`https://premiselabs.co/apps/dashboard/index.html` → 404; positive `/_middleware.ts` + `/privacy`/`/tos` 200). All `--dry-run`/`--list-files` references dropped (supersedes cycle-1 P2-6 mechanism).
  - **P1-4 (conditional-tense rule passes vacuously + unowned #528 re-check)** — T7 #10(c) now BOTH-AND: (c1) repo-state assertion (page wording matches ACTUAL `website/` instrumentation markers — "no analytics tools are currently deployed" IFF no markers) + (c2) per-tool WORD-BOUNDARY regexes (`\bmeta pixel\b`, `\bposthog\b`, `google analytics`) within 120 chars of `consent|activated|will|may|when|if`. S5 present-tense grep promoted to a pinned-set pytest assertion (T7 #10(e)) + T4 Step 5 build-time grep. #528/#658 re-check wired into T8 Step 4 as a NAMED pre-merge gate (owner = implementer, record on #657); standing Meta-sharing re-check added to T2 ⑤ mirroring ⑭.
  - **P2-1** — placeholder-leak split: broad `\{\{|\$\{|<INSERT|TODO` (must match nothing) + D4-shape `\[[A-Z][A-Z ,]{8,}\]` (matches only `[ENTITY TYPE, JURISDICTION OF FORMATION TO BE CONFIRMED]`; count conditional on G-gate ②).
  - **P2-2** — root-page markers pinned to concrete strings: product.html h1 `Your agents remember <span>why</span>, not just what.` and index.html h1 `The primary function of memory is not <span>recall</span>, but <span>learning</span>.` (normalized forms asserted; normalize-and-compare served root vs local files as fallback) — applied to T7 #6, T7 Step 0, T8 Step 5.
  - **P2-3** — license-drift note (stale AGPL local `main` LICENSE + pyproject MIT vs worktree BUSL-1.1 truth source) posted to #657 + PR description (T1 Step 3, T8 Step 4).
  - **P2-4** — T1 §5 + T3 ⑥: amounts (solo $9/pro $25/team $149) are CONTEXT ONLY; ToS body must NOT contain dollar figures — references pricing page textually ("fees as published"); S6 guard stands.
  - **P2-5** — T4 ∥ T5 marked parallelizable in the Execution Handoff (both depend only on T3; disjoint file sets).
  - **P2-6** — DNS gate scope clarified: applies to PRODUCTION-host checks only; local-preview `tortoise.*` checks (Host-spoofed) run regardless. DNS-propagation procedure added: poll `dig +short` + root-marker curl every ~30s up to 10 min (300s TTL + margin); assert CNAME target + product marker; re-run gate fresh at T8 start.
  - **P2-7** — cross-host local mechanism documented: Chromium-only `context.set_extra_http_headers({"Host": ...})` limitation; preferred curl `-H "Host:"` middleware smoke test against `wrangler pages dev` FIRST; explicit fallback: defer #5/#6/#8 cross-host to T8 production-only, stated in task output.
  - **P2-8** — effective date pinned as machine-readable `Effective date: YYYY-MM-DD` (single canonical string/attribute) in T4; PRE-MERGE check in T8 Step 4 (approved date == expected deploy date; bump BEFORE merge); CI date comparison warn-only; manual T8 Step 5 hard assert.
  - **P2-9** — post-deploy CI job defined: exact commands mirroring `welcome-e2e` verbatim (`pip install -e '.[test]'`, `python -m playwright install --with-deps chromium`, `-p no:cacheprovider`), `timeout-minutes`, failure response (re-run → fix + comment on #657 → reopen on red), idempotence under unrelated later deploys; manual T8 Step 5 remains authoritative.
  - **P2-10** — README one-liner: deploys are CI-owned (push to main with `website/**`); README command is for local troubleshooting only.
  - **P2-11** — T8 Step 5 starts with a deploy-completion poll (retry/backoff until `/privacy` 200 on premiselabs.co) before the full production suite.
  - frontmatter bumped to revision 3; task numbering/structure preserved; this cycle log appended.

- **Cycle 3 (2026-08-08):** plan-review cycle 3 dispatched parallel reviewers; controller merged 4 P1s + P2s. Fixes applied in revision 4:
  - **P1-1 (harness-guard mechanism, CI safety)** — T7 Env block pinned: module-scope `pytest.skip("legal suite: opt-in via RUN_LEGAL_E2E=1")` via `pytestmark` — NEVER `pytest.exit()` (aborts whole session → red `welcome-e2e` on every PR); short-circuit on `not RUN_LEGAL_E2E` FIRST (the `ALLOW_PROD` harness guard applies only when opted in); T7 header "Run:" invocation now shows the FULL env (`RUN_LEGAL_E2E=1 BASE_URL=http://127.0.0.1:8788 TORTISE_HOST=http://127.0.0.1:8788 python -m pytest tests/e2e/test_legal_pages.py -v`) — the previously-bare invocation would silently skip everything.
  - **P1-2 (middleware emulation fallback must discriminate)** — T7 Step 2 rewritten: (1) FIRST `git diff --quiet HEAD -- website/functions/_middleware.ts`; unchanged → (2) throwaway canary function calling `context.env.ASSETS.fetch` (200 = binding works under `wrangler pages dev`; gap = workers-sdk#165); (3) pre-smoke assertion Functions actually EXECUTE in the preview (known passthrough path serves AND canary/marker observable); `--compatibility-date` pinned to production's (or default recorded) so local/prod runtime semantics don't diverge; deferral allowed ONLY on an actual canary/probe failure with unchanged middleware, and the task output must LIST EXACTLY which checks were deferred (#5/#6/#8), not merely state the decision.
  - **P1-3 (mocked email-path signup test)** — T7 #7 gained an email-path test mirroring the OAuth one: `page.route("**/auth/v1/signup*")`, fill `#email` + `#password`, click `#btn-submit`, assert the `auth/v1/signup` request fired + acceptance block present + zero console errors; optionally assert the post-submit check-your-inbox state (signup.html `#email-form` line 278, `signUpWithEmail` → `supabaseClient.auth.signUp` line 407, inbox state ~line 290); ~20 lines, `_mock_supabase_success` pattern.
  - **P1-4 (HTML structural validation)** — T4 Step 5 added a parse gate (`python3 -c "from html.parser import HTMLParser; ..."` / html5lib / tidy) failing on unbalanced/unclosed tags in privacy.html + tos.html; T7 #10(f) structural assertions per legal page (every `<section>`/`<h2>` closing tag, section body text as TEXT NODES not attribute values, `</body></html>` present, `querySelectorAll('h1,h2').length >= N`); T7 #9 now asserts minimum rendered `innerText` length + heading count per page (not just scrollWidth — an empty shell with no horizontal scroll must FAIL).
  - **P2-1** — eligibility pinned: T1 Step 2 canonical sentence "you must be at least 18 years old" (+ visible "18+"); T7 #2 asserts THAT string, not bare "18"/"age" (vacuous — "age" is a substring of "usage"); T4 Step 5 grep guard added.
  - **P2-2** — EXACT repo-state sentence "no analytics tools are currently deployed" locked at T2 Step 2; T4 renders it verbatim; T7 #10(c1) IFF assertion guaranteed by construction (wording cross-referenced at c1).
  - **P2-3** — dollar-figure guard: `grep -nE '\$[0-9]' website/tos.html` must return nothing (T4 Step 5) + `re.search(r"\$[0-9]", cleaned) is None` in T7 #2.
  - **P2-4** — anti-phrase guard: `assert "do not sell or share" not in cleaned` on BOTH pages (T7 #1/#2) + T4 Step 5 grep (CCPA link label not shipping this release).
  - **P2-5** — git ls-files ordering in T8 Step 3(a): `test -f` presence checks first (new pages are untracked until Step 4 commits); `find website -type f -not -path 'website/apps/*'` minus `.wranglerignore` patterns diffed against the expected list (site pages + legal pages + `functions/` + onboarding-prompt.md); junk-file guard (`.DS_Store`, `*~`, `*.tmp`, `*.swp`); stage-before-git-diff or `git ls-files --others --exclude-standard` noted.
  - **P2-6** — CI post-deploy job gains the SAME DNS preflight as the manual gate (dig + root-marker); if stale → skip/xfail `tortoise.*` assertions (premiselabs.co-only) with clear "DNS stale — manual remediation per #657/#540" marker + documented expected-red state in the job summary.
  - **P2-7** — Appendix A stale refs fixed: "(T6)" → T4 acceptance + T7 #10(a); "(T5)" broken-links → T7 #8 / T8.
  - **P2-8** — authoritative canonical set: T4 Step 5's present-tense grep now points at T1/T2 Step 2 canonical sentences as THE pinned set; T7 #10(e) references the SAME list — T4 and T7 can never disagree.
  - **P2-9** — effective date: visible-line `Effective date: YYYY-MM-DD` MANDATORY exactly once per page (data attribute optional additive, NOT an alternative).
  - **P2-10** — Clock/TZ pinned: "deploy date = author's local calendar date at merge"; `date +%F` recorded in the PR description at T8 Step 4; BOTH the CI warn-only comparison and the T8 Step 5 hard assert compare against that recorded value.
  - frontmatter bumped to revision 4; task numbering/structure preserved; this cycle log appended.

- **Cycle 4 (2026-08-08):** plan-review cycle 4 dispatched parallel reviewers; controller merged 5 P1s + P2s. Fixes applied in revision 5:
  - **P1-1 (harness guard: runtime short-circuit, not pytestmark)** — T7 Env block: opt-in is now a RUNTIME statement as the FIRST executable line after imports (`if not os.environ.get("RUN_LEGAL_E2E"): pytest.skip("legal suite: opt-in via RUN_LEGAL_E2E=1")`), THEN the ALLOW_PROD harness guard below it; `pytestmark` demoted to belt-and-suspenders AFTER both runtime skips; invariant restated honestly ("bare collection NEVER ERRORS — skip is the guaranteed outcome"); `post-merge-validation.yml` (`python -m pytest tests/ -q --timeout=45` on merge — collects the new module too) added to the analyzed collection surfaces.
  - **P1-2 (stdlib HTMLParser is lenient — feed() never raises)** — T4 Step 5 pins the concrete mechanism: a stack-tracking HTMLParser subclass failing nonzero on (i) closing tag not matching stack top, (ii) non-empty stack at EOF, (iii) premature `</html>`/`</body>`; applied to ALL tags, void elements via `HTMLParser.void_elements`/`handle_startendtag` (meta, link, br, img, input, hr, source); html5lib/tidy optional enhancement only. T7 #10(f) extended to FULL-document tag balance (all tags, not just section/h2) using the same stack-check logic in pytest.
  - **P1-3 (upload-scope gate satisfiable)** — T8 Step 3(a): `README.md` added to the expected file set (ships with every `wrangler pages deploy .` upload — verified at site root); functions entry expressed as `functions/_middleware.ts` (FILE, not dir); the check restated as a NORMALIZED RELATIVE-PATH SET comparison (`found ⊆ expected AND expected ⊆ found`).
  - **P1-4 ($5M AUG vs dollar guard)** — the sole allowed `$[0-9]` occurrence in tos.html is the BUSL AUG "$5M" threshold in the license-boundary section; /tos guard relaxed to `len(re.findall(r"\$[0-9]", cleaned)) <= 1` (T7 #2) and `grep -cE '\$[0-9]' website/tos.html` ≤ 1 (T4 Step 5); exemption stated in T1 Step 3 + T4 Step 5 + T7 #2; license.html "$5M" unguarded (guard targets tos.html only).
  - **P1-5 (present-tense exact-equality vs REQUIRED live disclosures)** — classification rule stated IDENTICALLY in T2 Step 2 / T4 Step 5 / T7 #10(e): REQUIRED live disclosures (processors Supabase/Cloudflare/GitHub/Google — operating today, signup.html makes real `supabaseClient` calls; collected-data categories email/login + usage data) are EXPECTED content drafted in PASSIVE/IMPERSONAL phrasing ("data is processed by Supabase for authentication", "account data includes your email address") so they never match `we (use|collect|share|sell|process)`; only NEW present-tense claims OUTSIDE the approved pinned set fail; the RECOMMENDED passive-voice option chosen and pinned (simpler, no allowlist drift).
  - **P2-1** — TORTISE_HOST_CHECK contract: tortoise.* tests (#5/#6/#8) carry `pytestmark = pytest.mark.skipif(os.environ.get("TORTISE_HOST_CHECK") != "1", reason="DNS stale — manual remediation per #657/#540")`; CI sets the env ONLY when its DNS preflight is green; "expected-red" wording dropped (skip/xfail = green-with-annotation); T7 Step 0 reconciles: the manual gate HARD-blocks production tortoise.* checks; CI cannot block (relaxation noted explicitly).
  - **P2-2** — CI date comparison no longer reads PR descriptions (impossible — deploy-pages.yml is push-triggered): canonical date recorded in the MERGE COMMIT MESSAGE (T8 Step 4) + the pages' changelog entry; CI compares the SERVED effective date vs the DEPLOY COMMIT date (`git log -1 --format=%cs`) — warn-only; when unavailable → warn + skip (never error).
  - **P2-3** — `--compatibility-date`: T7 Step 2 first captures production's effective value (run `wrangler pages dev` once WITHOUT the flag, record the reported default — no wrangler.toml, no date set anywhere, production runs wrangler@4's default), then pins local to that; hardcoded 2024-11-01 dropped (any date is "example — verify"); recorded value noted in the task output.
  - **P2-4** — section-list pinning: T7 #10(f) derives the 15/16 section titles from the G-gate-approved drafts at T4 — assertion lists committed alongside the drafts in `docs/drafts/` (`2026-08-08-657-tos-section-titles.txt` / `2026-08-08-657-privacy-section-titles.txt`) and imported by the test — never invented at test time.
  - **P2-5** — post-#528/#658 ownership: T8 Step 4 G-528 gate gains the explicit statement — post-release merges of #528/#658 require the privacy-page re-draft (remove repo-state sentence, add per-tool conditional framing) as a REQUIRED COMPANION CHANGE to that issue's PR (verified by the c1 assertion); the deploy job's red on such a merge is the intended fail-loud signal, NOT a regression; recorded on #528/#658.
  - **P2-6** — T8 rollback block (new Step 6): failure classification — REVERT-WORTHY (negation-rule violation, 404 on legal URLs, effective-date mismatch on SERVED content) vs FIX-FORWARD (environmental/transient); reverting the merge restores prior served content via the existing push-triggered CI deploy; known-issue state documented on #657 while the fix is in flight.
  - **P2-7** — T7 #7: (a) pinned acceptance text asserted via `_clean` on `.legal-accept` (contains "by creating an account, you agree to the" + link labels "terms of service"/"privacy policy"); (b) mocked email-path inbox-state assertion now MANDATORY — `#confirmation-required` (signup.html ~line 291) visible AND `#confirm-email` contains the typed email.
  - **P2-8** — per-host product.html paths pinned: product footer asserted on `TORTISE_HOST/` (middleware rewrite) AND `BASE_URL/product.html`; crawl item "product" = BOTH URLs (premiselabs.co `/` serves index.html — no footer, by design).
  - **P2-9** — CI deploy-poll: retry/backoff poll (`curl https://premiselabs.co/privacy`, ~5–10 min) added to the CI post-deploy job BEFORE pytest, mirroring the manual T8 Step 5 poll.
  - frontmatter bumped to revision 5; task numbering/structure preserved; this cycle log appended.

- **Cycle 5 (2026-08-08):** final review cycle — controller merged 5 P1s + P2s. Fixes applied in revision 6 (final revision before sign-off):
  - **P1-1 (TORTISE_HOST_CHECK must gate #4's tortoise-host half)** — check #4 SPLIT: `BASE_URL/product.html` footer presence asserted UNCONDITIONALLY; the `TORTISE_HOST/` footer assertion gated on `TORTISE_HOST_CHECK == "1"` (same skipif as #5/#6/#8). Gated set extended everywhere it is enumerated (T7 Env block, T7 Step 0, T7 Step 2 deferral list, T8 CI DNS preflight). The CI stale-DNS branch now has NO red path by design — the premiselabs.co half is the green signal.
  - **P1-2 (no HTMLParser.void_elements)** — `HTMLParser` has NO `void_elements` attribute (CDATA_CONTENT_ELEMENTS / interesting_normal only); `handle_startendtag` fires ONLY for self-closed `<tag/>`. Pinned an explicit `VOID = {"meta","link","br","img","input","hr","source"}` checked in BOTH `handle_starttag` (tag in VOID → do not push) and `handle_startendtag` (never push) — T4 Step 5 + T7 #10(f). product.html's slash-less `<meta>`/`<link>` (0 self-closed void tags) covered by the `handle_starttag` check. All `HTMLParser.void_elements` references deleted; the stack failure modes (close-vs-top mismatch, non-empty stack at EOF, premature html/body) kept.
  - **P1-3 (purposes wording + pinned-set semantics + guard verb coverage)** — T2 Step 1 ④ gained the pinned passive purposes sentence ("your email address and account information are used to deliver the service, respond to requests, and manage billing; usage data is used to improve the product"); T7 #10(e) defines the expected-match set explicitly as "the SUBSET of canonical sentences that match the guard regex" (removes set-equality ambiguity — most canonical sentences contain "we do" and don't match); guard regex widened to `we (use|collect|share|sell|process|retain|store|transfer)\b` everywhere it appears (S5, T4 Step 5, T7 #10(e)); expected-match subset pinned accordingly.
  - **P1-4 (verifiable owner-approval gates T3→T4)** — T3 Step 2 + Acceptance: completion requires recording the URL of the OWNER-APPROVED comment on #657 (owner-authored, containing explicit approval + decision log + link-set record); T4 acceptance requires that comment URL in its task output BEFORE any build work; if no owner comment exists, T3 remains OPEN and sub-agents must NOT mark it complete; Execution Handoff note added (owner comments approval; executor fetches/link-verifies).
  - **P1-5 (draft↔render fidelity check)** — T4 Step 5 + T7 #10(g): normalize approved draft (strip markdown, lowercase, collapse whitespace) vs rendered page body text (strip tags, lowercase, collapse whitespace); per-section containment — every draft sentence ≥ 40 chars appears in the rendered text (closes the approve-render gap: paraphrase/drop/reorder detection); also covers processors ⑩ name presence (Supabase/Cloudflare/GitHub/Google/Meta/GA/PostHog/Stripe all ≥ 40-char sentences in the draft); sub-40-char placeholders exempt.
  - **P2-1** — upload-scope find pinned: `.wranglerignore` excluded from the found set via `! -name '.wranglerignore'` (T8 Step 3(a)).
  - **P2-2** — $5M AUG guards changed from `≤ 1` to `== 1` on tos.html (never 0, never >1) — T1 Step 3, T4 Step 5 (`grep -cE`), T7 #2 (`re.findall`).
  - **P2-3** — free-tier placeholder shape pinned: exact lowercase prose `[free-tier usage limits to be published]` at G-gate ⑥ so it never matches the D4-shape regex; d2 allowlist note extended (T7 #10(d2)).
  - **P2-4** — host-header reach: canary/echo endpoint must REPORT the received `Host` header; assert it equals the spoofed value before trusting Host-spoofed page-level runs (#5/#6/#8 + #4 tortoise-host half) — T7 Step 2.
  - **P2-5** — carve-outs: G-gate checklist item ⑩ re-confirms "statutory/GDPR liability, fraud, willful misconduct NOT capped"; canonical sentence pinned at T1 Step 2 ("statutory/GDPR liability, fraud, and willful misconduct are not capped by this limitation of liability section"); co-occurrence assertion ("willful misconduct" + "not capped") in T4 Step 5 grep + T7 #2.
  - **P2-6** — classification rule blocks made textually IDENTICAL across T2 Step 2 / T4 Step 5 / T7 #10(e) (single canonical block; guard regex referenced by name).
  - frontmatter bumped to revision 6; task numbering/structure preserved; this cycle log appended.

- **Cycle 6 (2026-08-08): G-gate decisions recorded (rev 7)** — owner answered the G-gate items in a session; decisions recorder locked them into scope rev 5 + this plan: ① effective date = deploy date at T8 (NOTE, unchanged); ② entity LOCKED — Premise Labs is **NOT a registered entity** → contracting party/controller = **Daniel Ospina, individual operating as "Premise Labs" (d/b/a)**; `[ENTITY TYPE, JURISDICTION OF FORMATION TO BE CONFIRMED]` placeholder REPLACED (T1 Step 2, T2 Step 1, T3 ②, T3 Expected output; T7 #10(d2) now asserts count 0 — the "exactly once" branch is dead; broad `{{/$/<INSERT/TODO` guard unchanged); ③ DPA LOCKED — `/dpa` ships now (T4/T5/T7/T8 updated: unconditional); ④ no-training canonical wording owner-confirmed (all tiers); ⑤ docs.html mailto fix approved (in scope); ⑦ Art. 27 conditional line locked + resolve before EU onboarding post-#533 GA; ⑨ link set LOCKED per recommendation — `/license` + `/dpa` both ship, footers link them (T4/T5/T7 updated: no fallbacks, no IFF); ⑩ liability-cap carve-outs locked (statutory/GDPR liability, fraud, willful misconduct NOT capped); **⑥ (free-tier placeholder + "fees as published" wording) and ⑧ (private rights-request intake) remain PENDING owner decision (2026-08-08 session)** — NOT locked. Per cycle-5 P1-4, T3's OWNER-APPROVAL comment on #657 is still required at execution (this cycle records the decision log, not the approval). frontmatter bumped to revision 7; task numbering/structure preserved; this cycle log appended.

- **Cycle 7 (2026-08-08): G-gate ⑥ RESOLVED — fees clause approved (rev 8)** — owner + legal research approved the fees-clause approach (G-gate item ⑥); decisions recorder locked it into scope rev 6 + this plan: fee clause = **general fee-structure framing** (canonical sentence pinned at T1 Step 2: "we may charge fees for the Service based on a combination of subscription, usage-based (metered), and/or per-seat pricing, as defined by the pricing tiers published on the Pricing Page"); **Pricing Page reference HYPERLINKED** from the ToS — pricing section on `tortoise.premiselabs.co` (no dedicated /pricing page yet; **URL finalized at build** — dedicated /pricing route or product-page pricing-section anchor); mechanics (standard, number-free): **30 days' notice for price changes → effective at the next renewal term (never mid-term); continued use after notice = acceptance; free tier subject to published limits; fees non-refundable except as required by law; paid plans may require a payment method; auto-renewal/invoicing deferred until #432 ships**; **dollar-figure guard unchanged (`== 1`, sole exemption the $5M AUG in the license-boundary section — no $ amounts in the fees section)**; **pricing-consistency follow-up added to scope §6.2** (product-page free-tier numbers conflict — "5,000 Points free" vs "1,000 write ops/mo" — reconcile BEFORE the ToS hyperlinks the pricing page; under #432 or in this issue's build step). Plan updates: T1 Step 1 §5 + T1 Step 2 fees canonical sentence; T3 ⑥ → LOCKED (Step 1 header + Expected output updated — only ⑧ remains PENDING); **T4 Step 6 (NEW)** — Pricing Page reference resolves + hyperlinked in `tos.html` (+ free-tier numbers reconciliation as pre-merge check / #432 dependency); **T7 #2** — fee-keyword assertions (canonical fees sentence, "30 days"+"renewal" co-occurrence, "non-refundable", "Pricing Page" href not `#`); **T8 Step 4** — new **G-PricingRef** named pre-merge gate; S6 surface map updated; scope D9 → RESOLVED; frontmatter bumped to revision 8; task numbering/structure preserved; this cycle log appended.

- **Cycle 8 (2026-08-08): G-gate ⑧ RESOLVED — self-service deletion primary + DNS fixed by operator (rev 9)** — owner + legal research approved the rights-mechanism approach (G-gate item ⑧; GDPR Art. 12/17 + EDPB guidance — controllers must facilitate rights; self-service authenticated deletion = best practice); decisions recorder locked it into scope rev 7 + this plan: **self-service deletion is the PRIMARY rights mechanism; NO private intake form now.** Design: (1) Privacy primary erasure path = **in-product self-service "delete account"** (authenticated by construction — solves identity verification; instant, no 1-month clock on that path); (2) **build-time dependency check (NEW — T2 Step 5 + T7):** VERIFY the product has a working delete-account capability BEFORE the policy claims it; 2026-08-08 repo check: **ABSENT** (`apps/dashboard` = shell only; `apps/graph-viz` no account mgmt; `server/main.py` deletes only user-created points/edges — no user-deletion endpoint; `welcome.html` key-management only) → expected branch: scope the policy claim ("request deletion via GitHub issues" + Art. 12(3) 1-month + Art. 12(6) verification) + file the product follow-up (scope §6.2 #5); (3) long-tail channel remains (non-account data, can't-log-in users, access/export/object → GitHub issues + redaction guidance + 1-month + extension-notice + Art. 12(6)); (4) retention carve-outs stated honestly (billing/transactional per Art. 17(3); analytics/usage per the analytics section); (5) private Supabase intake form NOT needed now — DEFERRED (revisit if long-tail volume grows; no build pressure); (6) optional self-service export/access button (nice-to-have, not launch-required). **DNS (2026-08-08):** `tortoise.premiselabs.co` CNAME **FIXED by the operator** (root h1 mid-propagation at the time; onboarding-prompt.md already served text/markdown — correct project) — **T7 Step 0 is now a VERIFICATION step** (dig + root-marker poll 30s × 10 min; fresh gate at T8 start); **canonical host = `tortoise.premiselabs.co` again (per middleware design), `premiselabs.co` secondary**; `TORTISE_HOST_CHECK` contract kept as the propagation guard (skipif reason updated to "DNS propagation pending — verify per #657/#540"). Plan updates: T2 Step 1 ⑯ rewritten (self-service primary + long-tail + retention carve-outs); **T2 Step 5 (NEW)** — delete-account capability build-time check (verify exists → else scoped claim + file follow-up); T2 acceptance updated; T3 ⑧ → LOCKED (Step 1 header + Expected output updated — **ALL G-gate items now LOCKED**); T7 Step 0 + S10 + Architecture host-mapping → DNS verification notes; T7 Env block + skipif reason updated. Scope: D10 → RESOLVED; §5.6 ⑯ rewritten; §6.1 gap 2 cleared (ALL LOCKED); **§6.2 follow-up 5 (NEW — CONDITIONAL self-service delete feature product follow-up)**; §5.7 DNS note; frontmatter bumped to revision 9; task numbering/structure preserved; this cycle log appended.

<!-- plan-review: cycles=5, status=clean, version=2.2.0 -->
