# Implementation Plan — Issue #1348: Measure RRF fusion quality levers (pool floor, k sweep, GraphRanker verdict)

> Draft from solution-converge (Phase 5). Pending solution-verify (Phase 5.5) + parallel review gates (Phase 7).

## Problem statement

Three RRF-fusion levers are unmeasured: (1) production pool depth is hardcoded `str_limit = limit * 2` (sdk.py:8725), below the RRF 50–100 consensus sweet spot at typical limits (pool 20 at limit=10); (2) fusion constant k is hardcoded 60 (search_engine.py:633) with no sweep; (3) GraphRanker (#25) is unwired from the fused path and unscorable on the synthetic oracle corpus (all three signals null: random α/β, random edges, dead recency). P@5=0.924 near ceiling at depth 50 means unpaired deltas at n≈100 are unpowered — the measurement must be paired and in-loop.

## Proposed solution (synthesis: A "Fusion-native harness" + B's discipline + env-var convention)

- **Pool floor (prod):** inline `TORTOISE_POOL_FLOOR` env knob (matches the repo's 23-knob convention) + optional `pool_floor` param on the SDK search methods for programmatic override (the E2E-8 leg and tests need it). New rule: `str_limit = max(limit * 2, pool_floor)` (full-scan exempt). Returned limit unchanged — truncation at sdk.py:8851 precedes EP decoration at 8853+, so decoration is flat (#1353 NOT a dependency).
- **k sweep (eval):** paired in-loop computation. `retrieve_per_strategy` already produces per-strategy ranked lists; compute `rrf_fusion(..., k=20/60/100)` once per query from those identical lists — perfectly paired, tightest CIs, single run. Pure Python over rank positions; no retrieval re-run.
- **GraphRanker leg:** add `"fused_rerank"` to `STRATEGIES` (run.py:75) — real path, in-harness: `GraphRanker.rerank` over the fused pool (scores.rrf drives similarity; graph_boost from confidence + degree). Two arms on the SAME corpus: fused ON vs OFF.
- **Corpus enhancement (default GraphRanker leg):** `generate_oracle_points(..., enhance_signals=True)` — topic-correlated `n.confidence` and topic-correlated operator edges between EXISTING nodes only (no new nodes; no oracle-denominator or FTS/vector input shift). **Byte-identical unenhanced path — rng contract (P1 fix, verified synthetic_corpus.py:485-529):** the posterior draws (alpha/beta at 522-524) are INSIDE the `for i in range(n)` loop on the CONTINUOUS main rng stream — isolating them would shift every subsequent point's retracted/content/embedding/kind draws → byte-identity breaks. Therefore: alpha/beta remain drawn from the MAIN seeded rng in BOTH paths (identical per-iteration draw counts); in enhanced mode, `confidence` is DERIVED from those same drawn alpha/beta values mapped through topic membership (overlapping ranges, noisy — not a clean per-topic constant, per scoping correction 3 P3-2); the isolated `random.Random(seed ^ salt)` stream is used ONLY for extra draws (edge selection, noise) that never touch the main stream. Recency stays dead (documented).
- **Two-phase sequencing:** Phase 1 = depth×k sweep on the plain committed corpus (baseline comparability, reproduces baseline-embedded-2026-08-17.json); Phase 2 = enhance, then GraphRanker arms. Retrieval metrics are EP-blind (run_fts/vector/structural score text/embedding/kind; rrf_fusion rank-only; EP annotation post-truncation), so Phase-1 metrics are contamination-free by construction — the report declares this.

## Implementation plan (ordered tasks)

1. **Issue-body amendment** (scoping correction 1): correct Context fusion set (FTS+vector+structural, not FTS/vector/TF-IDF), Depends-on line (#317 slice 1 + #1144, labeled set → documented oracle proxy), indicator 3's nonexistent "labeled set".
2. **Stale-surface corrections** (correction 6): baseline notes 1+2, run.py docstring, tests/eval/retrieval/README.md:55, `embedded_engine` provenance string — embedded fulltext index exists, FTS populated 40/100.
3. **SDK pool floor** (sdk.py:8725): env knob + param; `str_limit = max(limit * 2, pool_floor)`. Unit tests for math, full-scan exemption, returned-limit invariance.
4. **Eval harness** (tests/eval/retrieval/run.py): `--pool` knob (feeds degradation_chain; default = `--limit`, preserving baseline semantics), `--k-sweep` (in-loop k=20/60/100), `fused_rerank` strategy (Phase-2 flag `--graph-ranker-arm`, default OFF so default runs stay shape-identical), SCHEMA_VERSION=2 report with per-query fused@{20,60,100} ADDITIONAL keys — **`fused` key REMAINS as the k=60 alias** (gate compatibility: `_gate_vs_baseline` run.py:265-283 hard-reads `per_query[qid]["fused"]`; a rename would silently no-op the gate), per-strategy population counts, fused-vs-best-single delta (`_paired_vs_fused` EXTENDED to include fused_rerank + update test_integration.py:75 which asserts the exact 4-strategy set), **paired fused_rerank−fused verdict statistic with 90% CI** (the core in-loop pairing payoff), pool-semantics note (eval pool == eval limit; SDK str_limit = limit×2 floor 50), embedder, enhanced flag. Add schema_version check to `_gate_vs_baseline` so a future rename can't kill the gate quietly.
5. **Corpus enhancement** (benchmarks/synthetic_corpus.py): `enhance_signals` flag on `generate_oracle_points` + topic-biased `seed_operator_edges`; **rng contract per the P1 fix above** (alpha/beta on main stream both paths; confidence derived from same draws; isolated stream only for extra edge/noise draws). `seed_corpus` SET clause gains confidence/edge writes GATED on the enhance flag (an unconditional write would silently change GraphRanker's plain-corpus behavior — coalesce 0.5 → written value — without tripping the fingerprint, which doesn't check confidence). **Corpus fingerprint EXTENDED:** sha256 over concatenated vecf32 embeddings + per-topic live counts (the existing CorpusFingerprint at synthetic_corpus.py:82-108 has n_points/kinds/n_operator_edges/dim/seed but no embeddings hash or per-topic live counts — extend it; `n_operator_edges` already covers the edge-set diff). Deviation from scoping correction 3 noted: enhancement targets `generate_oracle_points` ONLY (eval corpus), not the non-eval `generate_points` at :149-151 — justified: the GraphRanker verdict runs only on the eval corpus; the latency bench never scores GraphRanker.
6. **Phase 1 execution**: depth-curve {10, 25, 50, 100} × k {20, 60, 100} on plain corpus, embedded + Docker legs; record nDCG@10, R@10/20, per-tier deltas, pre-registered "insufficient power → verdict: needs real data" outcome. **R@20 pre-registration:** at pool 10 per-strategy lists are 10 items (fused union ≤30) — R@20 is undefined there; report R@10/20 per depth with R@20 omitted at pool 10 (stated in provenance).
7. **Phase 2 execution**: enhanced corpus, fused ON vs OFF arms (**Arm OFF = fused@60 status quo; Arm ON = fused + GraphRanker rerank, i.e. fused_rerank**); GraphRanker verdict naming its data source and live signals (confidence + degree; variance annotation only). fused_rerank mirrors sdk.py:9005-9012 production order: **fuse → rerank the full fused pool → truncate to limit → metrics on truncated list**. State explicitly: the Phase-2 OFF arm (fused on enhanced corpus) is numerically identical to Phase-1 fused because retrieval is EP-blind — the k-sweep and the verdict are cross-comparable, ON/OFF delta is the sole difference.
8. **E2E-8 re-measurement** (Docker, after #1349 lands or with embedder pinned): censored end-to-end p95 (mix-weighted ≤300ms at customer surface) at new floor + retrieval-only p95 separately. **P1 fix — run_report.py pool knob:** the #316 runner's retrieval arms call run_fts_query/run_vector_query/degradation_chain DIRECTLY with hardcoded `limit=20` (run_report.py:237-255) — the floor never reaches them, so a naive e2e@pool-50 minus retrieval@pool-20 decomposition is invalid. Add a pool knob to run_report.py (thread limit=str_limit or --pool into _make_arm), re-measure retrieval arms at the new floor, record per-arm pool depth in benchmark provenance. Note: the e2e leg (_sdk_e2e, run_report.py:269-278) inherits the floor AUTOMATICALLY via sdk.tortoise_fts_query (pool 20→50) with zero code change — state this.
9. **Verdict writeup + close**: plan doc at `docs/plans/2026-08-17-1348-deeper-pool.md`, falsification-checked conclusions, real-EP dependency surfaced if verdict is "reject/needs real data".
10. **Config surface + consumers**: register `TORTOISE_POOL_FLOOR` (default 50, str_limit-only) in `.env.example` in the repo's annotation style; name `tortoise_fts_query` as the SDK signature to extend (pool_floor: int | None = None, backward-compatible) — all its consumers (recall_state, search_sessions, suggest_entry_points, MCP tortoise_search, hosted /v1/search) inherit via the SDK with NO new tool/API param (surface stays stable; state this so no parity issue is filed); E2E-8 covers the search surface; recall's budget not re-measured here (out of scope, stated).

## Testing strategy

- New unit tests: pool-floor math; k param honored for 20/100 (k=60 never overridden today); `fused_rerank` harness shape (GraphRanker.rerank with scores.rrf — complements existing 26 tests in test_ranking.py); **fingerprint equality** — `enhance_signals=False` output byte-identical to committed corpus, `True` differs only in confidence/edges.
- Determinism: two same-seed runs → identical reports (isolated rng).
- Baseline gate: `--baseline baseline-embedded-2026-08-17.json` with defaults must pass unchanged (proves no-flag drift).

## Verification plan

`uv run pytest tests/ -m "not slow"` + new tests; full suite `uv run pytest tests/ -v`; baseline gate above; Docker sweep with all strategies populating (population counts in report self-declare authority); E2E-8 leg on Docker.

## Acceptance criteria

1. `TORTOISE_POOL_FLOOR` default 50: limit=10 → pool 50; floor override → pool max(limit×2, floor); returned list still `limit`; truncation still pre-decoration. Test-asserted.
2. k sweep report contains per-query fused@{20,60,100} metrics from one run (paired) with 90% bootstrap CIs; `fused` key retained as k=60 alias; baseline gate still reads `per_query[qid]["fused"]` and gates cleanly against the v1 baseline.
3. Report includes population counts, fused-vs-best-single delta, paired fused_rerank−fused verdict CI, pool-semantics note, embedder, enhanced flag.
4. GraphRanker verdict delivered from two-arm ON/OFF run on enhanced corpus (fuse → rerank → truncate); names data source + live signals; adopt/hybrid/reject with bounds.
5. E2E-8 censored end-to-end p95 ≤300ms at new floor, with run_report.py pool knob (retrieval arms at the new floor, per-arm pool depth in provenance); or documented regression decomposed retrieval-vs-decoration.
6. All four stale surfaces corrected; issue body amended; `.env.example` registers TORTOISE_POOL_FLOOR.
7. Unenhanced path reproduces committed baseline byte-identically (alpha/beta on the main rng stream in both paths; confidence derived from the same drawn values; isolated stream only for extra edge/noise draws); fingerprint extended to embeddings sha256 + per-topic live counts.

## Runtime prerequisites

Docker FalkorDB (authoritative sweep legs); Python 3.12 + uv; #1349 landed or embedder pinned before Docker measurement; AGENT_INFRA_PATH for commit-workflow gate; skill gates: writing-plans (this doc), commit-workflow before every commit. No Tortoise graph writes in this issue.

## Rejected alternatives

- **B "Layered analysis" (post-processor + 3 separate runs)** — *would have been better* if the harness were frozen (e.g., #1144 CI gate locked run.py), letting the sweep ship with zero harness risk; or if P@5 had headroom, where unpaired 3-run deltas would still be interpretable. Rejected: near-ceiling 0.924 makes unpaired n=100 deltas unpowered; a post-processor rerank drifts from harness semantics (RRF input truncation, annotation shapes) and yields a less honest GraphRanker verdict.
- **C "Explicit orchestration" (subprocess sweep_k.py + rerank_eval.py)** — *would have been better* if cross-leg state bleed were a real risk (full isolation) or if the harness were unsafe to touch at all. Rejected: heaviest tooling, slowest (3× DB re-runs), no statistical gain — in-loop pairing is strictly superior.
- **Config module for pool floor (A's `tortoise/config.py`)** — *would have been better* if ≥5 knobs were imminent (a config module pays off then); for one knob the 23-knob env convention is the repo's native pattern. The SDK param, not a module, covers the eval's programmatic need.
