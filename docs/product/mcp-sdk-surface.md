---
title: "The agent-facing surface — every MCP tool and public SDK method"
type: synthesis
domain: capability
doc_status: live
created: 2026-09-17
ownedBy: epistemic-team
generated_from: config/surface-manifest.yml
issue: 3863
---

# The agent-facing surface — for review

Cut at `4d11a7b40`. **82 MCP tools · 150 SDK methods.**

Generated from `config/surface-manifest.yml`. **Do not edit this file** — change the manifest.

## ⛔ Changing this surface needs Daniel's approval

**You may not add, remove, or rename an MCP tool or a public SDK method without approval
from Daniel.** The surface is the contract every agent and customer integration is built
on, so a change materially affects customer outcomes. Ask Daniel first — repo `AGENTS.md`
→ "USER QUESTIONS" / "DECISION RELAY" — then follow `CONTRIBUTING.md` ('The MCP tool
surface and public SDK methods cannot grow by accident').

**The gates are drift controls, not approval gates.** `tools/surface-guard.py` and
`tools/surface_manifest.py check` compare the live declaration against this baseline; a
change that updates the registry and this baseline together **passes both**. They catch an
*unrecorded* change — they cannot tell an approved addition from an unapproved one.
Daniel's review is what carries the approval.

## How to read this

- **Grouped, not alphabetised.** Tools are grouped by the part of the system they belong to,
  and inside each group near-duplicates sit next to each other, so a cluster can be judged as
  a group.
- **`reaches` is the dependency question answered:** which SDK method the tool actually calls,
  or `handler-served` when it declares no SDK call at all.
- **`⚠ FALSE DECLARATION`** marks an entry that names an SDK method which does not exist. The
  tool still works; the declaration is wrong.
- **`DEPRECATED`** marks an alias kept for compatibility. **Retired** names are listed at
  the end: they are off the advertised surface, still answer, and warn the caller with the
  replacement (#3883).
- **`used by`** answers *what is this for*: which eval harness, piece of tooling, skill, or
  doc references it by name — and whether agents actually call it.
- **`recomm.`** is a vocabulary of five values, and it is a **proposal**, not a verdict:
  `keep` · `kill` · `merge` · `fix-declaration` · `review`. `review` means *look at this*,
  not *cut this*.
- **`never called`** means no call appears in our own tool-call log. That is evidence about
  *our* usage, not about whether the tool is useful to a customer.

## At a glance

| Group | Tools |
|---|---|
| memory | 27 |
| graph | 13 |
| reasoning | 12 |
| sessions | 8 |
| admin | 4 |
| onboarding | 7 |
| journal | 5 |
| sources | 2 |
| review | 3 |
| mining | 1 |
| **total** | **82** |
| **retired — callable, warns (#3883)** | **16** |

| SDK methods, by who reaches them | Count |
|---|---|
| agent-reachable | 88 |
| internal | 21 |
| control-plane | 2 |
| no-caller-found | 39 |
| **total** | **150** |

---

## What the gate compares — and what it does not

The freeze this document serves is on **tools and endpoints**. The gate compares the live
declaration against the approved list below. It fails on three kinds of name-level change:

1. **A new tool** — an MCP tool that is not on the approved list.
2. **A new endpoint** — a public SDK method that is not on the approved list.
3. **An approved entry disappearing** — a tool or endpoint that is on the list and is no
   longer offered.

It also fails when the implementation behind an approved name is swapped for a different
one, because that changes the endpoint while leaving the name intact.

Those are the name-level checks. The gate **additionally** fails on several served-surface and
evidence-integrity checks — an unapproved server transform, a moved SDK binding, a changed
HTTP-vs-stdio serving, a registry count that no longer matches the baseline, an exemption that
became reachable, and a missing or malformed baseline. They are listed in full in
[`CONTRIBUTING.md`](CONTRIBUTING.md); read them there rather than inferring the gate's whole
scope from this summary.

**What is not a gate failure: an added field on an existing response.** A field that is off
by default, and leaves the response unchanged when it is off, is neither a new tool nor a new
endpoint, so it is not a name-level change and does not gate **as an addition**. The precedent
is in the tree: the W4 why-layer key on the `tortoise_analyze` response is written only when
`TORTOISE_W4_ENRICHMENT` is truthy (1/true/yes/on; unset or `0` means off) — and every
other field stays byte-identical when it is absent (`tortoise/mcp_server.py`,
`tortoise/why.py`). That is a different `why` key from
the one on `volunteer_context`, which is present by default.

**One qualification, because the gate also fingerprints implementations.** The gate records a
digest of each registered tool's own code object, so a field added *inside a tool's handler*
changes that tool's fingerprint — and, since the fingerprint covers the function's source
position, the fingerprint of every tool defined after it — and reds the gate, correctly,
as a changed implementation rather than a new tool. Add response fields in the SDK or
assembly layer, not inside a tool function, and the carve-out holds.

**And it must still be recorded.** Every such addition goes in the table below, so this
document stays the single source of truth. Two of `check`'s properties defend that record: an
empty or missing `response_fields` block is a failure, and every entry must name a tool or
endpoint that exists in this manifest — so the record can be neither deleted nor left
unanchored. What no check *can* see is an off-by-default field that nobody recorded at all:
nothing inspects response bodies at runtime, so that half is a reviewing obligation and this
document says so rather than implying the machine guarantees it. The carve-out is about what
the gate *fails* on — not about what goes *unrecorded*.

### Recorded response fields

| Response | Field | Emitted when | Unchanged when off |
|---|---|---|---|
| `tortoise_analyze` | `why` | TORTOISE_W4_ENRICHMENT is truthy (1/true/yes/on; unset or 0 means off) | yes — the response is byte-identical when the flag is off |
| `tortoise_session_capture` | `capture_redactions` | always — the count of credential-shaped spans redacted from this capture's stored turn text (0 when nothing matched) | n/a — not gated by a flag; it is a receipt field, not a response-shape change (#4911) |

A field belongs in that table from the moment it is added — an off-by-default field that is
not recorded here has no approval behind it, and the carve-out does not cover it.

---

## The MCP tools

### memory — 27

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_query` | Query points by pointKind and/or property filters — structural exact-match retrieval for known shapes (Epic #888: paginated_query + query_points_by_tag merged in). Pagination vi… | query | agents, tooling, skill-docs, docs | **keep** | group: `query` · in use — called by agents |
| `tortoise_belief_timeline` | Dated, ordered belief chain for a topic — decision Points with validFrom, NAND/CORRECTS links, superseded priors visible. | belief_timeline | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |
| `tortoise_expand_relationships` | Full relationship payload for ONE Point, incl. each related point's content (the expand side of the #1353 list/expand split — search returns bounded state entries; use this to r… | expand_relationships | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_get_confidence` | Get EP confidence for a claim: {mean, variance, alpha, beta}. | get_confidence | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_get_source_reliability` | Derive a Source's reliability (0-1) — query-time, cache-consistency-checked. NOTE: refreshes the reliability cache on the Source node (write-through projection), so not read-only. | get_source_reliability | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_recall` | Epistemic recall — four intents via mode (preset + override). mode='state' (UC1): current high-confidence state — multiplicative confidence gate (score = relevance^a × confidenc… | recall_state | agents, eval-harness, tooling, skill-docs, docs | **keep** | in use — called by agents |
| `tortoise_search` | Hybrid semantic search — FTS + vector + structural RRF fusion with EP confidence annotation. Use when matching by MEANING (natural-language query); for exact structural/filter q… | tortoise_fts_query | agents, eval-harness, tooling, skill-docs, docs | **keep** | in use — called by agents |
| `tortoise_suggest_entry_points` | Entity resolution — NL query → matching entities from the graph. | suggest_entry_points | agents, tooling, skill-docs, docs | **keep** | in use — called by agents |
| `tortoise_index_files` | Index a corpus directory of .md files as Sources + Events/Documents (the unified index path — replaces tortoise_index_sessions + tortoise_ingest_corpus file semantics for local… | index_directory | agents, tooling, docs | **keep** | group: `deprecated-index` · in use — called by agents |
| `tortoise_create_point` | Create a Point node (statement, decision, vision, hypothesis, etc.). dedup=True (default): idempotent — returns existing Point if content matches. | create_point | agents, tooling, skill-docs, docs | **keep** | in use — called by agents |
| `tortoise_calibrate_summary` | Audit graph calibration state. Returns per-point guidance. | calibrate_summary | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_set_point_baseline` | Set Beta prior evidence for a claim. | set_point_baseline | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |
| `tortoise_set_source_tier` | Set (or change) a Source's credibility tier (T0-T4). Non-destructive — never overwrites sourceKind type strings. | set_source_tier | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_update` | Update a Point OR entity by id. Points get point-lifecycle semantics (draft→live promote via status, version increment for Point:Object, status validation); entities get a plain… | update | never called, tooling, skill-docs, docs | **keep** | group: `update` · never called · **proposed: move to `memory`** — not called over MCP, but used by tooling, skill-docs — removing it would break that · group `update` is split across ['graph', 'memory']; proposed family `memory` — waits for your approval |
| `tortoise_update_point` | Update properties on a Point. Safe — modifies one Point only. | update_point | never called, tooling, skill-docs, docs | **merge** | in group `update` · never called · **proposed: move to `memory`** — `tortoise_update` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `update` is split across ['graph', 'memory']; proposed family `memory` — waits for your approval |
| `tortoise_delete` | Delete a Point or entity by id. DESTRUCTIVE — requires human confirmation. Cannot be undone. dry_run=True previews the blast radius (which node resolves, and every edge that wou… | delete | never called, tooling, skill-docs, docs | **keep** | group: `delete` · never called · **proposed: move to `memory`** — not called over MCP, but used by tooling, skill-docs — removing it would break that · group `delete` is split across ['graph', 'memory']; proposed family `memory` — waits for your approval |
| `tortoise_delete_point` | Delete a Point. DESTRUCTIVE — requires human confirmation. Cannot be undone. dry_run=True previews the blast radius (the point and every edge that would be removed) and changes… | delete_point_wrapped | never called, tooling, skill-docs, docs | **merge** | in group `delete` · never called · **proposed: move to `memory`** — `tortoise_delete` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `delete` is split across ['graph', 'memory']; proposed family `memory` — waits for your approval |
| `tortoise_invalidate` | Mark a Point outdated with a CORRECTS edge from the correcting Point. dry_run=True previews the one-point transition + one edge and changes nothing. | invalidate_point | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |
| `tortoise_retract_point` | Tombstone-retract a Point — status='retracted' (point stays in graph, excluded from default surfaces). Terminal; cannot retract operators or already-terminal points. dry_run=Tru… | retract_point | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_supersede` | Atomically replace old Point with new — CORRECTS edge + outdated flag. transfer_edges=True (default): full supersede — all edges move from old to new. transfer_edges=False: inva… | supersede | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |
| `tortoise_audit` | Audit graph wiring quality — 8 checks: missing sourceKind (point-level legacy + Source-level canonical), missing sourceDate, superseded points without a CORRECTS edge, live IMPL… | audit | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_list_batch` | Audit one ingest bundle: the stamped artifacts (Points created or adopted via dedup + direct edges) carrying the given batch_id. Entities/sources are out of stamp scope; editori… | list_batch | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_list_batches` | Batch discovery — the most recent distinct ingest batch_ids with their point/direct-edge counts (ordered by newest stamp, capped at limit, default 20). | list_batches | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_dream_health` | Dream observability (epic 903-C7): zero-output silent-death alarm verdict + health record (last pass, coverage, failure rate, region_attempts, warm-start savings). Embedded call… | dream_health_check | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_issue_insight` | Return a compact 'there's more in the graph' insight for a would-be issue — call BEFORE filing. Surfaces cross-session decisions / EP-tagged claims matching the title (semantic… | issue_insight | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_assess_source` | Record an agent's assessment of a Source (0-1 score + rationale). Creates a pointKind='assessment' Statement Point; latest per (url, assessor) wins; weighted by assessor reputat… | assess_source | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_compute_confidence` | Compute confidence via EP belief propagation. Returns {iterations, converged, confidences}. #395: no-arg (no factors/anchors) runs LOCAL EP over the dirty subgraph on stdio/embe… | compute_confidence | agents, tooling, skill-docs, docs | **keep** | in use — called by agents |

### graph — 13

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_get_entity` | Get any entity by ID, eventId, or url. `type` selects the node kind (point / entity / operator / events / governance), dispatching exactly as the retired per-type getters did; o… | get_entity | never called, tooling, docs | **keep** | group: `fetch-by-id` · never called · **proposed: move to `graph`** — not called over MCP, but used by tooling — removing it would break that · group `fetch-by-id` is split across ['graph', 'sessions']; proposed family `graph` — waits for your approval |
| `tortoise_create_entity` | Create an entity — type: subject/object/event/document. Event entities wire about* edges from aboutSubject/aboutObject/aboutPoint/aboutDocument props. Returns {node, nudges} — n… | create_entity | never called, tooling, docs | **keep** | group: `create` · never called · **proposed: move to `graph`** — not called over MCP, but used by tooling — removing it would break that · group `create` is split across ['graph', 'sources']; proposed family `graph` — waits for your approval |
| `tortoise_create_event` | Create an Event node (meeting, decision, deployment, etc.). | create_event | never called, tooling, docs | **merge** | in group `create` · never called · **proposed: move to `graph`** — `tortoise_create_entity` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `create` is split across ['graph', 'sources']; proposed family `graph` — waits for your approval |
| `tortoise_create_object` | Create an Object node (product, customer, skill, etc.). | create_object | never called, tooling, docs | **merge** | in group `create` · never called · **proposed: move to `graph`** — `tortoise_create_entity` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `create` is split across ['graph', 'sources']; proposed family `graph` — waits for your approval |
| `tortoise_create_subject` | Create a Subject node (team, role, organization, person). | create_subject | never called, tooling, docs | **merge** | in group `create` · never called · **proposed: move to `graph`** — `tortoise_create_entity` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `create` is split across ['graph', 'sources']; proposed family `graph` — waits for your approval |
| `tortoise_create_edge` | Create a typed structural edge between two entities. Relation: performs, produces, uses, memberOf, ownedBy, managedBy, about*, related, dependsOn, etc. Operator-less per the rei… | create_edge | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_create_operator` | Create an operator connecting Points. op_type: IMPL, NAND, composedOf, decomposesInto, contains, wraps. | create_operator | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |
| `tortoise_ingest` | Heterogeneous bulk write (epic #888 W4) — one call writes points + entities + sources + connections coherently (nodes first, then connections). Connections carrying 'operator' (… | ingest | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |
| `tortoise_update_entity` | Update any entity's properties. | update_entity | never called, tooling, docs | **merge** | in group `update` · never called · **proposed: move to `memory`** — `tortoise_update` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `update` is split across ['graph', 'memory']; proposed family `memory` — waits for your approval |
| `tortoise_delete_entity` | Delete any entity by ID. DESTRUCTIVE — cannot be undone. dry_run=True previews the node(s) and every edge that would be removed and changes nothing. | delete_entity | never called, tooling, docs | **merge** | in group `delete` · never called · **proposed: move to `memory`** — `tortoise_delete` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `delete` is split across ['graph', 'memory']; proposed family `memory` — waits for your approval |
| `tortoise_operator_action` | Consolidated operator write action — action=mitigate/annotate. mitigate: reason + strength (0-1) — creates/updates the mitigation Point (idempotent). annotate: bias/precision/co… | operator_action | never called, tooling, docs | **keep** | group: `operator-action` · never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_annotate_operator` | Annotate an operator Point with structured epistemic dimensions (bias, precision, consistency, directness). | annotate_operator | never called, tooling, skill-docs, docs | **merge** | in group `operator-action` · never called — `tortoise_operator_action` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way |
| `tortoise_mitigate_operator` | Create a mitigation Point that modulates an operator's edge strength. Idempotent — second call updates existing mitigation. | mitigate_operator | never called, tooling, skill-docs, docs | **merge** | in group `operator-action` · never called — `tortoise_operator_action` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way |

### reasoning — 12

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_overview` | Graph orientation in one call — consolidates the list_*/status/health/taxonomy/structure zoo. section: taxonomy/structure/structure_check/pointkinds/tags/sources/namespaces/grap… | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_provenance` | Provenance chain — 'Who decided this?' Follows authoredBy → Subject → delegation. | provenance | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_traverse` | Multi-hop graph traversal from entity following ALL relationship types. Returns {entity, nodes: [{node, relationship, depth}]}. | traverse | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_entity_profile` | Multi-hop BFS from an entity with optional filters (pointKind, confidenceMin) — full neighborhood categorized by node type. Use for deep entity analysis; for a fast neighbor lis… | handler-served | agents, tooling, skill-docs, docs | **keep** | in use — called by agents |
| `tortoise_list_topics` | Fast one-hop neighbor enumeration for an entity — quick discovery and navigation. Use for shallow context; for multi-hop filtered BFS use tortoise_entity_profile. | list_topics | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_analyze` | Answer natural language questions about the Tortoise epistemic graph. Ask things like: 'where is the disagreement?' 'what supports claim X?' | handler-served | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_dream` | Run EP stabilization (dreaming, #85). Default: incremental dirty subgraph. Set full=True for whole-graph. mode (epic 903): explicit strategy override {"local", "stale-first", "f… | dream | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_find_cross_lens_candidates` | Cross-lens candidate discovery (READ-ONLY, #438 bring-your-own-agent): surface unverified candidate pairs between Points from DIFFERENT sources (cross-stream discovery over the… | get_cross_lens_candidates | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_check_structure` | Check Gate 0→4 chain integrity (orphans, dangling refs). | check_structure | agents, eval-harness, tooling, skill-docs, docs | **keep** | in use — called by agents |
| `tortoise_review_connections` | Review graph connections (READ-ONLY — never mutates the graph). Hygiene counterpart to connect: mode=add surfaces related-but-missing connections as suggestions {from, to, sugge… | review_connections | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_summarize_structure` | Structure summary — points on the graph across ALL point kinds (#2205). Returns {total, operators, gate0_jtbds..gate4_requirements, gate_total}: total counts non-operator Points… | summarize_structure | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |
| `tortoise_validate_domain` | Validate a domain's ontology integrity (issue #405) — advisory, read-only. Runs the domain's graph-surface rules (orphan useCase, dangling refs, draft hygiene) and returns enric… | validate_domain | never called, tooling, skill-docs, docs | **keep** | never called — not called over MCP, but used by tooling, skill-docs — removing it would break that |

### sessions — 8

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_get_session` | Get a single agent session Event by session_id. | get_session | never called, tooling, docs | **merge** | in group `fetch-by-id` · never called · **proposed: move to `graph`** — `tortoise_get_entity` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `fetch-by-id` is split across ['graph', 'sessions']; proposed family `graph` — waits for your approval |
| `tortoise_search_sessions` | Search indexed agent sessions. Returns Events with narrative_arc snippets. | search_sessions | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_session_capture` | File an agent session into the graph (epic #909 capture path): turns become episodic Points, the conversation is LLM-extracted into epistemic Points, and the Session links to su… | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_graph_set_recording` | Set or clear a graph's session-recording override (#2302) — the MCP twin of PATCH /v1/graphs/{graph_id} (recording), sharing the same hosted_api core so the surfaces never drift… | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_list_graphs` | List all graph names in the database. Useful for namespace discovery. | list_graphs | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_list_namespaces` | List installed pack namespaces. | list_namespaces | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_events_poll` | Poll graph/claim events after an opaque cursor (at-least-once). Returns {events, next_cursor}. Event types: 11 registered claim types (PointAdded, OperatorAdded, PointRetracted,… | events_poll | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_session_context` | Return 'what happened last session' — diary entries, recent Points, Events, confidence changes. | session_context | agents, tooling, docs | **keep** | in use — called by agents |

### admin — 4

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_backfill_v25` | Backfill database to ONTOLOGY v2.5 schema. EXCLUDED from tenant HTTP — schema-level migration (operator-only). | backfill_v25 | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_org_create` | Create isolated team graph via FalkorDB select_graph. EXCLUDED from tenant HTTP — provisioning belongs to /internal/provision behind FASTAPI_INTERNAL_KEY. | org_create | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_packs_list` | List this team's active packs (#318): the shared pack catalog joined with the tenant graph's PackInstall activation records. Read-only; empty result when nothing is installed (e… | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_pack_install` | Install a custom expansion pack on the HOSTED surface (#1935): validates against the shared registry + tenant policy (reserved starter namespace, ontology-only v1), stores the m… | handler-served | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |

### onboarding — 7

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_onboarding_demo_create` | Create the demo epistemic graph (4 layers) for this team. Idempotent (Q4). | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_onboarding_github_index` | Start background GitHub indexing of an org's issues/PRs (Q2). | handler-served | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_onboarding_seed` | File the two onboarding anchor Subjects (Organization/organization + User/naturalPerson linked memberOf) — interactive, ontology-precise (#1999 W3): call without names to discov… | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_onboarding_github_status` | Return GitHub connection status for this team (Q1 verify). | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_onboarding_github_connect` | Initiate GitHub OAuth — returns authorize URL + CSRF state (Q1). | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_onboarding_session_recording` | Toggle automatic session recording for this team (Q3). | handler-served | agents, tooling, docs | **keep** | in use — called by agents |
| `tortoise_onboarding_state` | Return this team's onboarding progress (Q6 verification). | handler-served | agents, tooling, docs | **keep** | in use — called by agents |

### journal — 5

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_diary_write` | Write an agent diary entry (AAAK format suggested). Creates a Point with pointKind=diary, authoredBy=agent. | diary_write | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_checkpoint` | Session batch save — two-tier dedup (content hash + embedding similarity). Returns {filed: N, duplicates: M}. | checkpoint | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_diary_read` | Read recent diary entries for an agent, newest first. | diary_read | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_file_decision` | File a simple decision directly to the graph. Creates decision + options + evidence + IMPL edges atomically. | file_decision | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_file_human_approval` | File a human approval of a planning artifact to the graph. Creates Event (eventKind: humanApproval) + decision Point (pointKind: humanApproval) + unidirectional IMPL fan-out (la… | file_human_approval | agents, tooling, docs | **keep** | in use — called by agents |

### sources — 2

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_create_document` | Create a Document node (research, planDoc, meetingNotes, etc.). | create_document | never called, tooling, docs | **merge** | in group `create` · never called · **proposed: move to `graph`** — `tortoise_create_entity` in the same group already does this job (its own description names what it consolidates), so this is one name too many — the caller keeps working either way · group `create` is split across ['graph', 'sources']; proposed family `graph` — waits for your approval |
| `tortoise_create_source` | Create a Source node for provenance (document, web, db, etc.). Sources track content origin — url is the permalink key. tier (T0-T4 / legacy alias) stores the credibility tier;… | create_source | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |

### review — 3

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_promote_point` | Reviewer-gated draft→live promotion — the only path a draft extraction Point may go live (quarantine lock, R16 operator promotion, deferred dedup/temporal wiring). WRITES status… | promote_point | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_list_dedup_candidates` | Review queue for dedup/temporal candidates (candidate_type=content/temporal/entity). | list_dedup_candidates | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |
| `tortoise_approve_merge` | Review a dedup/temporal candidate — action=merge/reject. Wiring deferred to promotion for live priors. WRITES review flags + wires edges (idempotent for repeats). | approve_merge | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |

### mining — 1

| Tool | Does | Reaches | Used by | Recomm. | Status and rationale |
|---|---|---|---|---|---|
| `tortoise_mine_conversations` | Mine agent conversations (single transcript or corpus batch) into the graph — extraction, entity reification, content dedup, temporal wiring, W-3 batch gate. Batch per-file fail… | mine_corpus | never called, tooling, docs | **keep** | never called — not called over MCP, but used by tooling — removing it would break that |

---

## Retired names — 16 (off the advertised surface, not out of the product)

A retired name is **no longer advertised**: it is absent from `tools/list`, so it is not
counted above. It is **not gone** — calling it still works and now returns an answer that
carries a warning naming the replacement (#3883). The ability to warn is what makes a
retirement safe; a retired name that stopped resolving would be a silent removal.

| Retired name | Use instead | SDK method it declared | Was |
|---|---|---|---|
| `tortoise_get` | `tortoise_get_entity(id, type=...)` | `—` | http |
| `tortoise_get_events` | `tortoise_get_entity(None, type="events")` | `get_events` | http |
| `tortoise_get_governance` | `tortoise_get_entity(id, type="governance")` | `get_owned_entities` | http |
| `tortoise_get_operator` | `tortoise_get_entity(id, type="operator")` | `get_point` | http |
| `tortoise_get_point` | `tortoise_get_entity(id, type="point")` | `get_point` | http |
| `tortoise_health` | `tortoise_overview(section="health")` | ~~health~~ (no such method) | http |
| `tortoise_index_sessions` | `tortoise_index_files(directory)` | `index_sessions` | stdio-only |
| `tortoise_ingest_corpus` | `tortoise_index_files(directory)` | `ingest_corpus` | stdio-only |
| `tortoise_list_pointkinds` | `tortoise_overview(section="pointkinds")` | `list_pointkinds` | http |
| `tortoise_list_sources` | `tortoise_overview(section="sources")` | `list_sources` | http |
| `tortoise_list_tags` | `tortoise_overview(section="tags")` | `list_tags` | http |
| `tortoise_paginated_query` | `tortoise_query(offset=..., limit=...)` | `paginated_query` | http |
| `tortoise_query_points_by_tag` | `tortoise_query(tag=...)` | `query_points_by_tag` | http |
| `tortoise_stale` | `tortoise_overview(section="stale")` | `stale_points` | http |
| `tortoise_status` | `tortoise_overview(section="status")` | `status` | http |
| `tortoise_taxonomy` | `tortoise_overview(section="taxonomy")` | `taxonomy` | http |

---

## The SDK methods

Grouped by **who reaches them**. This is the ranking that matters for cutting: everything in
`agent-reachable` is called by an agent today; everything below it is not.

### agent-reachable — 88

| Method | Does / depends on | Used by | Recomm. | Rationale |
|---|---|---|---|---|
| `TortoiseSDK.get_events` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_get_events (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.get_owned_entities` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_get_governance (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.index_sessions` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_index_sessions (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.ingest_corpus` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_ingest_corpus (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.list_pointkinds` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_list_pointkinds (retired) | tooling, skill-docs, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.list_sources` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_list_sources (retired) | tooling, skill-docs, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.list_tags` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_list_tags (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.paginated_query` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_paginated_query (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.query_points_by_tag` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_query_points_by_tag (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.stale_points` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_stale (retired) | tooling, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.status` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_status (retired) | eval-harness, tooling, skill-docs, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.taxonomy` | reached by 0 registered tool(s) and 1 retired name(s): tortoise_taxonomy (retired) | eval-harness, tooling, skill-docs, docs | **keep** | reached by 0 registered tool(s) and 1 retired name(s) |
| `TortoiseSDK.get_point` | reached by 0 registered tool(s) and 2 retired name(s): tortoise_get_operator (retired), tortoise_get_point (retired) | eval-harness, tooling, docs | **keep** | reached by 0 registered tool(s) and 2 retired name(s) |
| `TortoiseSDK.annotate_operator` | reached by 1 registered tool(s): tortoise_annotate_operator | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.approve_merge` | reached by 1 registered tool(s): tortoise_approve_merge | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.assess_source` | reached by 1 registered tool(s): tortoise_assess_source | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.audit` | reached by 1 registered tool(s): tortoise_audit | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.backfill_v25` | reached by 1 registered tool(s): tortoise_backfill_v25 | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.belief_timeline` | reached by 1 registered tool(s): tortoise_belief_timeline | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.calibrate_summary` | reached by 1 registered tool(s): tortoise_calibrate_summary | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.check_structure` | reached by 1 registered tool(s): tortoise_check_structure | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.checkpoint` | reached by 1 registered tool(s): tortoise_checkpoint | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.compute_confidence` | reached by 1 registered tool(s): tortoise_compute_confidence | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_document` | reached by 1 registered tool(s): tortoise_create_document | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_edge` | reached by 1 registered tool(s): tortoise_create_edge | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_entity` | reached by 1 registered tool(s): tortoise_create_entity | eval-harness, tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_event` | reached by 1 registered tool(s): tortoise_create_event | eval-harness, tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_object` | reached by 1 registered tool(s): tortoise_create_object | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_operator` | reached by 1 registered tool(s): tortoise_create_operator | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_point` | reached by 1 registered tool(s): tortoise_create_point | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_source` | reached by 1 registered tool(s): tortoise_create_source | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.create_subject` | reached by 1 registered tool(s): tortoise_create_subject | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.delete` | reached by 1 registered tool(s): tortoise_delete | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.delete_entity` | reached by 1 registered tool(s): tortoise_delete_entity | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.delete_point_wrapped` | reached by 1 registered tool(s): tortoise_delete_point | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.diary_read` | reached by 1 registered tool(s): tortoise_diary_read | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.diary_write` | reached by 1 registered tool(s): tortoise_diary_write | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.dream` | reached by 1 registered tool(s): tortoise_dream | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.dream_health_check` | reached by 1 registered tool(s): tortoise_dream_health | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.events_poll` | reached by 1 registered tool(s): tortoise_events_poll | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.expand_relationships` | reached by 1 registered tool(s): tortoise_expand_relationships | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.file_decision` | reached by 1 registered tool(s): tortoise_file_decision | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.file_human_approval` | reached by 1 registered tool(s): tortoise_file_human_approval | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.get_cross_lens_candidates` | reached by 1 registered tool(s): tortoise_find_cross_lens_candidates | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.get_confidence` | reached by 1 registered tool(s): tortoise_get_confidence | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.get_entity` | reached by 1 registered tool(s): tortoise_get_entity | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.get_session` | reached by 1 registered tool(s): tortoise_get_session | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.get_source_reliability` | reached by 1 registered tool(s): tortoise_get_source_reliability | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.index_directory` | reached by 1 registered tool(s): tortoise_index_files | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.ingest` | reached by 1 registered tool(s): tortoise_ingest | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.invalidate_point` | reached by 1 registered tool(s): tortoise_invalidate | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.issue_insight` | reached by 1 registered tool(s): tortoise_issue_insight | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.list_batch` | reached by 1 registered tool(s): tortoise_list_batch | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.list_batches` | reached by 1 registered tool(s): tortoise_list_batches | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.list_dedup_candidates` | reached by 1 registered tool(s): tortoise_list_dedup_candidates | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.list_graphs` | reached by 1 registered tool(s): tortoise_list_graphs | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.list_namespaces` | reached by 1 registered tool(s): tortoise_list_namespaces | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.list_topics` | reached by 1 registered tool(s): tortoise_list_topics | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.mine_corpus` | reached by 1 registered tool(s): tortoise_mine_conversations | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.mitigate_operator` | reached by 1 registered tool(s): tortoise_mitigate_operator | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.operator_action` | reached by 1 registered tool(s): tortoise_operator_action | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.org_create` | reached by 1 registered tool(s): tortoise_org_create | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.promote_point` | reached by 1 registered tool(s): tortoise_promote_point | eval-harness, tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.provenance` | reached by 1 registered tool(s): tortoise_provenance | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.query` | reached by 1 registered tool(s): tortoise_query | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.recall_state` | reached by 1 registered tool(s): tortoise_recall | eval-harness, tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.retract_point` | reached by 1 registered tool(s): tortoise_retract_point | eval-harness, tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.review_connections` | reached by 1 registered tool(s): tortoise_review_connections | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.tortoise_fts_query` | reached by 1 registered tool(s): tortoise_search | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.search_sessions` | reached by 1 registered tool(s): tortoise_search_sessions | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.session_context` | reached by 1 registered tool(s): tortoise_session_context | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.set_point_baseline` | reached by 1 registered tool(s): tortoise_set_point_baseline | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.set_source_tier` | reached by 1 registered tool(s): tortoise_set_source_tier | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.suggest_entry_points` | reached by 1 registered tool(s): tortoise_suggest_entry_points | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.summarize_structure` | reached by 1 registered tool(s): tortoise_summarize_structure | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.supersede` | reached by 1 registered tool(s): tortoise_supersede | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.traverse` | reached by 1 registered tool(s): tortoise_traverse | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.update` | reached by 1 registered tool(s): tortoise_update | eval-harness, tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.update_entity` | reached by 1 registered tool(s): tortoise_update_entity | tooling, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.update_point` | reached by 1 registered tool(s): tortoise_update_point | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.validate_domain` | reached by 1 registered tool(s): tortoise_validate_domain | tooling, skill-docs, docs | **keep** | reached by 1 registered tool(s) |
| `TortoiseSDK.reconcile_sessions` | reached by no registered MCP tool; caller categories: cli | tooling, docs | **review** | no MCP tool binds it; callers: cli |
| `TortoiseSDK.close` | reached by no registered MCP tool; caller categories: cli, documentation, engine, eval-harness, other-tooling, tenant-rest | eval-harness, tooling, skill-docs, docs | **review** | no MCP tool binds it; callers: cli, documentation, engine, eval-harness, other-tooling, tenant-rest |
| `TortoiseSDK.session_index_health` | reached by no registered MCP tool; caller categories: cli, engine | tooling, docs | **review** | no MCP tool binds it; callers: cli, engine |
| `TortoiseSDK.apikey_create` | reached by no registered MCP tool; caller categories: cli, tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: cli, tenant-rest |
| `TortoiseSDK.volunteer_context` | reached by no registered MCP tool; caller categories: cli, tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: cli, tenant-rest |
| `TortoiseSDK.recall_gaps` | reached by no registered MCP tool; caller categories: mcp-handler | tooling, docs | **review** | no MCP tool binds it; callers: mcp-handler |
| `TortoiseSDK.recall_subgraph` | reached by no registered MCP tool; caller categories: mcp-handler | tooling, skill-docs, docs | **review** | no MCP tool binds it; callers: mcp-handler |

### internal — 21

| Method | Does / depends on | Used by | Recomm. | Rationale |
|---|---|---|---|---|
| `TortoiseSDK.apikey_verify` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.calibration_passed` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.compute_reputation` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.create_direct_edge` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.dream_health_state` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.list_relations` | reached by no registered MCP tool; caller categories: engine | tooling, skill-docs, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.membership_get` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.quarantine_batch` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.resolve_id` | reached by no registered MCP tool; caller categories: engine | tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.ulid` | reached by no registered MCP tool; caller categories: engine | eval-harness, tooling, docs | **review** | no MCP tool binds it; callers: engine |
| `TortoiseSDK.annotate_ask_hits` | reached by no registered MCP tool; caller categories: engine, eval-harness, other-tooling | eval-harness, tooling, docs | **review** | no MCP tool binds it; callers: engine, eval-harness, other-tooling |
| `TortoiseSDK.delete_point` | reached by no registered MCP tool; caller categories: engine, other-tooling | tooling, skill-docs, docs | **review** | no MCP tool binds it; callers: engine, other-tooling |
| `TortoiseSDK.supersede_point` | reached by no registered MCP tool; caller categories: engine, other-tooling, tenant-rest | tooling, skill-docs, docs | **review** | no MCP tool binds it; callers: engine, other-tooling, tenant-rest |
| `TortoiseSDK.invitation_get_by_id` | reached by no registered MCP tool; caller categories: engine, tenant-rest | docs | **review** | no MCP tool binds it; callers: engine, tenant-rest |
| `TortoiseSDK.link_source_to_entity` | reached by no registered MCP tool; caller categories: engine, tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: engine, tenant-rest |
| `TortoiseSDK.membership_create` | reached by no registered MCP tool; caller categories: engine, tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: engine, tenant-rest |
| `TortoiseSDK.org_get` | reached by no registered MCP tool; caller categories: engine, tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: engine, tenant-rest |
| `TortoiseSDK.signup_token_lookup` | reached by no registered MCP tool; caller categories: engine, tenant-rest | docs | **review** | no MCP tool binds it; callers: engine, tenant-rest |
| `TortoiseSDK.topic_summarize` | reached by no registered MCP tool; caller categories: engine, tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: engine, tenant-rest |
| `TortoiseSDK.sweep_invite_ghost_memberships` | reached by no registered MCP tool; caller categories: other-tooling | tooling, docs | **review** | no MCP tool binds it; callers: other-tooling |
| `TortoiseSDK.graph_list` | reached by no registered MCP tool; caller categories: other-tooling, tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: other-tooling, tenant-rest |

### control-plane — 2

| Method | Does / depends on | Used by | Recomm. | Rationale |
|---|---|---|---|---|
| `TortoiseSDK.apikey_revoke` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.graph_delete` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |

### no-caller-found — 39

| Method | Does / depends on | Used by | Recomm. | Rationale |
|---|---|---|---|---|
| `TortoiseSDK.apikey_list` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.backfill_about_entities` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.backfill_sources` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.batch_create_points` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.capture_session` | reached by no registered MCP tool; caller categories: none outside tests | eval-harness, tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.cleanup_expired_invitations` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.commit_session` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.complete_source` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.create_derivation` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.create_or_update_point` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.get_org_structure` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.get_provenance_chain` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.index_file` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.invitation_accept` | reached by no registered MCP tool; caller categories: none outside tests | docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.invitation_create` | reached by no registered MCP tool; caller categories: none outside tests | docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.invitation_get_by_token` | reached by no registered MCP tool; caller categories: none outside tests | docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.list_drafts` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.membership_delete` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.membership_list` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.membership_update_role` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.migrate_orgs_to_registry` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.org_delete` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.org_list` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.org_update` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.record_calibration` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.restore_point_at` | reached by no registered MCP tool; caller categories: none outside tests | tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.retrieval_legs` | reached by no registered MCP tool; caller categories: none outside tests | eval-harness, tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.test_guard` | reached by no registered MCP tool; caller categories: none outside tests | eval-harness, tooling, docs | **review** | no MCP tool binds it, and no caller outside tests was found |
| `TortoiseSDK.graph_active_key_count` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.graph_count` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.graph_key_ids` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.graph_restore` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.graph_set_name` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.graph_set_recording` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.invitation_list` | reached by no registered MCP tool; caller categories: tenant-rest | docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.invitation_revoke` | reached by no registered MCP tool; caller categories: tenant-rest | docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.signup_token_recover` | reached by no registered MCP tool; caller categories: tenant-rest | docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.signup_token_revoke` | reached by no registered MCP tool; caller categories: tenant-rest | docs | **review** | no MCP tool binds it; callers: tenant-rest |
| `TortoiseSDK.trash_graphs` | reached by no registered MCP tool; caller categories: tenant-rest | tooling, docs | **review** | no MCP tool binds it; callers: tenant-rest |

---

## What I recommend, in one place

Every row above carries its own recommendation. This is the same thing gathered up, because
deciding 82 rows one at a time is not a reasonable thing to ask of you.

**But these 82 lines are not all the same kind of statement, and the difference matters:**

- **11 rows execute a decision Tortoise has already made.** The declaration itself
  names one tool canonical and the other a duplicate. Correcting these follows from what we
  already decided to be. **These are recommendations in the strong sense.**
- **71 rows only describe what is being done** — called, referenced, or not.
  Usage is not a decision, and it does not get to decide what we are. A tool nobody calls may
  be exactly what we decided Tortoise is, for a user we have not reached yet; a tool everyone
  calls may still be the wrong capability. **These rows are evidence for your decision, not a
  substitute for it.** I have stopped labelling them keep/kill, because that was me treating
  what is as what ought to be.

### Fold in — 11 tools that duplicate a sibling

Each of these is already covered by the tool named after the arrow, because that tool's
**own description** says it consolidates this one. The caller keeps working; we stop
shipping two names for one job. This is the single biggest reduction available and the
only one where the evidence is the code's own words.

- **`tortoise_update` absorbs 2:** `tortoise_update_entity`, `tortoise_update_point`
- **`tortoise_delete` absorbs 2:** `tortoise_delete_entity`, `tortoise_delete_point`
- **`tortoise_create_entity` absorbs 4:** `tortoise_create_document`, `tortoise_create_event`, `tortoise_create_object`, `tortoise_create_subject`
- **`tortoise_operator_action` absorbs 2:** `tortoise_annotate_operator`, `tortoise_mitigate_operator`
- **`tortoise_get_entity` absorbs 1:** `tortoise_get_session`

### In use today — 71 tools (observed, not a verdict)

Called by agents, or referenced by an eval harness, internal tooling, or a skill.
**This says we use them; it does not say we should.** Which of these we keep is a statement
about what Tortoise is, and that is yours to make, not a reading of our own logs.

**Net effect if you accept the one concrete action and none of the judgement calls:**
82 tool names → **71**. Nothing an agent can
call disappears — the folded names are the same capability under a name the code already
designates as canonical.

## On the numbers alone

- **0 entries declare an SDK method that does not exist.**
- **39 SDK methods are reached by no agent path** — no MCP tool, no CLI verb, no tool handler, which is what `no-caller-found` means here. Of those, **11** are called from the tenant REST surface, so they are reachable by a client but not by an agent inside the gate; the remaining 28 have no caller outside tests at all.
- **21 SDK methods are reachable only from our own engine, our tooling, or the tenant REST surface** — reachable by something, but by no agent path inside the gate. (Methods reachable ONLY from tenant REST are counted in the class above, not here.)
- **Only 3 tools are referenced by an eval harness** (`tortoise_recall`, `tortoise_search`, `tortoise_check_structure`) — so almost none of this surface is covered by an evaluation.

## The surface size — measured, and argued to you

The comparable research measured one thing: **we advertise all 82 tools at once**, where no
comparable agent-memory system advertises more than 18 (Cognee 4 · Mem0 9 · Graphiti 13 ·
Letta 18). That is a fact about the field and a fact about us.

**I first read it as contradicting a decision, and that was wrong — in the direction that is
easy to miss.** I said an agent shown 14 of our tools and not told about the other 68 is being told
something "quietly incomplete", and dropped the candidate as one that could not be adopted at
all. But the decision I cited — *"delete what we can · de-identify what must persist · disclose
what nobody can remove"* — governs **promises about user data**. It says nothing about how many
tools we advertise. I got there by analogy, not by a decision, which is the same fault in the
opposite direction: reaching for a ruling as authority where the ruling's scope does not reach.
**No decision governs advertised tool count. The contradiction test returns nothing to
contradict.**

So this is a live question, and the honest thing is to argue it rather than drop it. Here is the
argument, both ways, with the weak parts named.

**The case for pinning a small advertised set.** Two thirds of what we advertise has never been
called by anything, including us (55 of 82). Mainstream clients cap the tools they will show — a
reported 40 in Cursor — so a large part of our surface is not merely unused, it is invisible
anyway, and we pay context for it on every turn. Every comparable we studied pins a smaller set,
and the pattern is not novel here: `tortoise_recall` is already one tool with four modes and
`tortoise_get_entity` already absorbed the six fetch-by-id getters. Deferring the rest keeps all 82 callable.

**The case against, which is real and not a formality.** Tortoise is genuinely broader than the
comparables — a graph memory *and* a reasoning engine with sessions, sources and mining — so some
of the gap is real scope, not fat. Tool search adds a hop and a failure mode: an agent that does
not know a capability exists may not think to look for it, and "the tool existed but was not
advertised" is a worse failure than a long list. And the fix is not obviously worth its cost —
the 11 rows above remove names where the declaration already says a name is redundant, which
shrinks the surface without inventing a discovery mechanism.

**The evidence does not settle it.** The comparison is solid; the thresholds behind "too many
tools" are practitioner opinion, not specification — the MCP spec says nothing about tool count.
So this goes to you as an argument, which is the whole point: I am not adopting it, and I am not
quietly dropping it either.

**Whichever way you decide, the measurement stands.** Refusing a change is not a verdict on the
finding — the numbers do not stop being true because 82 stays advertised, and they do not become
an instruction because the surface is tiered. They are what a later reopen of this question would
be argued from, and they are recorded here so that argument does not have to be rebuilt. Full
evidence: `docs/research/2026-09-17-3863-agent-memory-surfaces.md`.
