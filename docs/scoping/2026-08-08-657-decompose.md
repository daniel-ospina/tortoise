---
title: "Legal pages for tortoise.premiselabs.co — Decomposition (Stage 5)"
type: legal
issue: "#657"
date: 2026-08-08
status: draft
domain: legal
doc_status: draft
subjects.team: epistemic-team
created: 2026-08-08
---

# Decomposition — Privacy + ToS pages for tortoise.premiselabs.co (#657)

**Inputs:** scope doc `docs/scoping/2026-08-08-657-legal-scope.md` (rev 7 — all G-gate items ①–⑩ LOCKED, DNS operator-fixed) · plan doc `docs/plans/2026-08-08-657-legal-pages-plan.md` (rev 9 — T1–T8) · issue #657 body + comments · live repo issue search (2026-08-08)

**Stage-5 role boundaries honored:** does NOT re-plan, does NOT write code. Verifies MECE-ness of the deliverable, confirms wiring, and defines the proportional verification plan.

---

## 1. MECE decomposition check

### Verdict: **Single deliverable — NO decomposition.**

#657 ships as **one PR**: 4 new legal pages (`privacy.html`, `tos.html`, `dpa.html`, `license.html`) + 5 footer/nav edits (`product.html`, `welcome.html` footers **created**, `signin.html`/`signup.html`/`self-hosted.html` appended) + signup clickthrough acceptance + `docs.html` mailto contact **RESTORED (D2 SUPERSEDED 2026-08-08 — `hello@premiselabs.co` mailbox live; the earlier mailto→GitHub-issues swap is reversed)** + `tests/e2e/test_legal_pages.py` + `website/.wranglerignore`. **NOTE (corrected at PR review):** the planned `deploy-pages.yml` post-deploy CI job does NOT ship with this PR — the repo's pages deploy workflow (`deploy-pages.yml`, push to main + `website/**`) exists but has **no post-deploy verification job**; the post-deploy CI job is tracked as **follow-up #677** and manual post-deploy verification (`curl` + `RUN_LEGAL_E2E=1 ALLOW_PROD=1` pytest against production) is the current path. Child-issue decomposition would fragment a single deploy and break acceptance criteria.

### 1a. Would child issues fragment a single deploy? — YES, they would. Do not split.

| Splitting vector | Why it fails |
|---|---|
| Pages issue vs footers issue | I1/I2 are inseparable: footer links on `product.html`/`welcome.html` (I2) point at pages that don't exist without I1; "0 broken links" (target) is violated by any partial ship. The E2E suite (T7) tests the whole loop — pages, footers, acceptance, docs.html — as one contract. |
| Content-draft issue vs build issue | T1/T2 drafts are review artifacts feeding the T3 G-gate; T4 renders from the **approved** drafts. Splitting drafting from rendering breaks the approve-render chain and the draft↔render fidelity gate (T4 Step 5 / T7 #10(g)). |
| Pages vs signup acceptance | The acceptance block links `/tos` + `/privacy`; a separate acceptance issue would ship links to 404s. |
| Build vs E2E/CI | T7 asserts content that T4 must have rendered; the CI post-deploy job (T8) asserts the served pages. Verification cannot ship independently of what it verifies. |
| Rollback cohesion | Revert = one PR merge revert; the push-triggered CI deploy restores prior content atomically (T8 Step 6). Child issues would create partial rollback states. |

### 1b. Are the plan's T1–T8 genuinely task-level steps (not issues)? — YES.

- **T1 (ToS draft)** / **T2 (Privacy draft)** → parallelizable drafting tasks producing `docs/drafts/*.md` review artifacts; neither is independently ship-able (no user-visible deliverable alone).
- **T3 (Owner G-gate)** → mandatory human stop, not a deliverable; gates T1/T2 output.
- **T4 (build 4 pages)** + **T5 (site markup edits)** → parallelizable build tasks on **disjoint file sets** (T4 creates new files, T5 edits existing) — the reason T5 merges former T5+T6 (concurrent-edit risk) applies equally to any split; T4 output is unlinked without T5, T5 output 404s without T4.
- **T7 (E2E)** → verification task; **T8 (deploy + PR)** → release task. Neither produces scope-value alone.

None of T1–T8 yields a coherent shippable state in isolation; each is an implementation step with its own acceptance criteria inside one issue. This is the task/issue boundary test passing.

### 1c. Are separable concerns ALREADY filed / tracked? — YES (verified live 2026-08-08).

| Separable concern | Status | Verified |
|---|---|---|
| Consent banner (analytics) | **#658 filed, OPEN** — ePrivacy/GDPR consent layer; blocked on #528; privacy page states the banner mechanism now | ✓ `gh issue list` |
| Entity status + reissue | In-scope tracking, **no separate issue** (session decision, scope §6.2 #2) — entity resolved (individual d/b/a); reissue via versioned procedure if residence/jurisdiction changes | ✓ scope §6.2 |
| Pricing-page consistency (free-tier numbers conflict "5,000 Points free" vs "1,000 write ops/mo") | In-scope tracking (scope §6.2 #4) — reconciled **under #432 or in this issue's build step** before the ToS hyperlinks the pricing page (T4 Step 6 + T8 **G-PricingRef** gate) | ✓ scope §6.2 #4 |
| DNS propagation | **Not an issue** — operator-fixed CNAME (2026-08-08); verification gate (T7 Step 0 + T8), not a work item | ✓ scope §5.7 / plan S10 |
| Self-service delete-account feature | **§6.2 follow-up #5 — NOT YET FILED** — conditional product follow-up; fires only if the T2 Step 5 build-time capability check confirms absence (2026-08-08 repo check expects ABSENT) | ⚠️ **NEW ISSUE RECOMMENDED — see §4** |

### 1d. MECE summary

**MECE at the ISSUE level holds:** the deliverable is one mutually-cohesive, collectively-exhaustive unit — every touch point in scope §6.1 has exactly one owning task (T4/T5/T7/T8); every separable concern is either extracted (#658) or explicitly tracked in-scope (§6.2 #2, #4) or is a verification step (DNS). No orphan surface, no duplicated ownership. The only item NOT yet externalized is the conditional self-service delete follow-up (§6.2 #5) — a **deferred product feature**, correctly OUTSIDE this deliverable's scope (it would add a backend auth-admin + dashboard UI feature to a static-content issue) but needing filing at T2 Step 5 (expected branch).

**Conclusion: single atomic deliverable — one PR, one deploy, one rollback unit. No child issues. T1–T8 execute in-sequence per the plan's Execution Handoff (T1∥T2 → T3 hard stop → T4∥T5 → T7 → T8).**

---

## 2. Wiring confirmation

Re-verified against scope §6.1 wiring table, plan surface map S1–S11, and the two most recent G-gate resolutions (⑥ pricing-ref, ⑧ self-service deletion). **Every touch point has exactly one owner/task — NO gaps.**

| Touch point | Owner (plan task) | Status |
|---|---|---|
| `privacy.html` / `tos.html` (new) | T4 Steps 2–3 (content from T1/T2 drafts) | Wired ✓ |
| `dpa.html` (new, REQUIRED G-gate ③) | T4 Step 4 | Wired ✓ |
| `license.html` (new, REQUIRED G-gate ⑨) | T4 Step 4 | Wired ✓ |
| `product.html` footer (create — none exists) | T5 Step 1 | Wired ✓ |
| `welcome.html` footer (create — none exists) | T5 Step 2 | Wired ✓ |
| `signin.html` / `signup.html` / `self-hosted.html` footers | T5 Step 3 | Wired ✓ |
| `signup.html` clickthrough acceptance (same-viewport) | T5 Step 4 (asserted T7 #7) | Wired ✓ |
| `docs.html` mailto contact RESTORED (D2 SUPERSEDED 2026-08-08 — mailbox live) | T5 Step 5 (asserted T7 #10, grep — mailto line must be present) | Wired ✓ |
| `functions/_middleware.ts` | S3 — verify only, no change (read confirmed) | Wired ✓ |
| Cloudflare Pages deploy | T8 Step 4 (CI-owned; `.wranglerignore` T8 Step 1) | Wired ✓ |
| CI post-deploy job | **Follow-up #677** — deploy-pages.yml has NO post-deploy job; add post-deploy pytest + DNS preflight + warn-only date compare | Wired ✓ (tracked #677) |
| #528 analytics (adjacent) | S5 — conditional PostHog-residency + Meta-sharing wording; **G-528/#658 re-check** named pre-merge gate (T8 Step 4) | Wired ✓ (non-blocking) |
| #432 billing (adjacent) | S6 — anticipatory fee clause; **G-PricingRef** gate (T8 Step 4); T4 Step 6 href-resolve | Wired ✓ (non-blocking) |
| #658 consent banner (follow-up) | Privacy page states banner mechanism; banner itself owned by #658 (blocked on #528) | Wired ✓ |
| DNS/CNAME (S10) | T7 Step 0 verification gate (dig + root-marker poll 30s × 10min; fresh at T8 start); `TORTISE_HOST_CHECK` CI contract | Wired ✓ (verification, not work) |
| E2E regression (S9, existing suite) | T7 Step 3 (`python -m pytest tests/e2e/`) | Wired ✓ |

### 2a. NEW wiring considerations introduced by recent G-gate resolutions — assessed, none are gaps:

1. **G-gate ⑧ (self-service deletion primary, 2026-08-08)** introduced: (a) **T2 Step 5 build-time capability check** — new task step, wired into T2 (informs ⑯ wording) and T7 (policy-vs-product IFF via T7 #10(c1) repo-state assertion + scoped-claim branch). Wired ✓. (b) **Conditional product follow-up (scope §6.2 #5)** — the filing action is owned by T2 Step 5's expected-ABSENT branch. **Wiring complete in-plan; the ISSUE itself does not yet exist (see §4).** The plan's scoped-claim wording makes the policy correct even if the follow-up is filed late — no correctness gap, only an action item.
2. **G-gate ⑥ (fees clause + hyperlinked Pricing Page, 2026-08-08)** introduced: T4 Step 6 (pricing-reference URL finalized at build + resolves) + T8 **G-PricingRef** named gate. Wired ✓. The **pricing reconciliation dependency** ("5,000 Points free" vs "1,000 write ops/mo") is explicitly owned "under #432 or in this issue's build step" — an execution-time dependency with a named gate, not an unwired surface. Flagged: the implementer must NOT let the ToS hyperlink the pricing page until reconciliation lands (G-PricingRef enforces this pre-merge).
3. **DNS operator-fix (2026-08-08)** converted a blocking unknown into a verification step — no new surface, gates updated (S10, T7 Step 0, T8 Step 5). Wired ✓.

**Wiring verdict: NO gaps. 14/14 touch points owned. Two execution-time actions flagged (not gaps): file §6.2 #5 follow-up at T2 Step 5 (expected), reconcile pricing numbers before G-PricingRef.**

---

## 3. Verification plan definition

**Tier:** Standard (single deliverable, content-domain heavy, config/UX low). Proportional depth: full E2E suite pre-deploy + CI post-deploy job + manual production verification. No unit/integration/pgTAP layers apply (zero backend code changed; middleware verified-only).

### Pre-deploy (before merge)

| Layer | Check | Where |
|---|---|---|
| Owner gate | **T3 OWNER-APPROVAL comment URL on #657** (owner-authored: explicit approval + decision log + link-set record) recorded in T4 output BEFORE any build work; no approval → T3 stays OPEN | T3 Step 2 / T4 acceptance |
| Local E2E suite | `tests/e2e/test_legal_pages.py` against `npx wrangler@4 pages dev` (port 8788, compatibility-date captured): checks 1–10 — 200s on /privacy,/tos,/dpa,/license; **negation-safe content** (canonical sentences + anti-pattern guards, no-sale co-occurrence, minimal-PII pair, anti-phrase absent, carve-out co-occurrence, dollar guard `== 1` on tos, fee-clause keywords, "30 days"+"renewal", eligibility verbatim "you must be at least 18 years old" + "18+"); footer link presence incl. `/dpa`+`/license` unconditional; **same-viewport acceptance at BOTH 1280×720 and 375×667** (bounding-box overlap); cross-host (Host-spoofed, canary/Host-echo-discriminated — deferral only on proven emulation gap with unchanged middleware); middleware root markers; mobile render @375px (no horizontal scroll + min-content/heading floors); 0 broken links crawl (enumerated per-host set + external links); HTML tag-balance (stack-tracking parser); served-content assertions (revision history, consent-banner sentence, repo-state IFF, placeholder-leak, present-tense expected-match subset, draft↔render fidelity) | T7 Steps 1–2 |
| Regression | Existing `tests/e2e/` suite green (legal suite auto-skips via `RUN_LEGAL_E2E` opt-in) | T7 Step 3 |
| DNS propagation | T7 Step 0: `dig +short` CNAME == `premise-labs.pages.dev` + root-marker curl; poll 30s × 10min; **re-run FRESH at T8 start**; stale → manual Cloudflare DNS edit (not continue-on-error CI sync), do NOT proceed to prod `tortoise.*` checks | T7 Step 0 |
| Named pre-merge gates (recorded pass/fail on #657) | **G-528/#658 re-check** (PostHog residency, Meta sharing, tool-name disclosures, consent-banner markers; re-draft if #528/#658 merged mid-cycle) · **G-PricingRef** (pricing href resolves in prod + free-tier numbers reconciled + fees sentence matches canonical wording) · **G-EffectiveDate** (`date +%F` recorded in merge commit message + changelog; bump BEFORE merge if deploy slipped) | T8 Step 4 |
| Commit-workflow pre-flight | T7 suite green + regression green; no typecheck (static content) | T8 Step 4 |

### Post-deploy

| Layer | Check | Where |
|---|---|---|
| CI post-deploy job | **Follow-up #677** (NOT shipped in this PR): deploy-pages.yml exists (push-to-main deploy only) but has NO post-deploy verification job — the legal E2E suite never runs in CI today. #677 tracks: deploy-completion poll (`/privacy` → 200, retry ~30s × 5–10min) → DNS preflight (green ⇒ `TORTISE_HOST_CHECK=1` ⇒ tortoise.* tests RUN; stale ⇒ skip green-with-annotation, premiselabs.co half still green) → pytest with `RUN_LEGAL_E2E=1 ALLOW_PROD=1` → warn-only effective-date vs deploy-commit-date comparison (unavailable ⇒ warn+skip, never error); failure response: re-run once → fix + comment #657 → reopen if red. Current path: manual post-deploy verification (T8 Step 5, curl + production pytest) | T8 CI job (S11) → #677 |
| Manual T8 Step 5 (authoritative) | Production pytest (`BASE_URL=https://premiselabs.co`, `TORTISE_HOST=https://tortoise.premiselabs.co`, DNS gate green) + curl checks: 200s on both hosts for all 4 legal URLs; root markers both hosts (pinned h1 strings); **upload-scope negative check** `premiselabs.co/apps/dashboard/index.html` → 404 (no `apps/` ships); negation-safe content also via curl on `premiselabs.co/privacy`; cache headers (etag + cache-control with no-cache fetch); **effective date == deploy date against SERVED content — HARD assert** (visible `Effective date: YYYY-MM-DD` vs merge-commit recorded `date +%F`); `onboarding-prompt.md` 200 + text/markdown; `/welcome` 200; footer spot-check both hosts | T8 Step 5 |
| Rollback classification | **REVERT-WORTHY:** negation-rule violation on served content, 404 on any legal URL, effective-date mismatch on served content (revert merge → CI auto-restores prior content). **FIX-FORWARD:** environmental/transient (DNS staleness → skip-annotated, cache edge, transient 5xx, poll timeouts). Known-issue state documented on #657 while fix in flight | T8 Step 6 |

**Deploy-surface detection:** static site (Cloudflare Pages) → post-deploy-verify style checks only (curl 200s + content markers + upload-scope negatives); no app restart, no DB migration, no backend smoke needed. `_middleware.ts` is verify-only.

### "Done" verification checklist

- [ ] T3 owner-approval comment URL recorded; T3 complete before any T4 build work
- [ ] T7 local suite green (checks 1–10, both viewports) + regression suite green
- [ ] DNS propagation gate green at T7 Step 0 and fresh at T8 start (or remediation documented on #657)
- [ ] All 3 named pre-merge gates (G-528/#658, G-PricingRef, G-EffectiveDate) recorded pass on #657
- [ ] PR merged with review signatures (commit-workflow; license-drift note in PR description)
- [ ] CI post-deploy job green (or tortoise.* skips annotated with reason; premiselabs.co half green)
- [ ] Manual T8 Step 5: production pytest + all curl checks green on both hosts; effective date == deploy date on served content (hard assert)
- [ ] `.wranglerignore` verified — `apps/dashboard` NOT served (404 negative check)
- [ ] Scope §5.5 acceptance checklist all pass (I1, I2, T1–T6 targets, owner approval, 0 broken links, revision-history blocks, same-viewport acceptance, **docs.html `mailto:hello@premiselabs.co` present — D2 SUPERSEDED**) 
- [ ] Follow-up actions recorded: §6.2 #5 self-service delete issue filed (if T2 Step 5 confirms absence — expected), #658/#528 companion-change ownership noted on those issues

---

## 4. NEW issue recommendation — self-service delete-account feature (scope §6.2 #5)

**Status check (live, 2026-08-08):** searched the repo for "delete account / self-service / deleteUser / account deletion" — **NO issue exists.** The consent-banner follow-up (#658) is filed; the self-service delete follow-up is **not**.

**Recommendation: FILE when T2 Step 5's build-time capability check confirms absence (expected per the 2026-08-08 repo check)** — dashboard shell only, graph-viz has no account-management UI, `server/main.py` exposes no user/account deletion endpoint, `welcome.html` key-management only. Do NOT file pre-emptively if the check somehow finds a working capability (the policy then claims the in-product path and no product work is needed). If the check is deferred, the privacy-page scoped claim ("request deletion via GitHub issues" + Art. 12(3) one-month + Art. 12(6) identity verification) keeps the policy legal in the interim — the filing is an action item, not a correctness blocker.

**Suggested title:** `feat(hosted): self-service delete-account — Supabase Auth admin deleteUser + dashboard account UI`

**Suggested body sketch:**
- **Context:** Product follow-up from #657 (scope §6.2 #5, G-gate ⑧ LOCKED 2026-08-08). The Privacy Policy ships with in-product self-service deletion as its PRIMARY erasure path; the build-time capability check (plan T2 Step 5) confirmed the product has NO working delete-account capability today — `apps/dashboard` is a shell, `apps/graph-viz` has no account-management UI, `server/main.py` has no user-deletion endpoint. The policy therefore scopes the claim ("request deletion via GitHub issues") until this ships.
- **O/I:** Objective — authenticated in-product account deletion (authenticated by construction → solves GDPR Art. 12/17 identity verification; instant; no 1-month clock on that path). Indicators — dashboard "Delete account" UI; Supabase Auth admin `deleteUser` (plus graph/points/edges cleanup for the user's data); privacy-policy wording updated from scoped claim to in-product path + revision-history bump + owner re-approval (versioned-reissue procedure).
- **Depends on:** hosted auth (#533 merged; Supabase Auth in place). Related: #657 (privacy wording), #658 (consent — unrelated), #296/#432 (account/billing context).
- **Note:** the post-deploy CI legal-pages job's repo-state assertion (T7 #10(c1)) expects "no analytics tools are currently deployed" only — account-deletion capability is a separate surface; the privacy wording change ships with THIS issue via the versioned-reissue procedure, not silently.

---

## 5. Handoff

- **Decompose verdict:** single deliverable — NO child issues; wiring complete (no gaps); verification plan above is the contract for T7/T8.
- **Next gate:** `writing-plans`/`executing-plans` per project-workflow — T1∥T2 (parallelizable) → **T3 owner G-gate (hard human stop; owner-authored approval comment required on #657)** → T4∥T5 → T7 → T8.
- **Execution-time actions (not gates):** file §6.2 #5 self-service delete follow-up at T2 Step 5 (expected branch); reconcile free-tier pricing numbers before G-PricingRef (under #432 or build step).
- **Labels:** #657 carries `scoped` + `implementing` — applicable through implementation; `planned` applies after plan-review sign-off (already complete — plan rev 9).
