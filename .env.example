# Tortoise — required environment variables
# Copy this file to .env and fill in values:
#   cp .env.example .env

# FalkorDB connection URI (REQUIRED)
# Docker mode: docker://:password@host:16379/graph_name
# File mode:   /path/to/tortoise.db (embedded, for testing only)
TORTOISE_DB_URI=docker://:falkordb@localhost:16379/tortoise

# FalkorDB connection details (used by some CLI tools)
FALKORDB_HOST=localhost
FALKORDB_PORT=16379
FALKORDB_PASSWORD=falkordb

# API key for production mode (optional — unset for dev mode)
# TORTOISE_API_KEY=tt_your_key_here
